# Explain the upload-recovery page visibility timeout

- Severity: Low
- Status: Resolved
- Archive disposition: Resolved
- Archived: 2026-09-08
- Owner: P005 acceptance harness; later browser regression owners
- Recorded: 2026-09-07
- Affected files: tests/e2e/p005.ts; apps/web/src/App.tsx and Attachments.tsx if reproduction implicates application state
- Acceptance: P005-02/05; deterministic regression reliability

## Observed evidence and limits

An initial combined Node 24.11.1 `pnpm test:e2e` run exited 1 at the upload-response-recovery navigation: the `Message Codex` element was not found within the existing five-second visibility assertion. Source was `85cf8fd5a835b5b8ca8bd9cd7a2177c3a04c23179c4984236d52a70a93ec7105`. The retained log observation is `.test-runs/harbor-e2e-6d6c1fabe8/observed-failure.json` in the P005 integration worktree, written after the run from execution output. No screenshot, page body or navigation status was captured at the failure; the cause is unknown. This is not evidence of a specific application race or a proven timing-only test defect.

The subsequent test change asserts navigation HTTP 200 and includes the rendered body in the error without relaxing visibility or adding an automatic retry. Full Node 24 E2E `harbor-e2e-055d710de4` passed on `b4de09a4f03ab6857b9c966e21a7a3dfcdd932afdb366ff0b1da9c7a7ba88d71` (854 files; identical start/end), with no production-code change. A passing rerun does not establish a correction for the earlier failure. The independent integration reviewer accepted the feature evidence with this limitation retained in the [report](../../docs/reports/2026-09-07-p005-integration.md).

## Reproduction and correction — 8 September 2026

On 2026-09-08, two later feature branches failed different assertions around this recovery phase: P009 run `harbor-e2e-1812dba56e` could not find the incomplete-upload text after reload; P006's first full regression did not reach selected-file visibility after reload. Neither captured its own failing HTTP/page state, so a shared cause is not established for those historical runs.

A fresh root Node 24 reproduction, `harbor-e2e-587ce35021`, captured `.test-runs/harbor-e2e-587ce35021/p005-paused-reload-failure.json`: navigation/assets/identity returned 200, but project roots, project/session lists, capabilities, snapshot, attachments and draft startup calls returned 429. The intended conversation remained selected in the URL; the composer was absent, the rate-limit message visible, and PostgreSQL retained the expected 22-byte `uploading` row with no content. This directly identifies shared test-request pressure for that reproduction, without retroactively proving the cause of the original observation.

A separate implementer added two deliberate 10.5-second browser phase boundaries and removed the existing mutation helper's automatic 429 retry. Production rate limits and the five-second UI assertions remain unchanged. Status-only diagnostic capture remains bounded and contains no cookies, headers, file content or credentials. `pnpm check` and fresh full Node 24 E2E `harbor-e2e-309d140acb` passed on exact source `69193104445daedf16d5beb69db00e790ded8ae7a9c79b1d2c220bcaddc01644` (854 files, identical start/end).

## Closing evidence

One independent review round closed with no actionable finding. The reviewer checked both phase boundaries, removal of automatic mutation retries, unchanged acceptance assertions, the bounded diagnostic and matching-source passing result. The fix clears deliberate setup pressure before the browser fault scenarios without hiding rejected user actions. It changes only `tests/e2e/p005.ts`; production behavior and the previously tested Linux boundary are byte-identical.

Validation used Node 24.11.1 on macOS, Chromium 1194, fresh Docker PostgreSQL/Caddy, the real Harbor API/supervisor/UI and isolated external Codex/OIDC fixtures. Commands were `pnpm check` and `pnpm test:e2e`; both exited 0. Logs are `.test-runs/p005-paced-check-20260908.log` and `.test-runs/p005-paced-e2e-20260908.log`; the E2E result retains the exact source digest above. The [integration report addendum](../../docs/reports/2026-09-07-p005-integration.md#acceptance-harness-correction--8-september-2026) records this later checkpoint separately from the original integration evidence. This resolves the reproduced harness defect and missing diagnostics; it does not establish the cause of earlier uncaptured failures or close [P005's](../../design/proposals/archive/005-attachments-and-rich-input.md) separate live-account gate.

A future browser run may fail before upload recovery is exercised. Preserve the new HTTP/page diagnostic on recurrence, add a bounded failure screenshot/console/network observation if necessary, and reproduce the exact navigation in a fresh owned instance before changing application code or timing. Keep the intended visible-composer assertion. A reproducible application failure receives its own severity reassessment and fix/review evidence. No user or normal deployment state was used in this observation.

## Ownership navigation — 20 September 2026

The disposition and evidence above are historical. Retired proposal references identify feature lineage; any unfinished inherited acceptance is retained in the [active inbox](../) and the [current subsystem designs](../../design/systems/). The P017 baseline migration did not resolve another finding or rerun this record’s checks.
