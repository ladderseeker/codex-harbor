# P007 — Session history and recovery

- Decision: Accepted
- Delivery: Implemented
- Dependencies: [P001](001-secure-persistent-conversations.md)
- Outcome: The owner finds previous work, reconnects across storage/restart gaps, and resolves interrupted or uncertain operations without accidental replay.

## Scope and user/API flow

Add searchable/paginated conversation history, rename/archive/unarchive, durable status visibility, snapshot resynchronization after replay retention, and a recovery view for interrupted or uncertain operations. The owner can inspect the last confirmed state and deliberately continue with new input or leave the session stopped. P001 already supplies basic session reopening, browser-disconnect survival, and safe crash classification; this feature must not be required to make P001 safe.

Provide corresponding authenticated history, archive, snapshot, and recovery operations. Reopening a conversation does not automatically restart an old turn. Permanent deletion can be excluded from initial delivery; archival must not delete source folders or active work. Whole-VPS backup/restore is P009; scheduled work is P008.

## Contracts and ownership

Maintain application history indexes and native thread references without editing Codex's private database schema. Native history remains authoritative for conversation content; UI event retention can be shorter. A snapshot identifies its sequence, and a client resumes strictly after that sequence. Detect expired cursors, known gaps, and stale runtime generations.

Use the [operation state machine](../architecture.md#api-events-and-state-transitions). Persist and reconcile dispatch intent, recorded response, native history, and current runtime state. If they cannot establish delivery, keep `uncertain` visible. The owner may explicitly start a new operation after inspecting that risk; never label it an exactly-once continuation. Approval answers and cancellation need the same reconciliation discipline.

Database outage denies new mutations and approval answers; existing authorized work follows bounded buffering/emergency rules. Restoring connectivity cannot manufacture missing events. History search is owner-authorized, bounded, and treats snippets as untrusted content. Active operations/approvals and referenced attachments must survive retention maintenance.

## Independent acceptance

Seed delivered P001 with completed, waiting, failed, and long-running conversations plus deterministic crash points. Use the real database/supervisor and controlled external runtime fixture; no scheduler or deployment UI is needed.

1. **P007-01:** Search, paginate, rename, archive, and reopen conversations. Assert consistent UI/API results and unchanged native conversation/source files.
2. **P007-02:** Reconnect before and after event retention, including a snapshot/stream race. Assert no missing confirmed state, no duplicate rendered items, and explicit resynchronization for a gap.
3. **P007-03:** Kill the runtime before dispatch, after possible delivery, and after execution before acknowledgement. Assert the appropriate interrupted/uncertain state and no automatic duplicate side effect.
4. **P007-04:** Recover a waiting approval after viewer reconnect, then restart its runtime and submit the old answer. Assert the live request is answerable once and the stale request expires.
5. **P007-05:** Make PostgreSQL unavailable during active work, restore it, and exceed the bounded event buffer separately. Assert mutation denial, documented gap/recovery state, and resource bounds.
6. **P007-06:** Resolve uncertainty deliberately, continue a recovered conversation with a new operation, and retry that request. Assert explicit acknowledgement of uncertainty and deduplicated new input.
7. **P007-07:** Repeat native history/restart/resume checks with the pinned real runtime and a bounded live turn. Assert supported behavior rather than inferring it from fixture output.

## Delivery and verification

Use planned `pnpm check`, `pnpm test`, `pnpm test:e2e`, `pnpm test:contract`, and relevant `pnpm test:live`. Fault injection must affect only the run's services and storage. Follow [shared setup/evidence/cleanup rules](README.md#shared-verification-contract). Do not run host reboot or process-kill tests against the developer's normal instance.

Migrate indexes additively, document retention and recovery limitations, and preserve the original native state during reconciliation. Rollback cannot blindly downgrade a native history format; require the [release compatibility rules](../architecture.md#release-retention-and-rollback-rules). Record evidence and independent review before delivery is verified.

## Recorded implementation refinements — 7 September 2026

These decisions refine the owner's authorized P007 direction before implementation. They do not establish delivery evidence.

### History and metadata

P007 owns conversation archival as a visibility-only change, superseding P003's earlier idle-only conversation archive admission. Its additive migration preserves P003's `archived_at` field. Project/workspace archival remains P003's idle-gated operation; integration redirects its legacy conversation archive endpoint to the P007 metadata semantics.

`GET /api/v1/history` uses a bounded query (at most 120 characters), active/archived/all filter, optional project ID, a stable `(created_at,id)` keyset cursor, and page size 1–50. Search matches title and durable message text with a bounded plain-text snippet; results never render HTML. Pagination order is immutable creation order so a rename or new event cannot silently move an existing result across pages. A search cursor includes its query/filter fingerprint and rejects use with changed filters. SQL statement timeouts and the existing conversation/message limits bound searches; pagination never counts all matches.

`POST /sessions/:id/metadata` carries `expectedRevision`, optional title and archived flag. Revision conflicts return 409. Archiving changes visibility only: source folders, native history, messages, queued/running work and approvals remain intact. Default navigation lists active sessions. An archived session remains directly readable and can be explicitly reopened/unarchived. Permanent deletion is excluded.

### Recovery state and explicit new input

`GET /sessions/:id/recovery` reports unresolved operations, current generation, last confirmed application cursor/state, and the latest recovery's bounded native-history/read status. Original uncertain operations remain `uncertain` permanently; a separate acknowledgement marker records the owner's later decision without relabeling old effects as successful.

`POST /sessions/:id/recovery` accepts an idempotency key plus `expectedGeneration` and requests confirmed fencing. Only a session with unresolved uncertainty is eligible. A separate durable recovery record moves `queued` → `fencing` → `ready`, or `failed`. Only one nonterminal recovery per session is allowed. Duplicate keys return the same record; stale generation or a competing recovery returns 409. Expired owner authority rejects an unstarted recovery. No ordinary turn is admitted during unresolved uncertainty or fencing.

The supervisor removes the exact captured runtime, waits for trusted confirmed absence, and reconciles the persisted launcher identity after process restart. It advances the session generation only after the old runtime is confirmed stopped. A bounded read-only runtime probe may inspect the saved native thread; it never sends a turn or approval and is itself retired with confirmed absence before `ready`. Known native assistant items can repair missing or nonfinal application projection items by stable native item ID. A confirmed complete application item is never overwritten by a conflicting or incomplete native read: the bounded report records conflict/truncation/gap provenance. Native reads are bounded to 2 MiB/2,000 text items; unavailable or truncated reads are explicitly reported and never imply complete history. Native read failure remains explicit and does not manufacture a complete transcript. Missing mapping or delivery evidence never changes the original uncertain operation into a success. All old pending approvals expire, and old queued turns are cancelled rather than silently released. Any unconfirmed retirement leaves recovery failed and the original uncertainty blocking.

`POST /sessions/:id/recovery/continue` requires `recoveryId`, its fenced `expectedGeneration`, `acknowledgeUnknownEffects: true`, and explicit new turn input/settings. In one transaction it verifies the ready fence, acknowledges the listed original uncertain operations, consumes that recovery, and admits a fresh operation through normal quota/policy checks. Exact retries return that same new operation. A new key after consumption, stale epoch, or competing caller returns 409. The original prompt is never automatically copied or resent. A failed transaction acknowledges nothing. Durable idempotency lookup precedes current generation/consumed-state preconditions so a response-lost accepted continuation still returns its original new operation after restart. Native approval requests remain pending when an answer is provably rejected before send; only transport/commit uncertainty poisons delivery. The owner may leave a ready recovery stopped indefinitely.

Recovery is owner-browser-only in the P001 implementation. Later programmatic exposure requires an explicit policy descriptor rather than inheriting authority from a route prefix. P003 is not a dependency: an optional supervisor hook runs after confirmed fencing to release its exact workspace reservation; that hook preserves original uncertainty. P007 never assumes release alone reconciles a conversation.

### Replay and reserved control capacity

Replay rows are disposable projections; durable messages, operations, approvals, and necessary intent results remain intact. Each event transaction removes rows at or below `current_sequence - 2000`, and rows older than seven days, including unresolved sessions. A monotonic replay watermark records what was removed. Maintenance and snapshot/stream reads enforce the same age predicate; an expired cursor or any known internal gap returns explicit `resync`. Snapshot and its cursor are read under the same session lock; clients subscribe strictly after that cursor and replace their stream after resync.

Ordinary turn admission stops at 400 retained operation/recovery records in a conversation, with a hard 500-record total for new conversations. Migration preserves legacy histories that already consumed those slots: it stores a one-time ceiling of `max(500, existing operation count + unallocated active-turn cancellation slots + one recovery slot)`. This ceiling cannot grow through the API; legacy conversations at or above 400 cannot admit ordinary new work. This bounded migration exception preserves existing history and retroactively provides the missing stop/fencing reserve without deleting records. Admission also reserves one cancellation record for every outstanding turn lacking a control record and one session recovery record; it must prove `retained records + outstanding unallocated control slots + new turn/control pair + recovery slot <= 500` under the session lock before acceptance. A cancellation uses that target's reserved slot, coalesces pending/successful controls, and reuses a failed control's identity for at most three explicit attempts instead of appending records. A failed recovery is retried with `POST /sessions/:id/recovery` carrying `{recoveryId, expectedGeneration, expectedAttempt}` and a new key for the deliberate attempt. The transaction requires `state=failed`, the matching current generation and attempt, then increments the attempt and queues the same recovery ID. Exact old-key retries return their original accepted attempt without spending another attempt; concurrent distinct keys conflict. After three attempts it remains blocked with operator recovery guidance, not an indefinitely enabled retry action. A competing distinct intent never creates another active recovery. No-op controls against terminal targets reject without storing an intent. Original uncertain records and recovery acknowledgement history remain intact.

Ordinary browser mutation intents, including metadata changes, are serialized per owner and limited to 10,000 retained records. Guaranteed controls do not draw from a shared reserve that unrelated metadata can spend: each admitted turn reserves up to three bounded cancellation intent aliases; each persisted approval reserves up to three answer aliases; each recovery attempt uses one of its three request aliases and a ready recovery reserves one continuation alias. Control targets are validated existing UUID/epoch identities under the same session transaction. Duplicate/no-op new-key aliases cannot exceed that target's allowance; exact retained-key retries always reconcile first. Approval exhaustion fails closed, retains the pending request until deadline denial, and leaves the turn's separate cancellation/recovery path available. Recovery/cancellation retries reuse their target's operation/recovery identity, preserving their slots. A new operation after recovery may still receive an ordinary-work quota rejection; guaranteed fencing is not a promise of unlimited new work.

Control intent results are at most 8 KiB. Each recovery stores at most a 16 KiB provenance/report summary plus its bounded operation-ID list; native text repairs share the existing 2 MiB conversation-message quota. Recovery audit allowance is at most four rows of fixed action/correlation fields (under 512 bytes each): one per each of three deliberate fencing attempts and one consumed-continuation record, keyed uniquely so response retries/no-ops cannot append audits. Replay events are independently capped as above. These per-target allowances are reserved with the owning turn/approval/recovery, never charged to an unrelated actor-wide control bucket. Globally idempotent emergency stop uses its singleton durable state and does not allocate a new intent/audit alias each time it is already stopped; it remains available at logical history quotas. Physical database outage still denies mutations. These bounds must be exercised at the actual API/DB boundary before the source issue is closed.

If the optional P003 reservation-release hook fails or rejects its exact ownership CAS, reservation remains intact and recovery cannot become ready. Confirmed runner retirement alone does not authorize another workspace owner.

## Source issues

[Common-use live history acceptance](../../issues/2026-09-13-110315-common-use-live-acceptance.md), High and In progress, records the missing bounded native-history restart/resume driver for P007-07. The current live entry point exercises adapter/storage directly without that history scenario; supplying credentials alone does not complete this criterion. The common-use release retains both the implementation obligation and the separate credential prerequisite.

[Replay and control bounds](../../issues/archive/2026-09-07-185228-p001-retention-control-bounds.md), Medium, is Resolved after strict replay age/count and reserved cancellation/recovery storage passed exact boundary tests and independent combined review for P007-02/05/06 and inherited P001-03/07. The archive records discovery and closing evidence.

[Dedicated live credentials](../../issues/2026-09-07-171225-live-runtime-credentials.md) keeps the bounded live part of P007-07 unverified. Pinned non-model contracts and fixtures cannot waive it.

### First implementation review corrections — 7 September 2026

Fencing failures retain a bounded settlement repair until the database confirms `failed`; a failed terminal write cannot strand `fencing` indefinitely or release unresolved work. The supervisor stops admitting new work while its settlement repair is unavailable. Authority is locked before project/workspace/session locks and freshly evaluated after waits. Exact retirement, native inspection, and optional P003 reservation release stay serialized under that ownership transaction, preventing a competing recovery surface from retiring a successor.

Ordinary intent admission now precedes callbacks that may allocate storage. Credential set/replace shares the 10,000 ordinary-owner intent budget. Self-revocation has one reserved intent per browser grant, so ordinary quota cannot prevent sign-out. Removing the currently configured encrypted credential version has one reserved intent alias and one audit; already-absent repeated removal stores neither. Credential set/removal audit writes are therefore bounded by admitted versions and the existing 30-day audit retention (at most two audits per admitted version; older legacy rows remain subject to retention). Existing-key reconciliation still happens before new admission, with fresh current authority before disclosure. P001 external project registration checks authority after its admission locks immediately before filesystem effects; its accepted metadata grant survives expiry during those effects. P003 supplies the separate durable storage-intent lifecycle.

History cursors preserve PostgreSQL timestamp microseconds as exact strings. A previously consumed recovery is retained as history but does not hide the fencing action for newly uncertain operations. These corrections require the recorded round-one regression batch before issue closure.

### Implementation evidence checkpoint — 7 September 2026

The first implementation-review correction batch and the subsequent bounded UI reconciliation fix passed `pnpm check`, `pnpm build`, and the full real Harbor P001/P007 `pnpm test:e2e` run `harbor-e2e-50a81c3d77`. Its start/end source digest is `69e720432acae9b7a834e62adb2f041157ec3dd55794551512f923c647192bca` (812 source/configuration files). The same backend batch passed `pnpm test` (9 tests) and `pnpm test:contract` (13 tests) before the UI-only fix. Environment: macOS arm64, Node 26.7.0, pnpm 12.3.4, PostgreSQL 17.6, pinned Codex 0.153.4, Chromium 1194; the E2E uses real Harbor services and external Codex/OIDC fixtures. Stable desktop/mobile history and recovery screenshots were inspected. Round two independent implementation review is pending; this checkpoint is not review closure, integration evidence for P002/P003, or verification of the unavailable live gate. The source issue remains active.

The second review requested two additional evidence cases: an event committed strictly between snapshot and subscription, and restart while fencing settlement remains pending after both terminal writes fail. Both now pass. The final run also exposed and corrected an unhandled error event from a checked-out PostgreSQL client between awaited queries. The shared pool consumes that event without masking query rejection or replacing the supervisor's fatal lease handler; an exact owned-backend termination regression and whole-database outage verify both boundaries. Final source `90aa34aa3d55516ce3e4a0209cb697d10011a07d8314f07955a4499470e5a507` (812 files) passed check/build, 9 integration tests, 13 contracts and real-stack run `harbor-e2e-2f859a3a33` with identical start/end digests. The final narrow review closure and integrated Node 24/P002/P003 evidence remain separate; the live-account gate remains unverified.

Independent implementation review closed after two rounds on the final source above, including the narrow PostgreSQL client-error correction. No actionable scoped finding remains in that review. P002/P003 integration and the dedicated live-account gate remain pending; this closure does not mark P007 Verified.

### P002/P003 integration refinement

The integrated recovery path locks the current actor, project, workspace and conversation in that order. It captures the workspace's actual `writer_generation`, independently of the conversation generation changed by supervisor restart, and holds those locks through exact retirement and reservation release. A missing reservation can be reconciled only while it remains unowned; another conversation's reservation blocks recovery. A derived-workspace probe uses the registered checkout and Git common-store identities. Normal confirmed completion releases its reservation while retaining acknowledged historical uncertain operations; unacknowledged uncertainty remains blocking. The legacy conversation archive route now uses P007 visibility-only semantics, while project/workspace archive retains P003's idle requirement. P002 token routing, project filtering, quota ceilings and fresh authority checks remain in force; P007 recovery and metadata controls remain browser-only until explicitly scoped by a later design. Combined evidence is pending and does not replace the standalone checkpoint above.

The P002/P003 integration candidate passed `pnpm check`, `pnpm build`, `pnpm test` (9 tests), `pnpm test:contract` (15 pinned-runtime contracts), and the real workspace E2E lane `harbor-workspaces-5e0a57a497` on source `2976f6286a9aea9645e2a27dfcb497e677c5268d5ff034593752dc98211b40bd` (842 files; matching start/end digests). The full P001/P002/P007 real-stack run `harbor-e2e-3890f83dff` passed on the identical production source, before adding only the derived-workspace test, with source `44285c761c564728834de678114711e3ff296b242224c39c3e7924617901fbf9`. Combined cases cover active conversation archival, recovery across the restart-changed session epoch and original workspace writer epoch, new-input continuation, derived checkout preservation and final reservation release with acknowledged historical uncertainty. Environment remains macOS arm64, Node 26.7.0, PostgreSQL 17.6, pinned Codex 0.153.4 and Chromium 1194. Independent integration review, matching Node 24/Linux integration and the dedicated live-account gate remain separate pending evidence.

The narrow independent P002/P003/P007 integration review closed with no actionable finding on `2976f6286a9aea9645e2a27dfcb497e677c5268d5ff034593752dc98211b40bd`. It confirmed the unchanged native launcher/adapter boundary can retain the prior Linux checkpoint without a duplicate run. The [integration report](../../docs/reports/2026-09-07-p007-history-integration.md) distinguishes the matching candidate checks, test-only digest change, inherited isolation evidence and outstanding dedicated live gate.

### Integrated delivery — 7 September 2026

P007 is Implemented at source `2976f6286a9aea9645e2a27dfcb497e677c5268d5ff034593752dc98211b40bd` (842 files), reviewed integration `0118dfe`/`ca1a2cf`. Two feature review rounds and a separate integration review are complete. Node 24 check/build, 9 integration checks, 15 contracts, 8 workspace checks, full P001/P002/P007 E2E `harbor-e2e-10d1a7178a` and workspace E2E `harbor-workspaces-a5ce817a7b` passed; both E2E artifacts have matching start/end source. The [integration report](../../docs/reports/2026-09-07-p007-history-integration.md) records inherited unchanged Linux evidence separately. The [implementation review](../../issues/archive/2026-09-07-203000-p007-implementation-review.md), [shared authority correction](../../issues/archive/2026-09-07-202308-authority-expiry-after-lock-wait.md) and replay/control finding are resolved and archived. Required live-account acceptance remains blocked, so P007 stays active and is not Verified.
