# P024 attachments and composer — execution record

Current outcome: P024 is Implemented and archived as Completed. Commit `817c3d0971665871b64aa29c0fb25e8c8b132f02` was pushed and deployed to the owner’s VPS on 21 September 2026. The [final delivery](#committed-release-and-vps-delivery) records artifact identity and installed checks. The sections below preserve earlier checkpoints, including failures and subsequently resolved prerequisites; they do not override the final outcome.

## Initial record

[P024](../../design/proposals/archive/024-attachments-and-chat-composer.md) is Accepted and in implementation under the owner's 21 September request. No completed verification, commit or deployment is claimed. Main researched the pinned native input forms and existing blob/draft/materialization boundaries, then delegated separate High-effort backend and Medium-effort interface workers with fresh self-contained briefs and disjoint file ownership. P018's lifecycle changes remain independently owned and unverified.

The selected design stores personal attachment copies in private instance state outside project/cache write grants, supports normalized PNG/JPEG and opaque general files, and preserves server-generated paths, authentication, draft CAS and upload/turn idempotency. The interface uses picker/drop/clipboard File batches, concise labels and one full-height chat scroll surface. Sidebar ordering remains last accepted user query.

Evidence is retained under `.test-runs/p024/`; the [combined P018 record](2026-09-21-p018-concurrency.md#combined-candidate-checkpoint) identifies the tested source, shared build/check/test/design results and unresolved failures. Dedicated live authentication remains unavailable until the owner provides isolated credentials or explicitly authorizes the proposed credential-only test copy. Ordinary owner state and production project data are not test substitutes.

## Executed verification checkpoint

The canonical prototype passed desktop and touch-mobile browser observations for the full-height scroll surface, growing composer, reader position, bottom following, bounded attachment rows, focus and upload states. `.test-runs/p024/ui/prototype-receipt.json` records source identity, geometry, assertions and empty browser-error lists; adjacent screenshots and the reproducible script are retained. The actual application design lane also passed on source digest `38f3c4b448b612f292dd2a883f2e2dc7453ec7bae4fd9f3eb8bcce491078980b`, with screenshots under `.test-runs/harbor-e2e-9ae23916ad/`. Main inspected the application screenshots and identified crowded filenames in sent attachment cards for correction. Prototype evidence does not substitute for this application inspection, and simulated touch does not establish physical-device keyboard behavior.

Actual managed Linux attachment acceptance passed in the disposable `p024` Lima VM: Ubuntu 24.04 arm64, kernel `6.8.0-134-generic`, Node 24.11.1, pnpm 12.3.4, Docker 29.1.3, Compose 2.40.3 and an owned XFS project-quota filesystem. `pnpm test:isolation --attachments` exited zero on the same source digest and complete snapshot manifest. This exercised real API/PostgreSQL/supervisor publication, a failed-COMMIT same-inode retry, pinned native initialization, read-only session mounts, simultaneous Local/derived boundaries and identity fencing. OIDC/model dispatch remained fixtures. `.test-runs/p024/linux-host/result.json`, `handoff.md` and `guest-evidence/` retain exact preparation, commands, receipts and limitations. The first attempt lacked the fixed Git helper image; the second passed after that unchanged prerequisite was built and public fixture permissions were corrected. Owned containers, volumes, custom networks, XFS fixtures and control manifests were independently confirmed removed; the VM remains available for affected reruns.

The disposable VM has no host filesystem mounts or SSH-agent forwarding. Guest loopback/wildcard TCP and UDP canaries were reachable inside the guest and unreachable in 48 host probes; owned listeners were closed. This describes the verification environment, not a new product isolation claim.

Pending: corrected critical attachment regressions, personal-profile real-stack attachment acceptance, final visual corrections, dedicated live image/file delivery, independent design/provenance reviews and candidate/installed acceptance. No commit, push or deployment is claimed.

## Third-candidate observations

The sent-attachment layout correction passed the real design lane on source `e5e1b2ecc1cdcac2a651b648d67421327c83dc811eb89c0964c76d8cd994118d`. Main and the independent design reviewer inspected desktop/mobile application screenshots under `.test-runs/harbor-e2e-a2cce55918/`: files occupy distinct rows, long names wrap within the bubble, and the composer/scroll surface still fit. Local build/check and Linux build also passed on this source. The corrected critical attachment scenarios passed, including normalized image content, metadata stripping, stored hashes, retry identity, quota limits, association and archived downloads; the complete critical lane subsequently failed a separate cancellation/recovery assertion and remains unpassed.

The personal attachment lane failed before delivery because its test mutation keys omitted the required timestamp prefix. The binary-upload and retained-turn-retry constructors are corrected; the test still requires a fresh Linux run. Actual live model image/file delivery remains blocked by the separate credential prerequisite.

Fresh advisory design/provenance round 1 found no new attachment authority, quota or interface blocker. The provenance reviewer identified a stale upload retry label and an unqualified claim about historical P005 review rounds in the user guide; main corrected both. These advisory reviews and the shared P018 race finding are recorded in the [combined review checkpoint](2026-09-21-p018-concurrency.md#third-candidate-and-advisory-review). Completion reviews remain pending mandatory gates and fix verification.

## Sixth-candidate personal attachment acceptance

On unchanged source `e0535d00138567f6733006b553b40ca4ebece645e769e90b2ee332dffab749dd`, full snapshot manifest `666e1173701563ad3df075abd55a9707d133ee1a72821a69b8d102ab118d846e`, the isolated Linux command `node --import tsx tests/personal-vps/e2e.ts --attachments` passed with exit 0, 03:55:57–03:57:30 UTC. Receipt: `.test-runs/p018/snapshot-06/linux-attachments-01.json`; remote log SHA256 `eeb2c8fbf5e29267c6da7797ff5ccc61ab81d7763452e0d5f3e51302ca5bacdf`; application artifacts: `/var/lib/harbor-dev-20260914/p018-p024-snapshot06/source/.test-runs/harbor-p015-4707afaf6d`. This real browser/API/PostgreSQL/supervisor lane uses external runtime/account fixtures, not a model account.

The corrected upload/turn intent keys retain their required timestamp prefix. The lane exercises attachment-only and combined inputs, controlled personal paths, immutable publication, retained-turn idempotency, history and authenticated retrieval. It also caught a legacy VPS guard that skipped the supervisor's attachment maintenance. After the guard was removed, the test observed actual periodic cleanup: expired staged content became null, stale draft references cleared with a revision increment, a fresh draft's reference survived, and an expired unreferenced file in that same conversation was collected. No direct helper call substitutes for supervisor execution.

The full critical lane and 51 integration tests now pass on the same frozen source. Linux build also passed; local build/check evidence is from snapshot 05 with unchanged application inputs. The [combined execution record](2026-09-21-p018-concurrency.md#sixth-candidate-verification-and-fixes) identifies those receipts and the separate actual pinned native contract run (28 passed, zero skipped) on snapshot 04. Attachment publisher/media/adapter/native test/dependency files are unchanged across those snapshots. Snapshot 03 desktop/mobile application and prototype evidence remains applicable to unchanged UI code. Snapshot 02 managed XFS publication/read-only mount evidence remains applicable to that unchanged boundary; it does not claim execution of later supervisor admission or cleanup changes.

Advisory review round 2 confirmed the label/history fixes and found no new concrete attachment blocker. Two paired advisory rounds have been used, with normal completion review pending the full selected gate. [P024-04 live image/file delivery](../../issues/2026-09-07-171225-live-runtime-credentials.md#selected-personal-profile-prerequisites--21-september-2026) still lacks authorized isolated account credentials. No live model delivery, commit, push or deployment is claimed.

After the unchanged managed attachment boundary needed no further rerun, the Linux worker rechecked all 1,281 frozen snapshot 02 files and the absence of owned test containers, volumes, custom networks, XFS fixtures and control manifests. It unmounted the owned XFS filesystem and gracefully stopped only the `p024` VM; both commands exited zero. The VM disk and evidence remain preserved for recovery. `.test-runs/p024/linux-host/shutdown-receipt.json` records the exact commands and stopped state. This cleanup does not broaden the earlier test scope.

## Passing live delivery

The owner authorized standing VPS login reuse and selected VPS-only release support, superseding the earlier unavailable-credential checkpoints. On source `19b7886b0f218cf6b29223023e034528a22365d5b6c9a5ff591e2d9fa810b862`, `node --import tsx tests/personal-vps/attachments-live.ts` passed on actual Linux Codex 0.153.4 with `gpt-6-astra` at low reasoning, one model turn, 04:43:04–04:43:38 UTC, exit 0. The model identified the synthetic image's red color and returned the exact generated marker from the opaque file delivered through its controlled path. This is actual model receipt, alongside the separate real Harbor application and native read/write-denial gates.

The nonroot delegated test unit used a fresh private home containing only a credential copy, synthetic workspace and attachment bytes. The fixed wrapper confirmed the installed credential stayed unchanged, the owned unit/cgroup retired, and the complete temporary state—including copied login and materialized files—was removed. Receipt: `.test-runs/p018/live-final/attachments-receipt.json`; raw private log SHA256 `0685720507b4ce16e451071d2c984c61d48b33fbd5253ebbe121b32ef5f157e7`. P024-04 is satisfied for the selected personal VPS release. Native macOS lifecycle support is Not planned for this release; older managed live obligations remain separate. The scoped gate is green and final normal review remains pending.

## Final normal review

The final paired design/provenance review found no implementation or evidence blocker after the selected VPS live gate passed. Main corrected the shared P3 wording item separating current personal VPS results from older managed obligations; both reviewers verified the six-document fix within round 3 and reported no remaining findings. The [combined final review](2026-09-21-p018-concurrency.md#final-normal-review) records source/diff identities, all three rounds and limits. Application source remains `19b7886b0f218cf6b29223023e034528a22365d5b6c9a5ff591e2d9fa810b862`. Commit/package/promotion are authorized and next; no installed-delivery claim is made here.

## Committed release and VPS delivery

P024 was committed and pushed in `817c3d0971665871b64aa29c0fb25e8c8b132f02`, then deployed with P018 on 21 September 2026 as `/opt/harbor-personal/releases/p018-p024-817c3d097166`. Artifact manifest SHA256 is `f009fb788dc60ee2bc56f7be2b1fa42ce0bfb2d8df9d72ec085e17a7cfc3a8bc`; the application source digest remains unchanged from the passing final gates. The [combined delivery record](2026-09-21-p018-concurrency.md#committed-release-and-vps-delivery) records exact committed/package/inventory/archive identities, commands, migration preflight correction, matched private checkpoint, installed health/auth/native discovery and recovery limits. The packaged image decoder passed its own PNG encode/decode probe. Migrations 019 and 020 were applied without changing predecessor migration hashes.

| Acceptance | Passing evidence and boundary |
| --- | --- |
| P024-01 | Real application design/critical and personal attachment lanes cover picker, paste/drop, ordinary text, combined/attachment-only send, drafts, retries, reload and history. |
| P024-02 | The 51-test integration gate plus critical/personal application checks cover media/size/ownership/auth/idempotency boundaries and actual supervisor cleanup. |
| P024-03 | Actual pinned Linux native contracts and managed XFS helper/read-only mount acceptance cover immutable publication, identity fencing, reading and write/unlink denial. |
| P024-04 | One actual isolated model turn identifies the red image and exact opaque file marker using the owner-authorized credential copy. |
| P024-05 | Prototype and actual desktop/mobile application observations cover scroll/composer/state interactions; installed read-only UI inspection additionally confirms the enabled multiple-file picker, concise labels and composer inside the full-height chat scroll surface. |
| P024-06 | Source-linked build/check/test/critical/native/Linux evidence, three paired reviews with verified fixes and no remaining findings, committed package linkage and installed release checks. |

P024 is Implemented and archived as Completed. Private attachment copies live under instance state outside projects; PostgreSQL is authoritative. Uploaded PNG/JPEG images are normalized, while other files remain opaque bytes exposed through controlled paths. Original filenames/content remain untrusted data. No installed test prompt or attachment was sent to an owner project. Isolated test resources and copied login credentials were cleaned; evidence and the stopped managed-test VM disk remain retained. Native macOS lifecycle completion is outside this VPS-only release; older managed live/restore obligations and same-host backup limits remain unchanged.
