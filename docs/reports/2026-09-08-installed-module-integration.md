# Installed file, terminal and deployment integration

Delivery is in progress. This report records the cumulative P004/P006/P009 implementation and preserves intermediate evidence. The scoped integration passed independent source/evidence review, final host and managed Linux checks, and installed browser acceptance. The owning proposals remain active because their separately recorded mandatory gates are not waived. Protected off-host backup/restore and dedicated-account live gates remain separate and blocked. No repository initialization or protected transfer was performed.

The integration extends the fixed deployment registry with file operations, inspections, audits/events and terminal input/output/history. The installed API remains unprivileged; bounded file reads use the existing storage service's private `/files` endpoint. File mutation runs in the trusted supervisor with the immutable manifest's pinned helper image. Terminal purpose and its reviewed seccomp derivative remain pinned. Maintenance freezes queued file/terminal dispatch and new control/input, retains those queues without waiting forever for prohibited work, and permits bounded inspection/release/termination. Exact helper slots and terminal/background identities are retired before inventory. An explicit administrator retry of uncertain terminal retirement preserves the original record and respects its three-attempt limit.

Fresh restore metadata keeps original outcomes in the historical ledger, invalidates source file inspections, changes reservation/generation fences, revokes terminal controller/input authority and leaves unresolved effects requiring explicit destination reconciliation. Source launcher ledgers remain historical and never become destination authority. Terminal native directories are included in whole quota-unit publication, while native authentication and live lock files remain excluded. These semantics are canonical in [D006](../../design/decisions/006-portable-release-and-restore.md#p004p006-installed-module-integration--8-september-2026).

## Intermediate verification

The inherited reviewed baseline is commit `6f21e45`, source `f0a4cdf2aea0738bc790f9bff6eb539ffc0b4ccbdfd7dd83410464d9c8cced99`, 951 files. Parent ran cumulative critical and terminal acceptance on Node 24.11.1 at that baseline; those results do not cover this integration's later source.

Real application module acceptance `harbor-e2e-b8227e611f` passed on Node 24.11.1, exact start/end source `76e049a9eac8bc08598cf5032376857661a5acf1e5a2c0e634aed4337c807b67`, 957 files. It exercised actual UI/API/PostgreSQL/supervisor with external OIDC/Codex fixtures: pending administrator freeze versus final terminal wire, resource NOWAIT availability, rejected control and zero forbidden input marker, queued drain behavior and explicit local module rebind/release. The proposed three-party PostgreSQL deadlock was not observed in the parent's separate diagnostic; the early deployment SHARE ordering is a consistency correction, not a claimed deadlock fix.

Actual Ubuntu 24.04 arm64/Linux 6.8, Docker 29.1.3, XFS and Node 24.11.1 installation exposed two defects before final acceptance:

- Artifact `75ea68ec29f66fb5cb78adbd5650426f05127fa0937a609f10437f67939ea29f`, source `3a81e2a96748e4d1675522cf9779b5a6803fdc28370535845f8106d3a2786a7a` /954, omitted `apps/web/dist/index.html`. Authentication and backend readiness worked, but the browser received `INTERNAL_ERROR`. Packaging now copies that exact ignored Vite output and artifact verification requires the served document. Canonical root issue: `2026-09-08-031032-p009-installed-web-assets.md`.
- Corrected web artifact `2056977e4e0acf9a1a1e17f303d93beaac9c02630f5db2f2139856373c24bdbc` rendered the application and created a project, then file inspection failed. The workspace loader truncated the now-main-guarded quota module and raised `IndentationError`. A guarded module import replaces text truncation. Actual corrected source file inspection passed on the same owned XFS identity. Canonical root issue: `2026-09-08-031857-workspace-quota-import.md`.

Artifact `1b6e116c2e8ed1df3fc2cdc073f2f1b4396441f185ea943896927c0810116bcd` includes both corrections. Its initial installed UI test encountered an unsaved-draft dialog when closing the file panel. The test now waits for confirmed saved UI state. A later fresh candidate also lost its save response, so premature navigation alone does not explain this historical observation. Explicit reuse of that owned fixture passed installed Monaco save/CSP, real native xterm/PTY, no account/network, wrong-instance helper preservation, background retirement and queued drain. That diagnostic reuse remains distinct from final fresh-fixture acceptance.

The same installed artifact passed a fresh local synthetic database clone through the actual administrator restore-rebind bridge, and exact terminal recovery after an injected stale private lock, paused real container and owner SIGKILL. Original uncertainty remained historical, old controller authority was revoked, the exact reservation was released, wrong inode/absent acknowledgement/stale generation were rejected, and the owned container was absent. This is local metadata rebinding and recovery evidence, not protected A-to-B restore or power-loss evidence.

Intermediate current-source checks/build, 11 integration tests, 18 pinned-runtime contracts and 21 Python deployment contracts passed. Final source identities, full changed acceptance and independent review will be appended after the candidate settles.

## Managed identity correction and final review checkpoint

The first integration review at commit `a2f9389b5ec477076b159175108d121ea90ba3b3`, source `e396763b9bf29632dda51a47f651c086ec24599c667e7399e90d09af212b19ff` /961, found no source defect in registry, maintenance, restore-authority or exact helper retirement. Node 24.11.1 module application acceptance `520898b18d`, critical regression `3912f05d66`, workspaces `c7f57d8252` and full terminals `651636fc70` passed with matching start/end source. These are pre-identity-correction results.

The fresh installed artifact `bbb4c15f435b4a0e9ac78eb5102feaf1fa8dde896d567dc8fb7239db567cee83` contained the served document and quota import corrections. Its first file save persisted one succeeded operation and the expected bytes, but the browser reported a lost connection and retained the uncertain draft. No at-failure transport category was captured. One explicitly reused owned-fixture diagnostic, with bounded method/path/status/error-category capture, passed UI/save/PTY/drain and fresh local metadata rebind; the original transport cause remains unexplained. This is not a successful fresh-fixture result and no production retry policy changed. A terminal-recovery test expectation was subsequently corrected to distinguish the preserved `terminal-interruption` uncertainty from the later `terminal-recovery` retiring checkpoint.

Actual managed P004 run `harbor-files-f81037d538` then exposed a separate completed derived-workspace handoff problem. The post-create verifier discarded the workspace ID/root/kind/common tuple needed by the private managed validation IPC. Backend's independent P008 Linux run additionally captured completed storage followed by unavailable state. The caller now retains the immutable command identity and exact returned checkout/common device and inode; no identity comparison was weakened. Canonical root issue: `2026-09-08-035221-workspace-validation-identity.md`. Corrected complete P004 managed acceptance `harbor-files-a2ecb219a6` passed on Node 24.11.1 at matching source `7be9e7031fb1fba6451fd4d46b245c7eaef4b83fe99f32eb512aab28546bacf1` /961, including first Local-to-Git-worktree creation and actual file/Git UI use.

A bounded caller audit found that `workspaceValidate` supplied checkout `source` separately from `identity.common`; the Python validator selected the former and omitted the latter's common-store comparison. Downstream fixed helper/runner checks still guarded actual use; no out-of-scope execution was demonstrated. The trusted validator now checks every supplied common tuple under the existing storage lock, including canonical path, device/inode, directory, UID and symlink rejection. An initial extra requirement inferred source common metadata from destination kind and was rejected by independent review and the first-create Linux assertion; that failed `01a4c8f0…` checkpoint is retained. Removing that requirement preserves first Local import while checking supplied identities. The reviewer accepted this narrow source correction; final Linux and fresh installed artifact evidence remain pending.

## Frozen correction evidence

Commit `77b2ad50e3b4191a535b3fac02badc2cfd2316f6` records the corrected production source `f13afc57f3956a7b457e567da02f1bb00ad214405c52c7a3d28f2d5c94ad1d27` /961. Actual Linux `pnpm test:isolation --workspaces` passed at matching start/end source in `xfs-25f78396-daf3-43bb-86dd-06db9585a177-workspaces-linux.json`. It includes first Local-to-worktree creation, supplied common-inode and symlink rejection followed by restored exact-identity acceptance, disjoint checkout mounts, read-only common mounts, pinned native initialization/resume, XFS byte/inode limits and quota-escape ioctl denial, partial removal/process death and syncfs failure reconciliation. The native runner/Git/gateway images remain the reviewed immutable IDs; no isolation exception was added.

The full managed P004 UI/API/PostgreSQL/supervisor/file-helper lane also passed as `harbor-files-44ad9cde66` on Node24.11.1, matching start/end test source `5ab432bd979d419a2a6112a4efd568422b198713fd22ddf1afe6cd4ca8ca763e` /961. Only the installed-module test driver differed from `f13`; all production files were identical. The independent reviewer accepted the narrow caller/common-validation corrections separately from the still-pending installed browser outcome.

Fresh immutable installed artifact `8c11ca945b654d27fe1f787db0ef5b56ce3d269a88fe3883167d3820c8848f22` contains exact `f13` production. The co-located Linux browser's first save response failed with `net::ERR_NETWORK_CHANGED`, while one operation was succeeded, the bytes matched and `/me` returned200. A later owned diagnostic reached copy readiness and terminal running state, but its lazy xterm CSS/JavaScript assets failed with the same browser category. These establish the observed transport failure category, not its operating-system cause. The original first-save JSON was overwritten by the next diagnostic's then-shared output path; its detailed tool output survives and the retained summary is explicitly transcript-derived. New attempts use unique evidence directories. No automatic production mutation retry or browser security exception was introduced.

Installed `drain --interrupt` then succeeded. Immediate `resume` exited1 with `Private listener confinement failed`; later read-only status showed all four roles active, readiness ready and maintenance false. That later status does not change the failed command result. The separate P009 readiness follow-up is owned by the original deployment implementer.

An external macOS Playwright client is being connected to the same Linux installation to represent an ordinary remote browser. All Harbor services, filesystem/DB assertions, file helper, PTY and quota boundaries remain on Linux. The test explicitly drops a real accepted save response only after that exact operation succeeds, then invokes the visible same-request retry and checks unchanged operation identity/hash/count and file bytes. Client topology and deliberate response-loss evidence will remain distinct from the failed co-located fresh-save checkpoint.

The parent independently ran `pnpm check`, `pnpm build`, critical acceptance `5ddd82e4c7`, workspaces `fc670a1dba`, and full terminals `6b1cb43f5f` on Node 24.11.1 at exact start/end `f13afc57f3956a7b457e567da02f1bb00ad214405c52c7a3d28f2d5c94ad1d27` /961. All exited successfully. These final results supersede the earlier pending host/Linux language for those checks; installed-client and protected-transfer qualifications remain.

External-client attempt `3de214eb-ef1f-49f9-83b9-9bcd55ab8b56` authenticated with `/me` 200 and no observed `ERR_NETWORK_CHANGED`, but its save was denied before claim (`FILE_DENIED`, epoch null). Read-only diagnosis established that the earlier explicit `drain --interrupt` had retained emergency stop; the actor was valid and the exact managed workspace validated successfully. Only this disposable fixture's emergency flag was explicitly reset after confirming no active nonqueued terminal. Attempt `4922010d-1316-4c0b-a0f4-4d21d2a240ca` then exposed a test synchronization error: file bytes and the UI's request-timeout retry control became visible before the route handler completed its intentional succeeded-to-abort sequence. The test now waits for that exact sequence before asserting the unchanged operation identity/hash/count. Both failed attempts are retained and neither is relabeled a production correction.

## Final installed outcome and review closure

Two separate complete installed lanes exited 0 on the unchanged immutable artifact `8c11ca945b654d27fe1f787db0ef5b56ce3d269a88fe3883167d3820c8848f22`, archive SHA-256 `908d91e15493ce0cc1d6f0d585bd06e9d312d28344ca31bb30f585f7a668f355`, containing exact `77b2ad5` / `f13` production. Both used Node 24.11.1 and a separate macOS Playwright 1.56.1/Chromium 1194 client; all Harbor services, storage, PostgreSQL, fixed helpers, PTY and assertions remained on Ubuntu 24.04 arm64/Linux 6.8.0-134, Docker 29.1.3 and the supported XFS profile.

- `modules-cff10b97-b885-4321-bf15-84d7ac75996d` deliberately dropped the actual save response after the exact returned operation succeeded. The owner clicked the visible same-request retry; operation IDs, hashes, count, outcome and file bytes remained unchanged.
- `modules-61563bc3-aca4-450e-8d54-a9bfb5bc3567` exercised the ordinary save path with no response interception or intentional response loss. Its result explicitly records `reconciledDroppedResponse: false`.

Each lane passed actual Monaco save/CSP, copied-workspace readiness and pinned native xterm/PTY, no model account/network, immutable mount/image checks, preservation of the wrong-instance helper canary, queued-work maintenance freeze and whole background retirement. The fixed physical inventory includes file receipts and terminal native state while excluding live locks/native authentication. Fresh local synthetic database rebinding revoked prior file inspection/controller/input authority without replay or source contact. Exact administrator terminal recovery exercised a stale private lock, paused owned container and owner SIGKILL, preserving original uncertainty and rejecting absent acknowledgement, wrong inode and stale generation. Neither lane performs protected filesystem restore, off-host transfer, repository initialization, promotion or a live model call.

These are explicitly **reused owned-fixture** runs, not a claim that the earlier failed fresh-fixture attempts passed. The final test driver source is `a0a579868be8c0136253e512856bd06497aa90692bfbc2e01913ba9196eb10a8` /961; only `tests/deployment/modules.ts` and `modules.py` differ from `f13`. Node 24 `pnpm check` passed on that driver. Unique result JSON and secret-free editor/terminal screenshots are retained locally under `.test-runs/module-linux/installed-fault-cff10b97/` and `installed-ordinary-61563bc3/`. Each folder includes separate inventory, rebind and terminal-recovery results. The installed manifest's source remains `f13`; the driver identity must not be substituted for the artifact identity.

Additional failed fixture checkpoints remain retained: `00a231c5` passed the explicit save reconciliation and native UI assertions before an obsolete fresh-only terminal-count assertion rejected retained history; the corrected assertion checks the exact prior ID set plus the two new terminals. `2907a5eb` encountered 502 because its explicit reset had not restarted the deliberately drained roles. `8548d317` encountered a supported `FILE_UNAVAILABLE` refusal and failed readiness after repeated retained native homes exhausted the owned project's 512-inode hard quota. The first ordinary run was mistakenly launched before that readiness subprocess's failure had been observed; it does not establish a ready precondition. Four exact retired, container-absent account-free test homes (389 nodes) were preserved in the same VM's run-owned control evidence directory. The quota/profile was unchanged, and final readiness exited 0 before the successful ordinary lane started. No unknown, active or other owner's data was removed.

Independent module review completed two bounded rounds: initial cumulative source review followed by the corrected managed identity/common-store validation and final installed evidence review. The reviewer found no remaining actionable scoped issue. Earlier source-only review closure was superseded when actual managed execution exposed the handoff defect; it was not used to waive that correction. Parent final host results and the actual full managed file/workspace lanes above complete the changed-boundary regression map. The separate resume-readiness correction was subsequently reviewed on another installed component and integrated by the parent; that later source does not change the tested `8c11` artifact or turn its historical failed resume command into a pass.

After final evidence collection, only this installation's API, supervisor, storage, dependencies and external identity fixture were stopped. Its recorded data, immutable releases and historical evidence remain; the SFTP fixtures, P004/P010 resources and all other owner namespaces were preserved. Dedicated-account live and protected A-to-B backup/restore/promotion gates remain blocked and must stay visible in the owning proposal/issue indexes.

The parent inspected both final result sets and ordinary editor/terminal screenshots, then closed only the owned external Playwright server and exact OIDC SSH forward. The existing Lima application forwarding and other browsers were not modified. Final VM read-only inventory showed no running containers after the earlier owned candidate dependency pair was also stopped.
