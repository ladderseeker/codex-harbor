# P011 development checkpoint

Delivery is In progress. This is an incomplete implementation checkpoint, with no independent implementation review yet. The canonical outcome remains [P011](../../design/proposals/011-private-project-previews.md) and [D007](../../design/decisions/007-confined-project-preview-origins.md).

The initial real application lane `pnpm test:e2e --previews` passed on Node 24.11.1 in `harbor-previews-1fb5feff56`. Its retained result records matching start/end source digest `4b3239f226171de7999c0da6e03ad6abe2224ec6a1e7f4cfe2f7a5f066374dd2` across 981 files. It exercises the actual UI, API, PostgreSQL, supervisor, relay and separate preview origin, including SSE, explicit opening, cookie boundaries and stopping with reader-lease release. Only external OIDC and Codex protocol boundaries are fixtures. This is not real native Linux isolation evidence. Later UI editing/fencing and OpenAPI changes are outside that tested source.

Two focused real relay/policy tests passed with fresh loopback HTTP, SSE and WebSocket endpoints. A sandbox-denied listen attempt preceded the authorized local run; it establishes no application failure. TypeScript and build checks have passed at intermediate checkpoints, but complete final checks and regression remain pending.

Initial application failures remain retained separately: `90198009c6` did not reach the opening control; `cd41c611e0` reached native readiness but failed the cross-origin ticket exchange; `e402ed1c73` reached the application and SSE but failed a cookie-inspection assertion. A separate actual npm invocation rejected using `/dev/null` for both npm configuration files; the fixed command now uses distinct trusted paths. A public-only Chromium form probe demonstrated that a `no-referrer` cross-origin form sends `Origin: null`; the narrowly recorded D007 bootstrap policy uses `origin`, while the untrusted landing and application retain `no-referrer` and exchange still requires the exact non-null Harbor origin. Pinned Playwright cookie filtering was unsuitable for asserting host-only transmission; the passing test checks actual request cookie names without retaining values. These later observations do not invent missing diagnostics for the earlier failures.

Remaining mandatory work includes complete lifecycle/fault/bounds and current-authority acceptance, read-only versus writer coordination, deployment registry and restored-authority integration, immutable installed relay configuration, actual pinned native and supported Linux isolation, complete UI acceptance, the critical regression suite, and independent review. The checkpoint does not authorize a public preview, arbitrary destination or runtime command, and does not claim P011 Implemented or Verified. Protected transfer and unrelated live-account gates remain governed by their existing records.

## Native and fault checkpoint, 2026-09-08

Delivery remains In progress; these intermediate results do not close the remaining acceptance or independent review gates above.

- The first actual Linux native preview lane `70c725192b` passed at exact start/end `56d85cc801490529181a5a62e30a3cd7ac03140ce0d6c00d5c86655e04c0d8c8` (1,021 files). It used Node 24.11.1, Codex 0.153.4 and the supported Ubuntu 24.04 ARM64/XFS/Docker 29.1.3 VM, with actual native startup and fixed relay image `sha256:fa8573a227cbc192fb10b06f0a5be33de2bedd7cef21646acf6ce0ef8bdf515f`. Only OIDC is external for preview execution. Result and screenshots are retained locally under `.test-runs/linux-initial/`.
- Expanded actual Linux run `bb7ffa68df` and macOS real application run `b941f5b032` both passed at exact start/end `478fbe79ab7e6720e56fa90879e3aaf24e32da6dd27a342bd1bc32cf18ef3de0` (1,023 files). Linux covers Local, copied and Git worktree previews, read-only/write checkout and common mounts, relay namespace identity and absence of project mounts, host-service/metadata/private IPv4/IPv6 denial, namespace/mount denial, project-quota inheritance/byte/inode exhaustion and confirmed stopping. Effective fixture limits are 128 MiB and 1,024 inodes; normal fixture defaults and production limits are unchanged. Both runs include actual WebSocket application traffic and idle stream revocation. Linux JSON and fixture screenshots are retained under `.test-runs/linux-expanded/harbor-previews-bb7ffa68df/`; cleanup was confirmed and no containers remained.
- The preceding Linux `3c6a33fad4` failed with ENOSPC while compiling a fixed namespace probe into a derived checkout under the original 64 MiB/512-inode fixture. Its diagnostics did not capture per-limit usage before cleanup, so the exhausted limit is not established. Its failure is preserved separately. Future failures record bounded trusted quota usage and filesystem free space before owned cleanup. macOS `c37330bcc9` failed before Harbor startup when Compose's public PostgreSQL manifest request returned EOF; the test now requires the already provisioned pinned images with `pull_policy: never`. Neither failure is relabeled as a production fix.
- Real application fault run `1836b522b4` passed at exact start/end `d2eb229899866fe3599bcf08babdaddb90575f9b17423ff1f17a7deb72ccb116` (1,024 files), Node 24.11.1. Actual deferred PostgreSQL COMMIT rejection after physical retirement retained uncertainty and reader ownership until an acknowledged reserved retry, even with ordinary command capacity exhausted. Rejection of ready-state COMMIT did not replay the native generation. Actual supervisor SIGKILL/replacement retired the old identity with a conservative output gap, and persisted lifetime expiry retired execution. This run uses the external Codex fixture on macOS; the same new fault cases have not yet been established on Linux.

The optional installed preview profile, immutable relay image registry, physical inventory, maintenance/drain, restored preview authority and administrator recovery code now exists in the working checkpoint, but installed acceptance is still pending. No protected backup or restore transfer has occurred. Later installed test-driver additions are outside the exact fault-run digest. Final check/build/contracts/critical regression and independent implementation review remain required.

## Interrupted-work recovery, 13 September 2026

Delivery remains In progress. The committed checkpoint `ed59d0997c751040ffacc57f8f8b14e9f5a2383a` survived, but the former temporary macOS worktree and its regular files did not. A clean persistent isolated worktree was restored from that commit. Recovery read only allowlisted public source and specifically named sanitized evidence from the existing D host; it did not restart the host, alter an installation or test fixture, copy native/account/configuration state, or perform a protected backup transfer.

The recovered immutable artifact is `ad5786c849eefbffe1f4046e2682a9efc825f7c00520fae47a4f949db956ba4e`, whose release manifest records source `93d3ef15e720b03285498a20e4ab5843e1e1a0362412654483de9f6649ca5d29` (1,028 files). Its three later production files—preview relay retirement, sanitized retirement diagnostics, and preview-purpose adapter guards—match the manifest and surviving guest source byte for byte. In particular, `infra/previews/launcher.ts` has SHA256 `8490a82b6b30efb71cdf71c74835231b6475fcad51c4c6054dd630f363e56e2e`. The recovered actual Docker regression has SHA256 `832411165063c6b801983539f503d218ccd2884007aadd4a7d8582fd99910cdd`.

The retained retirement evidence distinguishes an observed defect from an earlier unexplained failure: `preview-retire-fM28Kz/failure.json` records actual container disappearance after listing but before inspection, with erroneous retirement failure at source `89d176107524629eed7e8eea29ad304ee14cdcffe49693c9dc3da89d108e1f14` (1,027 files). `preview-retire-pivr7O/result.json` records both inspection and removal disappearance schedules passing at `93d3ef15…ca5d29`, while a wrong-instance container remains untouched. A successful fresh exact-name absence check now reconciles disappearance; daemon errors or changed ownership remain unconfirmed. One bounded independent source review closed this correction. This does not establish the cause of the earlier `c88de2288c` lifetime-retirement failure, whose original diagnostics did not identify the failing retirement stage.

Recovered installed evidence under `xfs-fa2cfb07-3852-4ead-8c0d-4894b9d17a23` includes:

- `previews-b543b25d-57a4-4853-88ed-4a7b8749570d`: actual installed browser/API/supervisor/storage/native preview, separate HTTPS origin, desktop/mobile/application screenshots, physical inventory, and explicit maintenance drain with exact runner/relay retirement. The test driver records source `66c12e1e0167acf15c82d014b5b50a837b1d01c58bdc26079f6c8eb9b78ca134` (1,030 files), distinct from the immutable release source above. It used an explicitly reused disposable fixture with an explicit fixture-only emergency reset; it is not fresh-host acceptance.
- `preview-recovery-2/result.json`: actual installed administrator recovery after an injected stale lock, paused owned runner, and owner SIGKILL; missing acknowledgement and wrong inode are rejected, exact runner/relay retirement is confirmed, stale generation is denied, and original uncertainty remains historical. This does not claim a separately observed surviving background child.
- `preview-rebind.json`: the actual installed database bridge passed on a fresh local metadata clone, preserving historical logs/native references while stopping previews with new generations, revoking viewer authority, and keeping exact restore retry stable. It explicitly reports no filesystem restore, source contact or protected transfer.

Earlier installed failures are retained separately: `previews-fd64fe07-153f-4a7d-bc73-58e174267c1e` preceded a test assertion correction for the deliberately stopped API after drain; `previews-92ac55ba-95d7-4986-a582-f503a699ca83` has an unexplained blank page after login. The subsequent pass does not retrospectively establish that blank-page cause.

The local recovery archive has SHA256 `a3dd9fc2bc59bc5c964ade1d0f5bfa875368784c66ee5a6aa15453625fd72071` (10,639,360 bytes, 1,957 public-source/evidence entries); its allowlist, file hashes, and original recovered copies are retained under the persistent worktree's ignored `.test-runs/recovery-20260913/`. Later guest test-driver changes are distinguished from installed artifact bytes. Host-only controls/expiry tests and the last unrun coexistence/migration/stream-bound additions were not recovered and will require reconstruction and new validation. Historical results do not substitute for final-source checks, remaining complete acceptance, current documentation, or full independent P011 implementation review.

The recovered checkpoint has source digest `08e5584bdb0d21203f413e405d534910d9d583e7ad4192a2fc2d9d90bb06d2de` (1,030 files). On 13 September, Node 24.11.1 `pnpm check` passed TypeScript, 89 documentation files, OpenAPI and maintained formatting; the two focused preview adapter contracts passed. These are recovery checks, not a repeat of the full application or Linux suites. Read-only D inventory found the old API/supervisor/storage stopped, only that installation's PostgreSQL/Caddy and fixture OIDC still running, no runner/relay containers, and sufficient free space. Those existing resources remain preserved pending explicit ownership coordination for new test namespaces.

Current interface and setup documentation is in the [preview user guide](../user/previews.md) and [developer guide](../developer/previews.md). These describe the branch implementation while final acceptance and full review remain open.

## Reconstructed acceptance checkpoint, 13 September 2026

Production remains byte-identical to recovered commit `4218d71`; the following additions are tests and actual documentation. Fresh macOS run `4657fa90b0` and fresh supported Linux run `6e7aa90abc` both passed at exact start/end source `134b8bd31f925171fc7209d7df3ab91d3c879ee6d40cb0ac33b656dca7c1fbc0` (1,035 files), Node 24.11.1, with confirmed owned cleanup. Linux used a new source namespace and fresh XFS project IDs, preserving the old installed fixture. It used the actual pinned native runtime and fixed runner/relay/file-helper boundaries; only OIDC was external. The macOS lane used external OIDC/Codex protocol fixtures.

The named result files establish these distinct outcomes:

- `access-cases.json`: eight concurrent connections followed by denial, complete 8 MiB-plus request denial, oversized WebSocket closure, an independent idle lease deadline during an actual database row wait, and idle SSE/WebSocket closure plus fresh denial after source-browser revocation and owner-identity replacement. These last cases restore only their disposable fixture's injected authority rows between independent assertions; they do not claim public session revival behavior. A paused stream reader left another viewer responsive, then closed within the stream-rate bound (Linux received 2,080,787 bytes). This proves the combined bounded-stream outcome, not an isolated attribution to a particular socket-buffer limit.
- `coexist-cases.json` on Linux: two native previews ran concurrently on separate origins/runners, with cross-use of their grants denied in both directions. An actual P004 file save succeeded while a read-only preview retained its exact runner/reader. A write preview kept another save queued and bytes unchanged; confirmed retirement released the save. A detached child was present after its spawning process exited and before whole-runner retirement; the exact container was absent afterward.
- `fault-cases.json` and `output-cases.json`: actual PostgreSQL deferred COMMIT failures, explicit reserved retirement recovery, supervisor SIGKILL and lifetime enforcement without generation replay, plus HTTP-triggered application stdout flooding that retired execution with visible loss and bounded stored chunks/bytes.
- `derived-cases.json` and `boundary-*.json` on Linux: Local/copy/Git worktree read-only/write mounts, exact runner/relay isolation, no account file or model egress, namespace/mount and host/private-network denial, and actual project quota inheritance and exhaustion under the explicit 128 MiB/1,024-inode fixture. Production limits were unchanged.

The final Linux result's inherited scope label still says broader hostile/installed acceptance is pending. Its named boundary files above establish the listed hostile-isolation checks; immutable installed evidence remains the separately identified `ad5786…` checkpoint, not this fresh source execution. No protected transfer or fresh-host filesystem restore is claimed.

The initial reconstructed Linux run `d4dd17ac0f` at `50824c747c1f76ac0238713be2199c6b8707504e4fedc269ca1a521bed10ea7f` failed with a request deadline in the new body-bound test. A subsequent local diagnostic pinned the timeout to advertising 8,388,609 bytes while sending an empty body. The test now sends the full declared bounded payload and retains the 403 assertion; no production limit or timeout was changed. Local intermediate run `b3f89a69a3` passed at `ca51c315346f2efd14b3bddeae048ff58b598a332f3a00a71d12242b9f21a98b` before the final explicit owner/source-browser and paused-reader assertions. Those failures and intermediate results remain separate.

Node 24 `pnpm check` passed with 91 documentation files and the exact lower-ID PostgreSQL test passed fresh schema plus migration 014 after the retained 016 predecessor, preserving existing migration digests and project data. Local build passed; the final Linux source installed its frozen lock offline and built successfully before its application run. Full pinned adapter contracts, the cumulative critical regression and complete independent implementation review are separate remaining gates at this checkpoint. No further broad Linux rerun is needed absent a changed boundary or actionable finding. Final Linux artifacts and hash inventory are retained under `.test-runs/linux-20260913/`; local results retain their original run directories.

## Full implementation review and corrections — 13 September 2026

The first full independent source review inspected production `4218d71` and clean test/document checkpoint `a8a2f2c`. Its two High findings and required follow-up are recorded in the [implementation issue](../../issues/archive/2026-09-13-024503-p011-implementation-review.md): new-key pending stop requests spent reservations intended for later physical attempts, and source-actor loss during readiness incorrectly stopped already-executed work. No other actionable finding was reported in that bounded full read. The distinct relay-disappearance correction above remains separately evidenced.

Stop reconciliation now reuses the retained physical attempt's hash/result before counting or inserting an alias. Exact-key responses remain unchanged. Readiness settles the already-authorized native execution under current owner/instance/emergency/deployment and exact generation/state/workspace/reservation fences; new execution and viewer requests still require fresh source authority. A second bounded independent source recheck found no remaining actionable issue in this correction scope.

Focused Node24.11.1 application run `c494743823` passed at identical start/end source `991fb86c4a2cefe8908c57df9e69435ae15ae07144c1be184fc1d1fc0b5517c4` (1,036 files), with exact owned cleanup. `readiness-cases.json` establishes an actual fixture-script write before source PAT revocation, successful readiness afterward, denied fresh revoked access, API SIGKILL/restart and stream reconnect with unchanged generation/runner/relay/lease, one execution marker, and current-owner stop. `fault-cases.json` establishes four new-key pending requests retaining one intent, two actual deferred retirement COMMIT failures, and successful acknowledged third retirement while 10,000 ordinary intents are occupied. The run uses external OIDC/Codex fixtures and does not claim supported Linux behavior. Its selector is `HARBOR_PREVIEW_REVIEW_ONLY=1 node --import tsx tests/previews/e2e.ts` after type checking.

Initial focused run `9848c0eae9` failed at source `efdc8525ef467668de03864c6d3355eafd996b1f8e86ec06eab6fe037eee4962` (1,036 files). Its new test waited for a persisted stdout marker before releasing readiness, but the serialized launch tick flushes logs only after readiness returns. The corrected test observes the actual script's file write in its approved workspace-write fixture, then separately requires one persisted marker afterward. This was a test observation correction; no further production change followed that failure.

The full D pinned contract attempt remained 19/20. Main then independently reproduced the existing terminal contract failure on both baseline `717ca45` and current P011 with fresh homes under `/tmp`: pinned Codex0.153.4 refused its Linux sandbox helper aliases there. Both adapters passed using fresh run-owned persistent homes. Test-only commit `e0e79f9` now creates mode0700 fresh `mkdtemp` homes under the ignored repository test namespace and resolves their canonical path; sandbox, readiness, resize, byte-output, sixteen-second delayed exit and exact owned cleanup assertions are unchanged. Independent review and main's exact D repeat passed both terminal checks, with generated homes absent afterward. The original 19/20 result is retained and has not been rewritten.

The reviewer inspected the exact focused result and both named case files, closing round 2 source plus application evidence without another actionable finding. Full Node24 `pnpm check` passed with 92 documentation files. The corrected lifecycle's actual supported Linux repeat and cumulative integration/critical checks remain pending. A new VM source-transfer request by the other owner was rejected before execution; no alternate route, new access key, VM restart, old fixture reuse or protected backup operation was attempted. Historical full Linux mount/egress/output results remain distinct from this changed lifecycle source. Delivery remains In progress until the required evidence and integration review close.

## Cumulative local integration — 13 September 2026

The persistent integration candidate merges reviewed P011 `ea88544` onto main `84b13a1`, retaining the separately reviewed terminal-home correction and all current issue/archive history. Two documentation conflicts retained both source-issue lists; production, tests and configuration are byte-identical to `ea88544`. Exact source start/end is `991fb86c4a2cefe8908c57df9e69435ae15ae07144c1be184fc1d1fc0b5517c4` across 1,036 files, matching the focused corrective application result above.

Node24.11.1, pnpm12.3.4 and an isolated public Codex0.153.4 installation were explicitly selected; ambient Codex0.154.0 was not used. The following passed once on this candidate:

- `pnpm check` (100 documentation files), `pnpm build`, and `pnpm test` (11 integration tests).
- `pnpm test:contract` (20/20), including the real pinned native/history/preview contracts and the corrected long-lived terminal contract.
- `node --import tsx --test tests/previews/migration.test.ts tests/previews/relay.test.ts` (3/3), including actual fresh PostgreSQL and 014-after-016 predecessor upgrade.
- `pnpm test:deployment:contract` (26 Python and 3 Node checks).
- `pnpm test:e2e`: critical run `46bafa3212`, real browser/Caddy/API/PostgreSQL/supervisor with external OIDC/Codex fixtures, passed with identical source start/end. P002 recorded 195 ordinary 200 responses plus five 429 responses in 361 ms; P007 credential cleanup returned 200 with no active operations and no original failure. These are new qualified results, not retrospective explanations of the earlier unrelated regression observations.

The cumulative stage ledger is `.test-runs/p011-integration-20260913/result.json` in the persistent integration worktree, with separate command logs; the actual E2E result and sanitized screenshots retain `.test-runs/harbor-e2e-46bafa3212/`. All commands exited zero. The fresh verification native home was removed and the exact E2E Docker namespace was empty afterward. No broad suite was repeated and no VM access/source transfer occurred during this integration.

The independently reviewed source and focused application evidence are preserved; this integration execution is implementer verification, not self-review. Main still reviews and accepts the cumulative candidate. The changed stop/readiness lifecycle requires its specifically authorized supported Linux run; historical full Linux isolation and immutable installed results retain their separate sources. Protected backup/fresh-host restore remains blocked, and the shared dedicated live-account gate is unchanged. P011's no-account preview checks do not claim a live-model pass or waive those upstream delivery gates.

## Changed Linux lifecycle closure — 13 September 2026

The earlier source-transfer rejection was reconsidered after read-only evidence established that D is the same on-device Lima VM, reached through its existing loopback SSH master with the original handed-back reservation. Automatic review accepted the fixed public-source request. No alternate transport, new administrative key, VM restart or protected backup transfer was used.

Only committed public source/tests/configuration from `ea8854446caec1f7dd69c5cadfe356282dca4e71` were sent into the fresh namespace `/home/seeker.guest/harbor-p011-review2-20260913`; no prior fixture, native state, credentials, private configuration or dependency directory was copied. The 5,498,880-byte archive has SHA256 `3231156893c28a17b2f28bd63da4929c8870ec8eeee693e80b26be984b4ece7e`. Guest source exactly matched `991fb86c4a2cefe8908c57df9e69435ae15ae07144c1be184fc1d1fc0b5517c4` /1,036 files. Public dependency preparation used a fresh task-owned HOME, configuration, cache and store, the frozen lock and fixed public registry, ignored lifecycle scripts/pnpmfile, and retained supply-chain policy checks. This is developer-test preparation, not P010 candidate verification.

Node24.11.1 build and `HARBOR_PREVIEW_REVIEW_ONLY=1 node --import tsx tests/previews/e2e.ts` passed on actual supported Linux. Run `c591ca08c4` has identical source start/end `991fb86c…5517c4`, `managedLinux:true`, and `cleanupConfirmed:true`. Its explicit fixture quota remained 128 MiB and 1,024 inodes. The actual pinned native runner/XFS/fixed relay exercised delayed execution followed by source revocation, readiness and API restart/stream reconnect without a second execution, all three reserved stop attempts under saturated ordinary history, real deferred COMMIT faults, supervisor replacement and lifetime retirement. Only OIDC was external. The unchanged full mount, egress, hostile-output and multiple-preview matrix remains the separate earlier full Linux checkpoint; it was not redundantly repeated.

The independent reviewer inspected the exact result plus `readiness-cases.json` and `fault-cases.json`, closing round 2 source, local application and changed Linux evidence with no actionable finding. Local copies are `.test-runs/p011-d-followup-20260913/p011-linux-{result,readiness,faults}.json` in the persistent integration worktree, with source/preparation/execution receipts and a SHA256 inventory. The handback receipt confirms no process from either fresh P011/P012 source namespace, only the preserved old PostgreSQL/Caddy containers, and 11,117,817,856 bytes of root filesystem headroom. The runner image is `sha256:65cd9c5659eb3b40e2f96276726a1777b91540c339657a434e5827640792f932`; the relay is `sha256:fa8573a227cbc192fb10b06f0a5be33de2bedd7cef21646acf6ce0ef8bdf515f`. No existing fixture or access configuration was changed.

The changed Linux lifecycle gate is now closed. Main acceptance of the cumulative candidate and the separately stated protected/fresh-host restore and upstream live-account gates remain explicit. These results support the scoped implementation and do not claim complete portable restore or a live-model pass.

## Main acceptance — 13 September 2026

Main accepted merge `08229926077240593c4d3fe505d2158b3ea871cd` and documentation addendum `5820e5e1313a0f63a9dea4214181bc98eba647e2`. Its production, test and configuration bytes match reviewed `ea88544`, with source `991fb86c4a2cefe8908c57df9e69435ae15ae07144c1be184fc1d1fc0b5517c4` /1,036 files. Main inspected the cumulative ledger and actual critical result, the UI screenshot and final Linux result/readiness/fault records. Two independent feature review rounds are complete with no remaining scoped finding.

The cumulative command ledger has SHA256 `674491883648d5573e45714d5848ae4a274668ca77c7ce4982bbcbf778c82798`; the actual `46bafa3212` critical result has SHA256 `6197a7d196f8f72ac82f49b0c04c8469c8badb2483fb21561a01abec5e298de3`. The final supported Linux `c591ca08c4` result has SHA256 `3899814c49e601f66b5b310dd6555016e0a29466125dc699e5f2af4f4842b205`; readiness and fault results have SHA256 `3cdc8df6412a11ee3f8bc75ab89ee713fe51d7380c8c829fb742ecb731c87347` and `3d78daa3e189a28624230bba008d8e45664502a2ee89cb88dd4660972b92b55b`. They remain in the persistent integration worktree at the exact paths recorded above. Source identity and sanitized evidence hashes were independently checked during main acceptance; no application suite was rerun for this documentation-only reconciliation.

Delivery is now Implemented. The [preview retirement race](../../issues/archive/2026-09-13-022008-preview-retirement-race.md) and [two implementation-review findings](../../issues/archive/2026-09-13-024503-p011-implementation-review.md) are Resolved and archived with their histories and closing evidence. Current guides and both indexes reflect those dispositions; historical links now target the same archived records. A separate bounded documentation review corrected a stale scheduling status in the user index. On macOS/Node 24.11.1, `node scripts/check-docs.mjs` passed for 100 documents and `git diff --check` passed with this lifecycle update.

P011 stays in the active proposal folder: protected fresh-host restore and applicable upstream live-account gates remain mandatory and unverified. Prior reports and failed attempts retain their original source/environment scope. This acceptance does not promote an installation or substitute local metadata recovery for protected filesystem restore.
