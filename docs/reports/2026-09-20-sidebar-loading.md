# P023 sidebar loading correction

The owner requested removing sidebar loading indicators because they repeatedly insert a row and shift the conversation list. [P023](../../design/proposals/archive/023-sidebar-and-transcript-refinement.md#sidebar-loading-correction--20-september-2026) reopens for this narrow presentation correction. The work began on 20 September and continued after local midnight on 21 September. Baseline `e3905a22c6102fc967fe9cad69f902d75d21bf10` is clean; the deployed application at the start was `1464bb6fae3a340c4f7f437b2f2239d698ae6fa0`. The owner explicitly authorizes commit, push and deployment. Implementation, local/Linux verification, deployment and both independent review rounds are complete. Private evidence remains in `.test-runs/p023-sidebar-loading-20260920/`; earlier cycles retain their own reports.

## Preparation

Strict-host-key SSH returned `root` and `srv1464935`. The read-only preflight found the expected release `/opt/harbor-personal/releases/p023-1464bb6fae3a`, manifest `6761b3df921fb4670e378ab6a58522b32a6b77bbbac985000cfeb2ef9e54d3fc`, all three services active, 0 active operations and 62341066752 free bytes. This is prerequisite evidence only. Five candidate build/test/linkage/stage/installed helpers match the prior correction byte-for-byte. The reused original UI-only promotion helper requires equality for every API/runtime/migration input, retains full artifact verification, drains work, makes a matched backup and checks installed processes/auth. Its hash and original source are retained in `helper-reuse.json`; no helper safety check is relaxed.

## Implementation and local gate progress

The application delta is one rendering condition: the existing loading paragraph renders only in search results, so no sidebar loading row is inserted. Asynchronous state, busy semantics, disabled pagination, errors/retry, empty guards and query-recency logic are unchanged. Current design/guide and prototype are updated together. Existing real-stack loading assertions now check quiet initial loading and retained desktop/touch row geometry while history responses are held.

The first build passed at source `c12fee072b62d0fcc79b8972a27b892ae2dbcdf4fa7bfa02f432e2854a690992`. The first check failed only on Prettier formatting in the modified P023 test; TypeScript, documentation and OpenAPI checks passed. That failure is retained. Source is frozen while the critical lane runs; the same implementer will then apply the formatting-only correction and rerun affected gates. A green critical result can cover the unchanged boundary only with a complete source inventory and exact test-only formatting diff assessed by both independent reviewers. No failed check is counted as a pass.

The first two unit attempts retained the already documented macOS temporary-path mismatch (`/var` versus `/private/var`), with the first also subject to sandbox limits. With the established canonical `TMPDIR=/private/tmp` and scoped listener permissions, all 39 tests passed; no application/test logic was changed for that environment prerequisite. Critical-01 passed at unchanged `c12fee…0992`, raw-log SHA256 `86cabb73ee6ce2d9125609ab3cf573a444ad8a8d6ee180d0f9e23a2f1a705045`, owned real-stack result `harbor-e2e-20b1cf9ae5`. Main independently reconstructed all 1,093 source hashes using the retained pre-format test file.

Design-01 passed desktop checks, then failed its new touch geometry assertion with a 38.404px viewport-coordinate change when the browser scrolled to the tapped control. Main rejected measuring relative to the conversation list because that would hide the actual regression of a loading row inserted before the list. The same implementer must correct scroll handling while keeping sensitivity to layout shifts, retain the failed run, and rerun affected final gates. No product workaround or assertion removal is authorized.

Final Build, Check and Unit (39/39, none skipped) pass at `8079b1e59cdcaf5328d7f01b5d80daf30a4cab42bc31ac89d0ff86186f472089`. Main verified their receipt/raw-log hashes and identical start/end identities. The only change from green Critical-01 is the retained `critical-reuse.diff`: a formatter line fold and settling the touch Show more control before recording geometry. All application and critical-executed behavior inputs are unchanged; `critical-source-files.json` and `final-source-files.json` bind all 1,093 inputs. Final Design passes at the same source; actual real-stack result and screenshots are retained under `.test-runs/harbor-e2e-b5f835ad4e/`. Main verified the final receipt/log/result linkage.

The prototype check passed quiet initial loading and retained rows during pagination/background refresh on desktop and touch at prototype SHA256 `8288dc91eb35cd90426f77e384adfa986f34a0f5fa0146d664572c62ccf2df8d`. Main inspected both refreshing screenshots. The retained script/result/screenshots are prototype-only evidence, separate from actual application acceptance.

The application held-request screenshots show desktop and touch rows retained without a loading label. All local gates are green. Two fresh Medium-effort independent reviewers are assessing the scoped source, tests, provenance and exact critical reuse; no further implementation edits are pending.

## Independent review — round one

Both fresh independent roles clear the local gate without actionable findings. They independently verified final source `8079b1…2089`, the exact five-file diff, build/check/unit/design receipts, real-stack results, desktop/touch application and prototype images, complete source inventories and the narrow Critical-01 reuse delta. Main accepts both assessments. The P023 test module is imported by the E2E runner but its design-only body is not executed by the critical lane; the exact changed file is disclosed rather than claiming every imported byte is identical. Five deployment helpers match the prior correction and the promotion helper matches the earlier full-API-equality UI release workflow.

Prototype evidence includes the matching source/script, generated result and six inspected screenshots; a standalone prototype command/exit/raw-log receipt is unavailable. Complete raw review-session exports are also unavailable; reviewer messages record the independent assessments. Fresh Linux candidate and installed verification remain pending. No application edits follow the green gate.

## Committed candidate — 21 September 2026

The reviewed application was committed and pushed as `27c1e7d16f2d823ebbc0f12df3ee75e37d820919`; origin/main acknowledged the push and the checkout was clean. A fresh candidate at `/var/lib/harbor-dev-20260914/p023-27c1e7d16f2d823ebbc0f12df3ee75e37d820919` received seven inputs with matching local/remote SHA256 values. Bundle SHA256 is `c47562d6fae6fdd047dc23f94ee55b5654fe23e0d0e53c33da25522e0196b1fb`; upload archive SHA256 is `0e2a87b098d89df43fc78b1f2ac1513e9a1c4ab16125387e78830ca9e7480841`. The reused fixed workflow now performs a fresh nonroot locked install/build/unit gate and packages an immutable release. The current installed release is unchanged at this point.

## Linux release acceptance

The fresh nonroot Linux locked build and all 39 unit tests passed. The 244,807,094-byte package archive has SHA256 `2170b30739203986f56c00e9dbd8ece3fa75141ca956e4b4016f83df0ae37424`; its 14,221-file manifest is `f7270c421db6159ff0447688a670a4502a07a76736bb8da1e0e55d2a16f8038e`. Linux real-stack design result `harbor-e2e-af59e3fc3b` passes at exact final source `8079b1…2089`; full before/after source/package equality receipts are identical. The 31 retrieved evidence files, raw logs, manifest and actual screenshots are retained under `linux-evidence01/`, with hashes in `linux-verification-record.json`. The evidence archive SHA256 is `bbee539d445ffa920d47ce833cb799b5b04a9dc4ce3044f1a61202aa8cd8e63a`; main inspected the Linux touch held-request screenshot. This is application acceptance using real Harbor UI/API/PostgreSQL/supervisor and external OIDC/Codex fixtures, not new native runtime or isolation certification.

Stage-01 passed full manifest and root-ownership verification for `/opt/harbor-personal/releases/p023-27c1e7d16f2d`. Promotion now uses the reviewed unchanged helper with exact predecessor `/opt/harbor-personal/releases/p023-1464bb6fae3a`; installed verification follows.

## Installed verification — 21 September 2026

Promotion-01 passed after draining work and making matched backup `/var/lib/harbor-personal-backups/p023-27c1e7d16f2d-20260921`, preserving the existing preview. Current release is `/opt/harbor-personal/releases/p023-27c1e7d16f2d`, revision `27c1e7d16f2d823ebbc0f12df3ee75e37d820919`, manifest `f7270c421db6159ff0447688a670a4502a07a76736bb8da1e0e55d2a16f8038e`. The exact prior release `p023-1464bb6fae3a` is retained. No migration or restore drill was performed; existing SSH recovery and restore limits are unchanged.

Installed-01 passed. API PID 3995868 and supervisor PID 3995869 execute from the new release, with no predecessor processes. HTTPS health returns 200; anonymous root/index, `/api/v1/me`, `/api/v1/projects` and actual JS/CSS assets all return 401. All promotion/installed receipts have exit zero, stable source identity and retained raw-log hashes.

Main opened the previously approved disposable canary in a fresh Chrome tab because the earlier acceptance tab had been closed. The authenticated page loaded exact deployed assets `/assets/index-DD1ZLq0D.js` and `/assets/index-DL-aHLP7.css`, one history row and an empty composer; sidebar text contained no loading label. No query was submitted, no project content changed, and the owned check tab was closed without touching existing user tabs. `installed-ui-observation.json` records these narrow observations. They do not independently prove held-request stability; local and Linux real-stack gates do. A complete raw CUA trace/export is unavailable.

Independent closing review is now pending in round two. Application source remains unchanged from the clear local review. Report downloads/previews and unrelated historical native/restore obligations remain open in their existing records.

## Closing review and completion

Both independent roles clear round two. They verified the unchanged committed implementation, Linux candidate/result/screenshots, all 31 retrieved evidence hashes, full artifact equality, deployment receipts, retained predecessor/backup, installed service/auth identity and exact browser asset names. Design found one low-severity report inconsistency: the introduction used present tense for the predecessor revision. Main accepted and corrected it to explicitly identify the application deployed at the start; the same reviewer verified that fix and cleared completion. Provenance found no actionable issue. No application or deployment rerun was needed for that wording correction.

Main accepts both clear assessments. L1–L3 and all scoped gates are complete, with no unresolved in-scope finding. P023 is restored to Implemented and archived as Completed on 21 September under the same ID/filename; inbound/outbound links are repaired. The closing documentation gate passes for 163 Markdown files, including explicit archived-file whitespace/newline checks. The documentation-only closing commit/push follows deployed application `27c1e7d…0919`; it changes no built application artifact and requires no redeployment. The cycle closes after two review rounds, including verification of the sole accepted documentation finding. Earlier delivery evidence and unrelated open obligations remain historical and unchanged apart from proposal links.
