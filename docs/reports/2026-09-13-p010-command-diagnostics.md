# P010 command and log diagnostics checkpoint

Date: 2026-09-13. [P010](../../design/proposals/archive/010-self-development.md) remains In progress under [D008](../../design/decisions/008-isolated-self-development-workers.md).

The second actual C component used the original `1baf935` candidate capture `23d87d4f59e0fde6ac1ddb8ac9122ab0235dcd2735f30ee69ce3960307eae603` and the separately identified `4eac666` diagnostic controller. Build job `f2a2c241f50347b3855f0054ab2b5a69` uploaded successfully and recorded a completed command result with exit code 1, no log truncation and no trusted verification. Its subsequent final log read failed before private log publication. Exact worker retirement and allocation cleanup completed; no containers remained. The sanitized result and receipt hashes are retained in `.test-runs/roundtrip-diagnostic-4eac666/failed-result.json`, SHA-256 `7e9cdf5a547b6ca7fc991725dd599fa6088e993ab6e48dc64717401d32e2da02`. Neither the build failure nor the log-read failure has an established cause. The later guardian `PIPE_CLOSED` retirement reason does not establish an earlier lease failure. No artifact, fresh verifier, trusted report or stable UI acceptance passed.

A bounded source trace confirmed that the client discarded the broker's safe exception-class frame on nonzero process exit. It also found a separate maximum-log mismatch: two MiB of raw bytes may require twelve MiB of JSON-escaped string content plus the object envelope, exceeding the previous exact twelve-MiB client limit. These are established diagnostic/framing defects, not explanations attributed to the actual C failure.

The correction preserves only fixed diagnostic labels from a strictly shaped failed broker frame. Controller timeout, abort, pipe, spawn and size failures settle immediately with their own fixed labels; raw stderr and exception messages remain private. The command component records the final-log-read phase separately from the already completed candidate command result. The log response limit now includes a fixed four-KiB envelope allowance, while decoded text remains bounded to the maximum character/replacement expansion of two MiB raw input. This does not change the production raw log cap or grant report authority to logs.

Validation used Node 24.11.1 and local Python with fresh synthetic public process data. `node --import tsx --test tests/self/client-diagnostic.test.ts` passed four tests: actual child nonzero/invalid responses, maximum escaped NUL and replacement-character envelopes, one extra decoded raw byte refusal, and deadline/abort with exact child death. `python3 -m unittest discover -s tests/self -p broker_logs_test.py` passed two tests through the actual broker routing/serialization and bounded exchange pump, replacing only the Docker/identity boundary with a benign local process: exact two-MiB invalid-byte output retains `untrusted:true`; one extra raw byte produces only the safe error envelope. Logs are `.test-runs/client-diagnostic/contracts.log` (`15b258b65490ec8451c66acac9d99c30cb3556ee99faf5fc7bfa77e02207b6db`) and `broker-contracts.log` (`522597959ccc5756c9d89b89390fbe3478d2bae7855d68affc350e87ea6c465b`). TypeScript checking passed.

Independent correction review and a subsequent actual diagnostic remain pending at this checkpoint. No guest was rerun for these local contracts, and no protected transfer, base-image update or promotion occurred. The moving candidate-view implementation is separate from this correction.

## Independent correction review — 13 September 2026

A separate reviewer inspected frozen checkpoint `4f69765a6bf90f34c45cbf4d394f8721c03c9f6c`, the production observer and both retained contract logs. The bounded diagnostics/framing assessment closed with no actionable finding. The reviewer confirmed that successful responses wait for process close, controller failures retain fixed causes, and wire/decoded limits remain finite. No tests or C attempts were rerun by the reviewer. This addendum supersedes the earlier pending source-review statement only; actual corrected C diagnostics, complete candidate acceptance and main integration remain pending. The [active issue](../../issues/2026-09-13-063901-p010-log-diagnostics.md) retains those obligations.

The first failed C component is preserved separately from the second diagnostic attempt: candidate `1baf935`, job `e9f9a2e2ae4880e8a3b5413d546a8140`, the same exact captured source digest above, build exit 1, and confirmed retirement/allocation cleanup. Its driver omitted final log collection before cleanup, although production already collects logs before handling a failed build result. The P010 worktree retains `.test-runs/roundtrip-1baf935/failed-result.json`, SHA-256 `2eba0dc5a817e21486d76d4fe66c407ef98bc1f95d03a845486a1c7e5ef4f6e5`. Its cause remains unknown; neither the later diagnostic correction nor a separately identified packaging prerequisite explains that original outcome without runtime evidence.

## Third actual diagnostic — 13 September 2026

The third owned C component used candidate `1baf935` with the unchanged exact source capture `23d87d4f59e0fde6ac1ddb8ac9122ab0235dcd2735f30ee69ce3960307eae603` and a separate controller containing only the `4f69765` client/roundtrip correction over that baseline. Job `f06095a063e88a0787f5e053b9721dc1` uploaded successfully and completed its candidate command with exit 1, no truncation and no trusted verification. The corrected client retained `guest-build-untrusted.log` privately on C before cleanup: 2,741 bytes, SHA-256 `ad4dbec98ef0a58ced5e3718e3883eb7e2eb497eedb8494fa2e826d82023a9d7`. Main did not copy or inspect that private log.

Main read and independently hashed the sanitized contemporaneous receipt `.test-runs/roundtrip-diagnostic-4f69765/failed-result.json` in the P010 worktree: SHA-256 `99eb98d884a34bd018f643fdbe691edc744109535d1442cd9c88b577e0e71dcd`. It records phase `candidate-command-result`, confirmed retired job/worker/guardian records, removed allocation, no running containers and an inactive/dead component service. The allocated verifier ID is a ledger record; no verifier ran. Physical control free space after cleanup was 22,113,234,944 bytes. The receipt's qualification records the implementer's private-log observation of esbuild spawn `EACCES`; exact failed-path permissions, ancestors and mounts were not retained after guest retirement.

This establishes actual corrected log capture and owned cleanup. It does not establish a successful build, artifact, verifier, browser, stable UI or deployment outcome, and it does not explain earlier failed attempts. The [separate cache executable-mode finding](../../issues/2026-09-13-070147-p010-cache-executable-mode.md) records independently confirmed source/archive evidence and the required correction. No further guest attempt was launched for this diagnostic readback.

## Navigation and ownership addendum — 20 September 2026

P017 reconciled legacy planning records into the [proposal archive](../../design/proposals/archive/). [Current subsystem designs](../../design/systems/) now own requirements, and unfinished acceptance belongs to the [issue inbox](../../issues/) until a new proposal is selected. Historical statements here about active proposal states, queues, permissions and recovery locations describe the recorded date. This addendum changes navigation/ownership only: original tested sources, commands, results, review rounds and limitations above remain historical evidence. No application gate was rerun for the documentation migration. Historical raw artifacts or worktrees not present in this checkout were not newly verified.
