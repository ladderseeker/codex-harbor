# P023 query-recency correction

The owner clarified that only sending a query changes sidebar recency; opening a session must preserve its position. [P023](../../design/proposals/023-sidebar-and-transcript-refinement.md#query-recency-correction--20-september-2026) reopens at clean baseline `c74f88ec067ea7d7c171d4dcea24e23f72178c19`. Prior UI and correction reviews remain historical in their reports. The owner’s existing commit, push and deployment authorization continues.

Implementation and verification are in progress. Main owns planning, combined gates and deployment. A fresh Medium-effort implementer owns the ten-file fence; new independent design/provenance reviews follow the green gate. Private evidence is retained under `.test-runs/p023-query-recency-20260920/`.

## Source and deployment prerequisites

Read-only source investigation confirmed that accepted turns insert user messages in the same transaction as operations, while idempotency replay returns the original result before admission. Supervisor output/status events alter `sessions.updated_at`, so that field is intentionally not used for the new query ordering. The query mode derives the latest persisted user-message time with session creation as the no-query fallback. It needs no new schema, write path, dependency or runtime behavior. Existing created/updated API modes remain compatible.

Strict-host-key SSH returned `root` and `srv1464935`. The fresh preflight identified `/opt/harbor-personal/releases/p023-a94f4c1f8bed`, manifest `e153ca89e91d4abc203e161d56fd6cddf9a0fbe1c07cf81c2976d1292a9cbc7d`, all three services active, zero active operations and 65.7 GB free. These are prerequisites only. Five build/test/linkage/stage/installed helpers are reused byte-for-byte; the promotion variant changes only its exact predecessor history-file SHA256 to `e50c73f6d7141ead40c33d4a60d5ef107fab079373f29ee9a1bb2b32d6a57ea1`. Full candidate verification, other backend/runtime/migration equality, drain, backup and SSH recovery constraints remain unchanged.

## Prototype acceptance

Main's `prototype02` check passed desktop and touch interactions against the unchanged prototype file identified in `prototype-evidence.json`: viewing another conversation and typing a draft preserve all ten loaded positions; sending moves only that conversation first, and later viewing another leaves the order intact. Main inspected desktop and settled touch screenshots. The initial `prototype01` behavior assertions also passed, but its touch screenshot caught the sidebar animation; the second capture polls settled geometry. Both runs remain retained. These are prototype-only results while application tests were still being edited, not final application-source acceptance.

## Local verification in progress

Build/check and all 39 unit tests passed at frozen source `17a8da4f3ff6a2c3b1e6c8c0f4ef168ef7c688bd1ed48d8d6f37455be735c46d` (1,093 files). `impl-unit01` failed because sandbox restrictions denied a required local listener; the identical command with scoped permissions passed as `impl-unit02`. Main verified their raw-log hashes and unchanged start/end identities.

`main-critical01` stopped before application startup when Docker's registry authentication endpoint returned EOF while pulling the pinned PostgreSQL image. It is not an application assertion failure. Both exact Compose images were subsequently present; an initial inspection used the wrong Caddy tag, then inspection of the actual `2.10.2-alpine` tag succeeded. The failed gate remains retained. Main identified reduced legacy updated-mode cursor assertions during final source review and requested restoration by the same implementer after running lanes clean up; source remained frozen during concurrent verification.

The first design lane failed in a newly added test comparison: the captured `textContent` included hidden “Local” text while its assertion requested visible text. The same implementer aligned capture and assertion using visible text; product code did not change. Main also required restoring the still-supported updated-mode one-item cursor traversal and filter/selection mismatch checks. A TypeScript fixture-map inference error in that restoration failed Build-02/Check-02; its explicit type correction passed Build-03/Check-03. Final Unit-03 again passed all 39 tests. These final gates identify unchanged start/end source `0034863e2d95cdeadb32de6e0169f011ada442dc83b246713e0ed1c49fb9399e`. Final design and critical lanes are running concurrently on that frozen source. No failure was waived and no application workaround was introduced.

Full Critical-02 passed on frozen `0034863…9399e`, including the real application regression suite, with retained raw-log hash `76a6d5fd0c318dc54d06e0eec6c0a315fd3f3ce6e8676eeb7268d3703585fafb`. Design-02 passed the new desktop/API ordering checks and then failed in test setup: the new touch navigation assertion left a no-query conversation selected before the existing message-action check. Main accepted the narrow test fix to return to the message-bearing conversation, retaining both assertions. The implementer waited for critical cleanup before editing. The complete critical source inventory and original P023 test file are retained; the green critical result may cover unchanged inputs only if the subsequent delta is solely this design-lane setup correction. Final affected gates and both reviewers must validate that reuse.

## Final local gate and review candidate

Final Build-04, Check-04, Unit-04 (39/39, none skipped) and Design-03 pass at identical start/end source `1783cf6d1341ef8cc1433326aedee38b359ead3fc5ff97f3f66887772752bc69`. Actual design screenshots/result are retained in `.test-runs/harbor-e2e-792dc3b523/`. Main independently verified receipt/log hashes and all 1,093 source-file hashes. Compared with green Critical-02 (`.test-runs/harbor-e2e-e72e883419/`), the sole source change adds the four touch-test setup lines described above in `tests/e2e/p023.ts`; all API, UI, runtime and critical-lane inputs remain identical. `critical-reuse.json` and its exact diff bind these sources. The unchanged-boundary rule supports reusing that critical pass, subject to both reviewers.

The final ten-file implementation inventory and diff are retained in `review-implementation-files.json` and `review-implementation.diff`. No implementation fence deviation or unresolved finding remains; source is frozen. Independent local review and Linux candidate/installed verification are next.

## Independent review — round one

Fresh Medium-effort design and provenance reviewers independently cleared the ten-file implementation and local gate without actionable findings. Both verified final `1783cf6…bc69`, Q1–Q3 coverage, current contracts, actual prototype/application artifacts, and the exact four-line design-test delta supporting green Critical-02 reuse. Provenance independently reconstructed the 1,093-file digests, matched all final gate/raw-log/result identities and verified the unchanged deployment helpers plus the single exact predecessor-hash change. Main accepts both assessments. Review messages record these assessments; complete raw review-session exports are unavailable. Linux candidate and installed acceptance remain pending.
