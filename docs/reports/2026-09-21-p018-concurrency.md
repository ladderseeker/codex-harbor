# P018 concurrent conversations — execution record

Current outcome: P018 is Implemented and archived as Completed. Commit `817c3d0971665871b64aa29c0fb25e8c8b132f02` was pushed and deployed to the owner’s VPS on 21 September 2026. The [final delivery](#committed-release-and-vps-delivery) records artifact identity and installed checks. The sections below preserve earlier checkpoints, including failures and subsequently resolved prerequisites; they do not override the final outcome.

## Initial record

P018 is Accepted and in implementation. The owner requested implementation, verification, commit, push and deployment on 21 September 2026, with the existing sidebar order preserved. No completion, commit or deployment is claimed by this initial record. [D014](../../design/decisions/014-personal-conversation-concurrency.md) owns the changed personal-profile contract; [P018](../../design/proposals/archive/018-concurrent-conversations-and-runtime-capacity.md) owns its acceptance.

## Baseline and prerequisites

The checkout began clean on `main` at `1cc41d7`. The preceding implementation includes the last-accepted-query sidebar order and visually quiet loading. Source discovery covered active/archived proposals and issues. Main updated the proposal, focused decision and canonical contracts before delegating disjoint core and interface implementation with fresh context, using High and Medium reasoning respectively.

The local toolchain reports Node `v24.11.1` and pnpm `12.3.4`. The default PATH Codex reports `0.154.0` and is not the protocol pin. The complete native `0.153.4` executable is available at the explicit private tooling path used by the probe. Docker access and native process inspection need the scoped sandbox permission mechanism.

The first sandboxed SSH connection returned `Operation not permitted`; scoped strict-host-key connections returned `root` and `srv1464935`. Read-only preflight identified the current personal release `/opt/harbor-personal/releases/p023-27c1e7d16f2d`, revision `27c1e7d16f2d823ebbc0f12df3ee75e37d820919`, manifest SHA256 `f7270c421db6159ff0447688a670a4502a07a76736bb8da1e0e55d2a16f8038e`, all three instance services active, and zero active or uncertain operations. This is prerequisite evidence, not P018 deployment acceptance.

A fresh empty-home direct pinned runtime probe initialized and started a read-only native thread without account credentials. Process inspection returned `known` with startup Git descendants, and the old group-based inspector returned `runtime_gone` after guardian retirement. The probe cleaned only its own runtime/home. Later native probes invalidated that absence claim for ordinary retained commands: separate PGID/SID children survived native SIGTERM and fulfilled guardian retirement. The initial probe covered only its startup topology, not complete runtime ownership. `.test-runs/p018/tests/native-probe-evidence.json` preserves the subsequent discovery, independent PID observations, exact pinned binary identity, bounded child expiry and raw-log limits. No process-name allowlist is introduced.

The standard dedicated live test key/model were absent locally. The inspected private candidate parent had no matching dedicated live configuration. The owner has been asked to configure dedicated credentials or explicitly authorize a credential-only copy into a disposable test home. Ordinary Codex state and live project data are not permitted test substitutes. Missing authentication remains a mandatory acceptance blocker until resolved, with the existing [live-credential issue](../../issues/2026-09-07-171225-live-runtime-credentials.md) retaining its broader obligations.

## Evidence and next gates

Private evidence is retained under `.test-runs/p018/`: `checkpoint.md`, `core-brief.md`, `ui-brief.md`, `preflight.py`, `preflight.json`, `classify-native.ts` and `classify-native.log`. The preliminary probe command exited zero; its output distinguishes live extra descendants from confirmed retirement. Initial documentation validation passed with 164 Markdown files and `git diff --check` passed. These checks do not establish application acceptance. Complete raw session exports are unavailable; retained commands, receipts and reviewer messages will identify their narrower evidence scopes.

The earlier P023 promotion helper rejects backend/runtime/migration changes and cannot be reused unchanged for migration 019. A separate fixed candidate helper is being prepared; it has not promoted anything.

## Combined candidate checkpoint

The second immutable candidate has source digest `38f3c4b448b612f292dd2a883f2e2dc7453ec7bae4fd9f3eb8bcce491078980b` (1,112 application/test/configuration files), baseline `1cc41d7c11cc45e2490625ab528d05530bddc567`, and full snapshot manifest SHA256 `0ca69fe830510c97b01886fcc55d94669fffba2dcb5540b97e63c4773b6a01a9` (1,281 files). This is a dirty-source candidate identity, not a committed revision. Its create-only source bundle, archive, diff and inventory are retained in `.test-runs/p018/snapshot-02/`; the corresponding isolated VPS directory is `/var/lib/harbor-dev-20260914/p018-p024-snapshot02`.

On that source, Linux `pnpm build`, `pnpm check`, `pnpm test` (51 passed, zero skipped), and `pnpm test:deployment:contract` exited zero. Exact commands, environment, before/after source checks, exits and log hashes are in `linux-static-01.json` and `linux-deployment-01.json`; original command logs and receipts remain in the isolated VPS directory. Local `combined-check-02.json`, `combined-test-02.json` and `combined-design-01.json` also record passing checks, 51 tests and the real browser/API/PostgreSQL/supervisor design lane. The design lane runs P014, P023 and P024, although its console/result scope label still names only P014/P023. External OIDC and Codex responses are deterministic fixtures. These checks do not establish native account acceptance.

The first combined candidate exposed refined Zod-schema extension errors; the corrected API/OpenAPI paths passed the second check/test gate. The Linux native contract run on snapshot 02 passed 27 of 28 checks, including the actual native attachment read/write boundary. Its remaining concurrency assertion compared a sandbox PID with a host PID. The test is being corrected to independently map the process through its owned cgroup and verify its host PID and birth identity; that correction still requires a new native run. The personal concurrency E2E stopped before its scoped cases because fixture-account readiness timed out. The managed critical lane stopped on an obsolete original-PNG-byte expectation after image normalization changed. Both failures remain failures until their corrected tests pass; the source and failure logs are retained.

Remaining mandatory gates include corrected native contracts, real-stack personal concurrency and attachment acceptance, the complete critical lane, dedicated live parallel/reuse and image/file delivery, and independent design/provenance reviews. No commit, push or installed promotion has occurred. The native macOS release boundary and dedicated live credentials remain unresolved prerequisites.

## Third candidate and advisory review

Snapshot 03 has source digest `e5e1b2ecc1cdcac2a651b648d67421327c83dc811eb89c0964c76d8cd994118d` and full manifest SHA256 `1c05339404a9a55831480b3fb67ff1e6046e644bce3b44b13f66e4273071a4cb`. Its 13-file delta from snapshot 02 is retained in `.test-runs/p018/snapshot-03/delta-from-02.json`: application changes are limited to sent-attachment layout; other changes concern acceptance tests and documentation. Backend, lifecycle, attachment storage/publication and dependencies were unchanged in this snapshot. Earlier integration and managed attachment-isolation receipts retain their original source and bounded relevance.

Local build/check/design and Linux build passed on snapshot 03. The actual desktop/mobile sent-attachment screenshots were inspected after the layout correction. Linux frozen installation completed with exit zero, but the SSH transport lost its response; main recovered the original remote receipt rather than rerunning the installation or inferring success. The remote live API/supervisor remained active and unchanged.

The critical lane passed its attachment/workspace/history and programmatic API scenarios, then failed the cancellation-crash uncertainty assertion. A diagnostic rerun later passed that assertion, but its source changed during the run; it is not frozen-source acceptance. Inspection identified a recovery-retry starvation path: a persistently failing cancellation settlement can prevent pending terminal uncertainty repair from running. The first transient uncertainty failure in the original run was not captured. A deterministic regression and independent repair handling are being added without weakening the original expected outcome.

Linux native contracts again passed 27 of 28 checks. Independent sandbox-to-host mapping fixed the sleep-process check; the remaining preview check guessed an executable name. Its replacement independently maps a nonce-tagged preview through the owned cgroup, namespace PID, exact arguments, executable identity and process birth time. A fresh native run remains necessary. Personal concurrency advanced through overlap, capacity, sidebar, protected reuse, targeted stop and archive checks, then received HTTP 403 when sending after supervisor restart; the restart readiness boundary is under investigation. Personal attachment acceptance received HTTP 400 on its first binary upload. Its test used a UUID without the required timestamp prefix for mutation keys; the two affected constructors are corrected, with a fresh application run pending. These are partial observations, not passing lane results.

Two fresh independent reviewers completed advisory round 1 because mandatory live/platform dependencies were still unavailable. The design reviewer found one P2 admission/retirement race: reuse used a runtime captured before acquiring the shared fence, while an old completion could retire a newly claimed continuation. Main accepted the finding and assigned atomic reuse rechecks/claims, operation-fenced completion retirement and deterministic SQL-barrier coverage to the original core implementer. The provenance reviewer found two P3 guide defects—an old upload retry label and ambiguous historical review wording. Main corrected both and linked the original P005 evidence explicitly. Fix verification and reviewer rechecks were pending at this checkpoint; later results appear below. Dispositions are retained in `.test-runs/p018/review/round-01-dispositions.json`; reviewer messages are review evidence, not complete raw session exports. This advisory round does not satisfy the normal completion review gate.

## Sixth-candidate verification and fixes

Snapshot 06 has source digest `e0535d00138567f6733006b553b40ca4ebece645e769e90b2ee332dffab749dd` (1,112 source files) and full manifest SHA256 `666e1173701563ad3df075abd55a9707d133ee1a72821a69b8d102ab118d846e` (1,281 files). The immutable archive, Git bundle, diff, inventory and receipts are under `.test-runs/p018/snapshot-06/`; the corresponding isolated Linux candidate is `/var/lib/harbor-dev-20260914/p018-p024-snapshot06`. This remains uncommitted source.

The core implementer fixed the reviewed reuse/retirement race by rechecking and claiming the exact runtime under the admission fence, retaining completion ownership through required retirement and rejecting dispatch through a retiring runtime. A follow-on admission recheck initially returned false after mutating the managed writer reservation. It now raises a typed retry signal that rolls back the whole transaction and leaves the turn queued. An actual PostgreSQL barrier test proves the reservation is absent after rollback and a subsequent ordinary API turn succeeds. The prior critical failure's exact row state was unavailable; the source defect and passing regression do not retroactively prove that uncaptured state.

Terminal uncertainty repairs now run independently, before cancellation settlement retries, with per-entry failure isolation. The critical regression injects two failed uncertainty writes and persistent cancellation COMMIT failures, then requires the uncertainty repair to retry successfully. Separately, the newly enabled VPS attachment flow exposed a legacy supervisor maintenance guard that skipped garbage collection. Removing that guard lets the actual supervisor expire unreferenced staged bytes and stale drafts while preserving fresh draft references; the test polls that behavior rather than calling the helper directly.

The complete local critical lane passed on unchanged snapshot 06 source: `node --import tsx tests/e2e/run.ts --critical`, exit 0, 03:52:29–03:56:59 UTC. It exercised actual UI/API/PostgreSQL/supervisor behavior and the P001/P002/P003/P005/P007 regressions, including the rollback and repair faults; only external Codex and identity-provider responses were fixtures. Receipt/log: `.test-runs/p018/combined-critical-06.{json,log}`, log SHA256 `4a3f215c42071979249fc82111e7d61548340f2b09e46cdc99c22ac4409840ad`. The expected injected supervisor failure message inside that passing log is not a failing gate.

`pnpm test` passed 51 tests with no skips on snapshot 06. Local build/check passed on snapshot 05, source `89b466288ee9bdfd5063e7aa98b4b0314039700d6f54e9ebd50f6aba1a1d402a`; the recorded 05→06 delta changes only the concurrency test's generation observation during its own SQL barrier, replacing a self-blocking snapshot API call with a plain database read. Application/build inputs were unchanged. Linux snapshot 06 frozen install, build and browser preparation passed. Its personal attachment application lane also passed, including actual supervisor expiry cleanup; the [P024 record](2026-09-21-p024-attachments-and-composer.md#sixth-candidate-personal-attachment-acceptance) provides scope and receipt identity.

Linux pinned native contracts passed all 28 checks, zero skipped, on snapshot 04: source `076b2b8538f3adfe2ea296292f564097f039268074f1eecc027151af714327b0`, full manifest `065c8ac5681e34c98c5b74ae6274a77caf2f639583b403481f43fb5f6e1795be`. `pnpm test:contract` ran in a fresh delegated nonroot systemd unit using Codex 0.153.4. The tests independently mapped real idle, non-PTY sleep and preview processes through namespace PID, owned cgroup, exact arguments, executable identity and birth identity, then checked targeted absence and sibling survival. Native attachment reading and write denial also passed. Receipt: `.test-runs/p018/snapshot-04/linux-contract-01.json`; remote raw log SHA256 `8520c73fa398b1e49efa05bcbbeaafd3ebdea3c6da73e17455ff63a86935240f`. Adapter, cgroup, attachment publisher, native contract and dependency files are unchanged through snapshot 06. This evidence applies to those boundaries; later supervisor integration changes are covered separately and do not inherit an unrun contract claim.

Linux concurrency snapshot 06 still failed: its restart readiness helper required fresh model discovery even during deliberate maintenance, where discovery is disabled. The isolated journal showed the supervisor running; the failed predicate is retained in `linux-concurrency-01.json`, raw log SHA256 `54060fee695d7eab40679831b69fd43d6d87d42eb9e3dbd445a43714c2e036c7`. The subsequent workspace-replacement scenario also intentionally prevents discovery. The original implementer is correcting those test cases to require a new supervisor generation and the exact expected operation outcome, while preserving ordinary restart discovery checks. This lane remains failed until a fresh run passes.

Advisory paired review round 2 verified the first-round fixes and found no new concrete source blocker. Both reviewers checked the frozen snapshot identities; provenance also inspected the 28-pass native raw log and the bounded source links. Dispositions are `.test-runs/p018/review/round-02-dispositions.json`. Two paired advisory rounds have been used. They do not satisfy normal completion review while mandatory authentication/platform gates are missing. A final normal review remains within the three-round limit once the complete selected gate is green.

The [native macOS ownership issue](../../issues/archive/2026-09-21-121000-native-macos-runtime-ownership.md) records the pending release-scope decision. The [live prerequisite addendum](../../issues/2026-09-07-171225-live-runtime-credentials.md#selected-personal-profile-prerequisites--21-september-2026) names P018/P024 ownership without absorbing older managed obligations. Neither question has an owner answer. No credentials were copied, and no commit, push or live promotion occurred.

## Passing Linux application concurrency

Snapshot 07 passed the complete personal concurrency lane: `node --import tsx tests/personal-vps/e2e.ts --concurrency`, exit 0, 04:08:00–04:09:40 UTC. Source digest: `a38f2e86a85be2cdb2673a7650e527cb4c72cf23cc3b648c3f5b84b9e26b3e14`; full manifest: `db85dec13903b4fedfc9c7d28374568c2353089238a448c55d5bd89303d6f7be`. Before/after inventories matched all 1,282 files. Frozen install, Linux build and browser preparation also passed. Application code is unchanged from snapshot 06; the two changed test files distinguish ordinary restart discovery from exact maintenance/invalid-workspace operation settlement. The full delta is retained in `.test-runs/p018/snapshot-07/delta-from-06.json`.

The lane passed 12 grouped checks across same-directory writable overlap, independent thread identity, same-session serialization, oldest eligible eviction and reuse, admission/retirement barriers, active/runtime limits, approval/input protection, durable queue and cancellation, protected reuse fairness, targeted stop/archive, permission revocation, maintenance, replaced workspace identity, unknown restart fencing and desktop/mobile sidebar observations. External Codex and OIDC responses remained fixtures; actual native and live account gates retain their separate scope. The receipt is `.test-runs/p018/snapshot-07/linux-concurrency-01.json`, raw log SHA256 `295f285b5eb747739a82aea2c48ac92f688f0ed9fb09c95805522d76fc88f0ee`. The known `concurrency-result.json` artifact has SHA256 `a52c4b5431d7f5e916db491a225c5bf46958016b504bc46daefd6ff52c3e4c4e`.

Main inspected the copied screenshots. Desktop showed four independent green resource dots with preserved query order. The mobile capture occurred during the existing 180 ms sidebar transition, clipping its left edge; it is not a settled-layout screenshot. The original implementer added a geometry-based wait for the sidebar's full width and zero horizontal offset, preserving all existing assertions and without a fixed sleep. This is a test-only evidence correction; a new capture run is pending. It does not invalidate the completed behavioral observations or establish an application layout defect.

A read-only cleanup check found no containers, test users, systemd units or temporary directories for the seven identified application run IDs through snapshot 07. The installed P023 release and manifest still matched the initial preflight, and API, supervisor and dependencies remained active. `.test-runs/p018/observed-cleanup-07.json` retains the bounded observation. The managed attachment VM is stopped with its disk and evidence preserved. No live installation was changed.

## Final available-gate checkpoint

Snapshot 08 passed the same complete Linux concurrency lane after the screenshot correction, exit 0, 04:15:14–04:17:03 UTC. Its source digest is `19b7886b0f218cf6b29223023e034528a22365d5b6c9a5ff591e2d9fa810b862` (1,112 source files), full manifest SHA256 `bfcfe08b361ae9c17882f0168c4c9994dc641ae3bbad9654ea965c3abaa6c887` (1,282 files). Frozen install, Linux build and browser preparation passed; before/after source inventories matched. Receipt: `.test-runs/p018/snapshot-08/linux-concurrency-01.json`; raw log SHA256 `8ad17e30630e7656667e7d9b448e58c930a52df48d784c3f4b909b570c86c116`. The 07→08 code delta is only the geometry wait in `tests/personal-vps/concurrency.ts`; all application files remain unchanged from snapshot 06.

Main inspected the settled mobile sidebar and unknown-capacity status screenshots. The sidebar is fully in view and shows four distinct green resource dots; the unknown state retains capacity and explains confirmed retirement. Local copied screenshot hashes match the remote originals in `.test-runs/p018/snapshot-08/final-observations.json`. The exact run `harbor-p015-1f7f2bb701` has no remaining container, user, systemd unit or temporary directory. Earlier passes retain their tested-source boundaries: complete critical/integration and personal attachment application acceptance on 06, native contracts on 04, application/prototype UI on 03 and managed XFS attachment publication/mount acceptance on 02. Later deltas do not change those implementation boundaries; they are not rerun claims.

Available implementation checks are now green. Live parallel/reuse and image/file delivery still require the pending isolated credential choice. Native macOS lifecycle scope still requires the pending owner decision. Two advisory review rounds addressed their findings; the normal completion review has not run and remains the third available paired round. Both proposals stay Accepted. All changes are uncommitted, unstaged and within the combined exact file fences; no stash, push or promotion was created. The current live site remains on its unchanged P023 release. Persistent ignored evidence and the execution checkpoint retain resumption details.

## Promotion preparation and recovery limits

The chronological checkpoints above precede the owner's answers and the successful live checks below. Their unavailable-gate statements are historical, not the current release decision.

A bounded advisory audit of the unexecuted promotion helpers found two defects: root-run backup copies lost service/database UID/GID ownership, and regular-file manifest hashes did not authenticate internal package symlink resolution. Main corrected both. The helper now preserves and checks ownership/modes for files, directories and symlinks, and staging/promotion verify a reviewed complete package inventory including paths, content, executable permissions and raw/resolved symlink targets before candidate code executes. A read-only recheck found both fixes present. This advisory audit is not one of the mandatory outcome reviews.

Local synthetic tests rejected changed symlink targets and executable modes and matched the package verifier's inventory digest. An actual root-run Linux synthetic test preserved UID/GID 995 and 999, private file/directory modes and symlink ownership; it removed only its two owned fixture directories. Receipts are `.test-runs/p018/candidate/audit-fix-local.json` and `audit-fix-linux.json`. These tests exercised helper behavior without accessing live state or promoting a release.

The prepared backup is a same-host checkpoint, not verified fresh-host restoration. Copies do not preserve inode/process identities and intentionally omit special files; registered project contents are outside the backup. Restoration must retire runtime writers, assess work accepted and project edits made since the checkpoint, preserve or revalidate registered project identities, and restore the matched database/native/config state before starting old binaries. Attachment filesystem identities may require explicit reconciliation. Do not start an older binary against a migrated database or describe these preparations as the separate managed restore gate.

## Lifecycle dependency correction

The actual pinned runtime starts ordinary non-PTY commands in separate process sessions. PTY closure can mask the same inspection gap. Generated experimental background-terminal APIs exist in 0.153.4, but their thread-specific inventory omits standalone command execution; an empty response is not complete native ownership evidence. macOS process group, session, native SIGTERM and process-tree polling cannot satisfy the selected complete-retirement contract. Its release scope is pending the owner's choice.

Linux implementation now uses fixed per-generation cgroup-v2 leaves in a delegated supervisor service, with before-start ownership persistence, complete member inspection, `cgroup.kill` and confirmed empty state before durable release. The API remains nondelegated. Missing/substituted recovered paths stay unknown; recovered identities never authorize a kill. Private module checks and application acceptance remain pending.

Main ran a disposable nonroot systemd delegation prerequisite on the actual VPS (kernel `6.8.0-124-generic`, UID 995). An ordinary forked orphan in an independent session remained in its owned cgroup; cgroup kill reached confirmed empty state and the owned leaf was removed. Exit 0; script SHA256 `0218f67830b995961c95a31bed178a5dd3d1e26812aca50081b9addca26573bb`, log SHA256 `b8faea8a5588b00fb75f65985bd4a80ab143761b485060c124ec96339139ac29`. Evidence: `.test-runs/p018/candidate/probe-delegation.py` and the retained remote prerequisite folder. This proves host support, not the Harbor implementation or native lifecycle gate. No installed Harbor configuration/services were changed.

## Owner decisions and passing live acceptance

The owner subsequently authorized standing use of the VPS Codex login and confirmed that VPS support is sufficient. `AGENTS.md` retains that authorization and the selected deployment target. D014/current designs now exclude native macOS lifecycle parity from this release. The [native macOS record](../../issues/archive/2026-09-21-121000-native-macos-runtime-ownership.md) is archived as Not planned, not technically resolved. No macOS retirement success is inferred from Linux evidence.

On unchanged snapshot 08 source `19b7886b0f218cf6b29223023e034528a22365d5b6c9a5ff591e2d9fa810b862`, `node --import tsx tests/personal-vps/concurrency-live.ts` passed four actual model turns, 04:41:47–04:42:16 UTC, exit 0. The run used the complete Codex 0.153.4 native binary SHA256 `56ef98ab4032d317ab26e9b5e5a175650717351edb16ed9cde0cb6d1734d62da`, model `gpt-6-astra` at low reasoning, a delegated nonroot Linux systemd unit, a fresh synthetic workspace and a credential-only private home. It verified overlapping native turns, exact synthetic file contents, retained-thread reuse, fresh-runtime thread resume/history, sibling survival and confirmed retirement. Harbor application admission remains independently established by the real-stack lane.

The separate live attachment driver passed one actual model turn on the same source/runtime and model, 04:43:04–04:43:38 UTC, exit 0: synthetic image color identification and exact opaque file contents. The [P024 record](2026-09-21-p024-attachments-and-composer.md#passing-live-delivery) details its boundary. Both fixed wrappers verified source identity before/after, original credential bytes unchanged, no remaining owned unit/cgroup, and removal of the copied credentials and complete one-time test state. No ordinary account histories, configuration or projects were imported.

Local sanitized receipts are `.test-runs/p018/live-final/{concurrency,attachments}-receipt.json`; original private logs and receipts remain under `/var/lib/harbor-dev-20260914/p018-p024-snapshot08/live-{concurrency,attachments}-01/`. Log SHA256 values are `fd9b462d0b2590cc646c2ff8827b01ef1140645139cd7416308b128ab74c4a55` and `0685720507b4ce16e451071d2c984c61d48b33fbd5253ebbe121b32ef5f157e7`, respectively. Wrapper source is retained at `.test-runs/p018/candidate/run-authorized-live.py`. These results close the selected personal VPS P018-09/P024-04 account gates; they do not close inherited managed-profile acceptance. The scoped combined gate is green and normal completion review is next. No commit or promotion is claimed at this checkpoint.

## Final normal review

The third and final paired round was a normal completion review after the full selected VPS gate passed. Fresh design and provenance reviewers independently checked snapshot 09 (full manifest `3b60c95d2f81b14dde83835d55225ddc151529f18752252a1e0f03d7ccd48225`, 1,282 files; unchanged application digest `19b7886b0f218cf6b29223023e034528a22365d5b6c9a5ff591e2d9fa810b862`). Neither found an implementation or evidence blocker. Both identified stale wording that did not separate passed personal VPS live checks from older open managed obligations. Main corrected six current design/user/developer documents; both reviewers verified the exact documentation-only diff (SHA256 `4850ef3e12e0bb82784e3065c4273d75d445e7f45318658252986f0c85d04638`) and reported no remaining findings. Documentation links/whitespace passed for 168 files. No application files changed during this round.

There were three paired rounds total: two advisory rounds and this final normal review, with accepted fixes verified within the limit. Reviewer messages and retained receipts are evidence; complete session exports remain unavailable. `.test-runs/p018/review/round-03-dispositions.json` records closure. The implementation is ready for the already authorized commit, push and immutable packaging/promotion. Proposals remain Accepted until actual delivery evidence is recorded; this section does not claim deployment.

## Committed release and VPS delivery

On 21 September 2026 main committed and pushed `817c3d0971665871b64aa29c0fb25e8c8b132f02` (`Implement VPS conversation concurrency and attachment composer`). The clean committed snapshot contains 1,282 files, full manifest SHA256 `f7f166e02d499f4bfecb371ab0a504e9bb18aec9714f303804596b6b5b8d9e43`, with unchanged 1,112-file application/test/config digest `19b7886b0f218cf6b29223023e034528a22365d5b6c9a5ff591e2d9fa810b862`. A fresh frozen installation and `pnpm build` passed on the VPS. The earlier check/test/critical/design/personal/native/Linux/live receipts apply only to the unchanged boundaries identified above; this is not a claim to rerun all suites on the commit.

The nonroot fixed packager completed at 05:04:06 UTC. `verify-candidate-inputs.py` compared all copied source/build/dependency bytes, executable bits and links; its complete release inventory also binds directory/file modes and raw/resolved symlinks. The packaged Sharp 0.35.4/libvips 8.18.6 encoded and decoded a synthetic PNG successfully. Release identities:

| Item | Verified identity |
| --- | --- |
| Installed release | `/opt/harbor-personal/releases/p018-p024-817c3d097166` |
| Artifact manifest | `f009fb788dc60ee2bc56f7be2b1fa42ce0bfb2d8df9d72ec085e17a7cfc3a8bc` |
| Complete package inventory | `326cddae7c5580a340a9e5901d69397639aac4906933c1e5d12fd6a9304359ef` |
| Archive SHA256 | `206385e54c7aea63c1541b2a0a9acf6385047ed30c407496c96ff63c410375d4` |
| Archive size / regular files | 253,214,127 bytes / 14,302 files |
| Reviewed promotion policy | `860870366bc4ae44c67c678ba3203d1eef2aa4e0c7c9cf119fb1e326c32a4a25` |

Read-only policy construction initially rejected an incorrect assumption that the predecessor had 18 migration files. Its authenticated manifest has 16, with historical numbering gaps at 012 and 015. Main corrected the fixed deployment helper to require the exact predecessor number set, retaining every old migration hash, the exact 019/020 additions and applied-database-map equality. This failed preflight made no live change. The original helper and correction receipt remain under `.test-runs/p018/candidate/`; final promotion helper SHA256 is `448682ee9d554f789a425c41e5f6039f54cbf3ce75748355201bd103f3c74e6e`. All application and packaged source stayed unchanged.

`stage.py` and `promote-migration-map.py` exited zero. Main reviewed the exact 203-file predecessor/candidate delta; unchanged dependency code differs only in generated pnpm shims/metadata outside the added Sharp/libvips/colour/detect-libc packages. Native Node/Codex/code-mode-host bytes remain unchanged. Promotion stopped API admission, drained all operations/writers/releases/background reservations to zero, stopped the old supervisor through systemd's control-group fence and confirmed its cgroup empty. The matched private checkpoint is `/var/lib/harbor-personal-backups/p018-p024-817c3d097166-20260921T050800Z`. Its PostgreSQL custom dump passed `pg_restore --list`; SHA256 `ddb7f5a1bedabea4e36a7452fe61df1ab4e2c25cc83d83ca6c8fe58938f94db7` was independently rechecked. Backup copies preserve UID/GID/mode and retain database, native/home/control state, configuration, units/drop-ins and AppArmor profile. No project files or special process state are included.

The new services initialized at approximately 05:08 UTC. Installed verification passed at 05:09–05:13 UTC: exact release/native process identity, no old-release processes, all 18 applied migration receipts matching the artifact including 019/020, stable API/supervisor PIDs, supervisor delegation enabled and API delegation disabled, and both services retaining `ProtectHome=read-only`. The entire site configuration is unchanged except its release path. Both project-write drop-ins retain SHA256 `f4cc28a6d4a95d94026bd3e35ae7325bb1856384127de9488339abb2a205871d`. The configured preview route is preserved and refuses anonymous access with 403. Certificate-verified HTTPS returned 200 for `/health` and 401 for anonymous application/API/assets. Supervisor generation advanced from 11 to 12, fresh native capability discovery confirmed the existing account authenticated and models available, and active/uncertain operation counts were zero.

A separate temporary browser tab followed the documented sign-in route using existing authentication. Read-only inspection of an existing conversation confirmed the enabled attachment button, multiple-file picker, concise Archive/Status labels and a chat scroll surface containing the composer, from y=57 below the header to y=752 at the viewport bottom. No prompt, attachment or project edit was submitted; the tab was closed. This installed observation supplements the isolated desktop/mobile application gates and does not replace their interaction coverage. Private conversation contents/screenshots were not copied into evidence.

Original candidate package/logs/policy/helpers remain under `/var/lib/harbor-dev-20260914/p018-p024-817c3d0971665871b64aa29c0fb25e8c8b132f02`. Local receipts are `.test-runs/p018/committed-candidate/{linux-install-01,linux-build-01,package-receipt,stage-receipt,policy-review-receipt,promotion-receipt,installed-verification,installed-before,installed-after,post-promotion-comparison,final-release-evidence,browser-installed}.json`. These identify exact commands, inputs and observed outputs; browser observations derive from tool responses, not a complete raw session export. Final lifecycle/link changes are documentation-only and do not alter the deployed application digest.

P018-01–07 are satisfied by the 12-group personal application lane plus the critical/integration/native boundaries above; P018-08 by settled desktop/mobile application/prototype observations; P018-09 by actual delegated Linux retirement contracts and the four-turn live parallel/reuse/resume check. P024 has its separate [acceptance mapping](2026-09-21-p024-attachments-and-composer.md#committed-release-and-vps-delivery). Both proposals are Implemented and archived as Completed after three paired rounds with no remaining scoped findings. The owner-selected release target is VPS only. Older managed live/restore issues remain open.

This same-host checkpoint is not off-host backup or fresh-host restore acceptance. Downgrade requires a deliberate matched database/native/config restore or forward repair, assessment of work accepted since the backup, and reconciliation of recorded filesystem identities; copies do not preserve inodes or live process identities. Never start the old release against the upgraded database. SSH recovery remains available and the predecessor immutable release is retained.

The final documentation-only closure passed `node scripts/check-docs.mjs` (168 files), `git diff --check`, exact file-fence inspection and a fresh source-digest comparison. Proposal IDs/filenames are preserved in the archive and inbound/outbound links repaired. No additional application change or fourth review round was introduced.
