# P006 terminal implementation checkpoint — 8 September 2026

P006 is in progress. The terminal UI, authenticated HTTP/WebSocket facade, durable input/output and controller state, supervisor ownership, dedicated native PTY and confined Linux launcher purpose are implemented in the isolated P006 branch. Two independent implementation review rounds and the branch terminal, workspace and critical acceptance checks are complete. Cumulative P004/P009 integration remains open. This report does not mark the proposal Verified or authorize a release.

## Source and environment

The original round-1 review source was `95cf523b9ef700d5ba3183c931dd3f41f2cd6ea0eb3e10e83918c6dc8a09c7af`, 873 files under the repository's `sha256-path-and-content-v1` fingerprint. It is uncommitted on `codex/p006-terminal`, based on `cb6be0d`; later root documentation/index changes are not integrated. The host checks below select Node 24.11.1 explicitly, pnpm 12.3.4 and pinned Codex 0.153.4. No personal account or project is used.

The native Linux checkpoint is separately pinned to `c46d8111082cd8bed536f3d82a0b628c56001a698570444ee54b7fd9bef2d5a1`, 872 files, on Ubuntu 24.04 arm64, Linux 6.8.0-134, Node 24.11.1 and Docker 29.1.3. Its runner image is `sha256:65cd9c5659eb3b40e2f96276726a1777b91540c339657a434e5827640792f932`. It is not the aggregate source of the later host tests. Comparing all 817 production files from the transferred archive to the review source found only two differences: exact Prettier formatting of `apps/web/src/Terminals.tsx` and `apps/web/src/styles.css`. The API, supervisor, storage, adapter and native launcher bytes are identical; the later test deltas include host fault coverage and external-fixture cleanup.

## Executed checks

| Command/check | Result and scope |
| --- | --- |
| `pnpm check` | Passed TypeScript, 58-file documentation links/whitespace, public OpenAPI schema validation and maintained formatting and the final frozen-source check. The later report addition also passed the 59-file documentation check. |
| `pnpm build` | Passed TypeScript and Vite build on Node 24.11.1. |
| `pnpm test` | 11/11 integration tests passed. The first restricted attempt failed only because the sandbox denied the test-owned Unix socket; the authorized retry passed. |
| `pnpm test:contract` | 18/18 passed, including pinned generated schemas, prior conversation/attachment/recovery contracts, the real no-account PTY with resize and a 16-second command, and the exact terminal-only seccomp derivative. |
| `pnpm test:e2e --terminals` | Passed, `.test-runs/harbor-e2e-343367f888/result.json`, exact review source at start/end, Node 24.11.1. Real browser/API/PostgreSQL/supervisor; only external OIDC and Codex transport are fixtures. The Codex fixture wraps a real private PTY/shell. |
| `pnpm test:isolation --terminals` | Passed, `.test-runs/p006-first-supported-linux.json` and matching log, exact Linux source at start/end, exit 0. Real root storage broker, XFS quota unit, Docker runner and pinned Codex PTY; external owner identity fixture, no model account. |
| `pnpm test:e2e` critical regressions | First attempt failed at unchanged `tests/e2e/p005.ts:725`: after a deliberately paused attachment metadata request and reload, the selected-attachment notice did not appear within five seconds. No root cause is established. The log is retained; the inherited harness did not capture a final failure result. The unchanged-source retry passed as `.test-runs/harbor-e2e-9bdf80be75/result.json`, matching the review digest at start/end. That pass does not explain the earlier failure. |

The passing terminal E2E covers owner creation without a model credential, real typed/colored output, strict CSP and exact xterm style nonce, desktop/mobile rendering, at least 260 pixels of visible mobile terminal canvas with termination fully visible, multiline paste preview/cancel/send, hostile OSC/link/HTML/title/clipboard output without browser side effects or automatic input, all browsers closed plus API restart while shell state/output survive, takeover and stale input, exact input retries/conflicts and old-outcome expiry, token scope/revocation, fresh expiry after a real database workspace-lock wait, real deferred COMMIT faults before settlement and retained repair, input row/byte/rate bounds, output count/byte/age pruning and visible replay gap, queued-terminal capacity, creation retry/conflict, output-limit retirement and lifetime expiry. Synthetic rows are used to reach finite storage limits; they are not described as hundreds of native shell commands.

The Linux lane proves read-only dirty Local preservation, writable derived Git checkout isolation and canonical aliases/common metadata, physical mount modes, UID 10001, read-only root, no capabilities, no new privileges, cgroups, absent host/admin mounts and credentials, IPv4/IPv6 egress denial with `network:none`, XFS byte quota and project-inheritance ioctl denial, explicit user/mount namespace and mount denial, shell exit while background work retains ownership, exact whole-runner termination, independent terminal ownership, actual Docker runtime kill, and supervisor SIGKILL with pending input followed by durable retirement/uncertainty without replay. All test-owned services/containers/quotas/directories were cleaned; zero containers remained running at VM handback. Shared VM baseline, SFTP and other feature artifacts were preserved.

## Failed checkpoints and correction evidence

Initial native `openpty` failed with EPERM. The accepted terminal-only profile adds exactly `TIOCSPTLCK`; the unchanged base continues denying filesystem write ioctls. The next native checkpoint preserved bubblewrap's namespace denial. The fixed private factory now selects the pinned external-sandbox command policy only after confined runner admission, with the physical profile and network boundary enforced by Docker/XFS. Both decisions and official references are in [P006](../../design/proposals/006-persistent-terminal.md#dedicated-terminal-runtime-and-writer-ownership). No namespace creation permission or host fallback was introduced.

Subsequent Linux failures were assertions using a nonexistent container session label and the database spelling of a public profile field; exact runner-name and public-profile assertions corrected those tests without weakening the confinement outcome. The final Linux lane passed the resulting checks.

Host checkpoints `db0b841755` and `1528a8099e` timed out waiting for output-flood retirement. The diagnostic showed durable `OUTPUT_LIMIT`, floor 67 and ownership retained in `retiring`. An isolated external-fixture reproduction identified its wait for the shell during undrained PTY teardown. Closing the fixture PTY master before exact child retirement resolved the final real-stack test. This is an external-fixture correction; production overload policy was unchanged. Earlier mobile/CSP/controller test corrections remain historical artifacts; they are not substituted for the final frozen result.

## Open acceptance and integration gates at the original checkpoint

- Independent implementation review has not yet completed; zero closed implementation review rounds at this checkpoint.
- The full critical suite has a matching-source pass alongside the unexplained earlier attachment assertion above. The combined P003 workspace-manager lane remains to run on this branch.
- Explicit adversarial evidence remains to be added for outbound slow-reader/backlog isolation, all viewer/global live/retained limits, exact snapshot-to-stream commit ordering and split UTF-8/ANSI boundaries, and long database-outage watchdog behavior. Existing bounds and native output tests do not silently close these distinct acceptance cases.
- Real managed conversation/terminal contention and the final P004/P007 shared writer integration need a combined branch test. P004 is not merged here. The generic reservation epoch is independent of native generation and the shared migration fragment has been coordinated with its owner.
- P009 maintenance/drain/backup/restore registry integration is not merged here. A saved database/output tail cannot restore a live PTY. Portable release/restore gates remain owned by P009.

P006's tested terminal path needs no model account. These no-account tests neither require a fabricated credential nor establish a real authenticated model turn. Existing proposal live-account gates remain separate and unchanged.

Current documentation: [terminal user guide](../user/terminals.md), [developer workflow](../developer/development.md#terminal-development), [canonical proposal](../../design/proposals/006-persistent-terminal.md).


## Round-1 corrections and round-2 verification checkpoint

This addendum preserves the original results above. The corrective candidate is `89f4f59db58c259ed80e46e63e4eaefa87b861acb5c1dfd8a666c7582d28e688`, 876 files. It remains uncommitted and in progress. The independent reviewer closed round 2 after recomputing this exact source and inspecting the complete terminal result. No actionable finding remains in the reviewed terminal scope; downstream integration remains open.

The original findings were a reconciliation button that could newly admit missing input, an emergency-stop race before terminal wire delivery, and unreported possible output truncation after supervisor death. The outcome query now only reads the exact retained mailbox identity and returns missing/expired without inserting input. Final wire delivery holds the instance/emergency row through synchronous send in the shared authority order. Unclean supervisor startup conservatively persists possible truncation and renders it even without new chunks. A real failed-retirement test also found that the next tick could retry automatically; uncertain retirement now retains its writer and waits for one of the finite deliberate retries.

Expanded acceptance exposed a separate PostgreSQL deadlock while pruning output. Sanitized evidence in `harbor-e2e-03f210564c`, `db811f460c` and `2cee072467` records the project/terminal/floor-update cycle without SQL values or terminal bytes. A flush first updated the terminal sequence, then updated its retention floor. PostgreSQL 17.6 can recheck unchanged foreign keys on a row version created by the same transaction, so the second update could request parent locks while holding the terminal. A viewer already held those parents and waited for the terminal. The [pinned PostgreSQL implementation](https://github.com/postgres/postgres/blob/REL_17_6/src/backend/utils/adt/ri_triggers.c#L1240) supports that mechanism. Output flush and retention maintenance now share the project → workspace → terminal resource order. Admission and final delivery retain authority checks around that resource locking; single-statement startup/close/failure updates hold no conflicting parent locks.

`pnpm test:e2e --terminals --terminal-review-fixes --terminal-stream-check` passed as `harbor-e2e-c50554982c` on Node 24.11.1, exact source `4ea7ddc8327af98343b8103977ee2a7e72096d80017003f3c1bab2fdcc22f7e6`, 876 files. During actual output retention, the test holds the project row, observes the supervisor waiting at that parent and acquires the terminal with NOWAIT. After release, the fast viewer receives the complete marker while a real nonreading WebSocket peer is detached. A global assertion rejects any `40P01`, including on the deliberately slow viewer. The later candidate differs only in `tests/terminals/expiry.ts`: it now holds the exact accepted input row and observes the final dispatch UPDATE after authority/resource checks, then expires the token before release. The previous full-run checkpoint `569e8e2f07` failed because its old workspace-query assertion no longer identified that boundary; it did not establish a production expiry failure.

On the `4ea7ddc…` production source, Node 24 `pnpm check`, `pnpm build`, 11/11 integration tests and 18/18 contracts passed, including the pinned real no-account PTY smoke. Complete `pnpm test:e2e --terminals` acceptance passed as `harbor-e2e-2ecaf4ca80`, exit 0, Node 24.11.1, exact final candidate at start/end. The stronger final input-dispatch expiry assertion and all corrective cases ran in that complete flow. Final candidate `pnpm check` also passed after the test and report updates. The workspace-manager regression also passed as `harbor-workspaces-c1f16e0a43`, exact candidate start/end, exit 0.

The additional real application checks cover before-acceptance and after-acceptance dropped input acknowledgements without new reconciliation wire, final emergency-stop ordering with independent browser/SQL gates, exact snapshot-to-subscription output once, split UTF-8/ANSI bytes, slow-reader isolation, per-terminal/actor/instance viewer limits, wrong-project/profile/Origin denial, finite retained/live admission limits, actual failed retirement and deliberate retry, and a held database output transaction long enough for the independent watchdog to retire the native fixture before supervisor SIGKILL. Synthetic rows reach the finite count ceilings; they are not claimed as hundreds of running shells.

Early diagnostic corrective runs that passed did not reject a deadlock on the slow viewer and therefore do not close that finding. The passing full-flow diagnostic `fd59ba9b20` and other intermediate results remain distinct from the frozen corrected evidence. The root-reviewed P005 harness pacing from commit `31d20e3` is now imported for critical regression execution; it removes automatic mutation retry, preserves production limits and adds deliberate test phase boundaries. It does not retrospectively explain the original uninstrumented attachment failure.

The native Linux checkpoint remains `c46d811…` above. The corrective batch changes SQL, API/UI and tests, not the native adapter, launcher, seccomp, mounts or runner image; it does not claim a new matching aggregate Linux run. P004 shared writer/CSP and P009 registry/drain/restore integration remain open. P009 must register terminal metadata/input/output and exact authority ledgers, drain the entire dedicated runner, and revoke restored controllers and pending input without PTY replay. No off-host backup, restored live shell or real model account is claimed.


The critical regression checkpoint `harbor-e2e-78be409ad3` passed P005/P007 attachment phases but failed the unchanged P002 rate assertion: 205 sequential reads did not observe a 429. Its log preserves that outcome; no elapsed/status histogram was captured, so a window split or throughput explanation remains source inference rather than a proven historical cause. The limiter still uses a fixed 10-second IP window and 200 requests; no production policy changed.

The final candidate `1e636388d92f578a8b905f9b44332a264d72f2d157c6706f3b104114ac6845d1`, 876 files, differs from the complete terminal/workspace checkpoint only in `tests/e2e/p002.ts`. Its bounded read-only burst uses eight concurrent requests, at most 405 total and remaining-time deadlines within nine seconds, stops after observing a rejection, and requires both successful authenticated reads and a retryable 429 within ten seconds. The finite budget exceeds two possible fixed-window capacities even when a boundary intervenes. It records only status counts and elapsed time, never credentials or content, and does not retry any request or mutation. Independent review of this test-only correction closed with no actionable finding. Its complete critical rerun is pending at this note; terminal/native production remains identical to the already reviewed candidate.


Critical run `harbor-e2e-82771d0087` passed that P002 burst (195 successful reads and five 429s in 375 ms), then failed P007's credential-restoration `finally` assertion with 409 instead of 200. The inherited `finally` could hide an earlier quota assertion, and no error code/state-count diagnostic was available. It is not attributed to terminal production code. The first added diagnostic run, `4536fcf036`, contained an undeclared artifact-path variable and failed while writing diagnostics; that was a test-edit mistake and establishes no P007 cause. The reference and cleanup-exception preservation were corrected, and full `pnpm check` passed before the next instrumented execution. That execution records sanitized original assertion metadata separately from credential cleanup error/state counts, retains the original error if cleanup also fails, and introduces no mutation retry. Its result remains pending here.


## Final reviewed branch handoff

The final critical suite passed with exit 0 as `harbor-e2e-211efe006b`, Node 24.11.1, Codex 0.153.4 and Chromium 1194. Its start and end fingerprints both equal `1f83957bf23aa55479a6a05679ff9e6bdf8fb03f5437b97d2ad120e13dfc1204`, 876 files. The command was `pnpm test:e2e` with Node 24 selected explicitly. Full `pnpm check` passed before this execution. Relative to the independently reviewed complete terminal/workspace checkpoint `89f4f59…`, only `tests/e2e/p002.ts` and `tests/e2e/p007.ts` changed; no terminal production, native confinement or workspace code changed. The earlier matching terminal/workspace, build, integration, contract and separate Linux evidence retain their exact identities above.

The final rate artifact records 195 HTTP 200 responses and five retryable 429 responses in 372 ms, with 200 total reads. The P007 cleanup artifact records HTTP 200, no error code, no active operations and `originalFailure:false`. No quota assertion failed in this run. The independently reviewed diagnostics preserve an original assertion when cleanup also fails and make a recurrence observable; this successful run does not establish the cause of the historical P007 409. That uncaptured observation remains an open Medium follow-up, with the next action to inspect the bounded original-error and cleanup/state diagnostics on recurrence. No automatic mutation retry was added.

Two terminal implementation review rounds are closed with no actionable finding in the tested branch scope. The additional bounded P002 rate and P007 diagnostic changes also received independent review. The feature is handed off from base `cb6be0d` for cumulative integration; this is not a Verified proposal or an installed-release claim. P004 generic writer/CSP integration and P009 installed service/image/registry, exact runner drain, administrator runtime recovery and restored controller/input revocation still require implementation and combined acceptance. Restore must preserve terminal history and uncertainty without replaying a PTY or pending input. The canonical review, rate and historical P007 issue records remain in the root worktree and will be linked with that integration rather than duplicated here.


## Main integration tracking — 8 September 2026

The reviewed branch is committed as `f033824b495c1b42fe2c6fd9e9d94bac8db8db3f`. Root integration is in progress on the reviewed P004/P009 baseline. Its canonical [P006 implementation issue](../../issues/2026-09-08-005028-p006-implementation-review.md) remains active for shared writer, installed module/service/image, drain and restored-authority work. The [P002 rate assertion](../../issues/archive/2026-09-08-015752-p002-rate-limit-acceptance.md) is resolved with the final critical evidence above. The [historical P007 cleanup observation](../../issues/2026-09-08-020429-p007-regression-evidence.md) stays Open; diagnostic delivery and a passing run do not establish its old cause.
