# P004 file and change review candidate evidence

Date: 8 September 2026. This report records P004 feature implementation and two independent review rounds on the sources below. Subsequent [installed module evidence](2026-09-08-installed-module-integration.md#final-installed-outcome-and-review-closure) closes scoped P006/P009 integration. P004 is Implemented, with mandatory protected fresh-host restore and inherited verification gates still open. The [proposal](../../design/proposals/archive/004-files-and-change-review.md) owns the complete acceptance contract; the [file guide](../developer/files.md) describes available behavior. This evidence does not mark the proposal Verified.

## Candidate identity and environment

The frozen round-1 candidate is the `codex/p004-files` worktree at baseline `fee16e7` plus its uncommitted feature. It imports only the reviewed P005 harness file from `31d20e3`. Source digest `f5998b404925e222841e4f1167824cb246bab8e6c6244d5714a01fdf8edff079`, 881 files, uses the repository's `sha256-path-and-content-v1` inventory. Documentation/evidence changes are excluded; source, configuration, lockfile and test changes are included.

The final application checks use Node 24.11.1, pnpm 12.3.4, PostgreSQL 17.6, Chromium 1194 and the pinned Codex 0.153.4 contract. Managed file acceptance runs in the disposable Linux VM with Docker 29.1.3, Compose 2.40.3 and project-quota XFS. It creates fresh database, HTTPS, authentication, storage, quota-root and helper identities. OIDC and Codex are external deterministic fixtures; Harbor's browser, API, database, supervisor, root storage service and file helper are real. No personal projects, credentials or Codex home are inputs.

## Results at the frozen candidate

- `pnpm check` and `pnpm build`: pass on Node 24. The check includes TypeScript, public-schema compilation, documentation links/whitespace and maintained-source formatting.
- `pnpm test:e2e`: pass, `harbor-e2e-b180a991d0/result.json`, exact start/end candidate digest. This runs the critical P001/P002/P003/P005/P007 fixture regression flow, including the corrected P005 rate-window pacing without retrying failed user mutations.
- `pnpm test:isolation --files`: pass, `harbor-files-627c302c9b/result.json`, exact start/end candidate digest. Evidence is copied locally beneath `.test-runs/p004-linux-evidence/`.
- `pnpm test:workspaces`: 8/8 pass. `pnpm test:e2e --workspaces` also passes: `harbor-workspaces-97f07897b1/result.json`, matching the candidate start/end digest.
- `pnpm test`: 11/11 pass; `pnpm test:contract`: 16/16 pass, including the pinned real-runtime smoke. These ran before the final bounded watcher-only delta; that delta is covered by final type/schema checks and the real application/SSE lane. Adapter/native-launch source did not change between these executions.

The complete file lane asserts disk bytes/revisions and downloads, a second managed-client conflict with draft preservation, graceful API restart, explicit revision refresh/reapply, exact UI-selected text hunk staging and reviewed commit identity, and repeated-key reconciliation. The native helper checks separately cover Unicode/no-final-newline hunks, unstage preserving worktree bytes, rename/mode/deletion whole-file semantics, and rejection before index publication when the prepared receipt is not acknowledged.

A real deferred PostgreSQL constraint rejects result persistence after the helper's committed Git effect. Killing/restarting the owned supervisor leaves the original operation uncertain, fails queued dependents, and never repeats the commit. The reserved inspection and acknowledgement flow still works at ordinary-operation capacity, returns the original completed receipt/OID and releases only the captured typed reservation. Original uncertainty remains recorded.

Bounded read coverage includes 205-entry tree/search pagination, binary/oversized/hardlink results, schema validation, cookie-free token scopes/project/profile denial, queued grant revocation and expiry with no helper epoch/effect, revoked SSE closure, and archived inspection with write denial. The final watcher test modifies an open nested file, creates/deletes a directory entry, creates a gap while the stream is live, and requires resync followed by further live invalidation.

Actual helper-container probes assert UID 10001, read-only root/inspection mount, exact selected mounts, dropped capabilities, no host socket/control secrets and denied IPv4/IPv6 network access. These establish the Linux boundary; the earlier macOS rendering check is not isolation evidence.

## Corrections before independent review

Development found and corrected a malformed CSS block that hid Monaco, missing file-stream shutdown during graceful API restart, asynchronous stale-revision refresh, clipped hunk controls, and an unenforced complete staged-review UI gate. The final combined run also exposed excessive full-workspace inspection on each editor refresh: helper capacity exhaustion could mark a workspace unavailable. The file tree now supplies authorized bounded lease metadata and UI refreshes coalesce; the complete managed run passes without read/mutation retries or weakened quotas.

The full-lane screenshots were captured before the asynchronous file selection finished. They are retained as intermediate states, not claimed editor screenshots. The same-source render-only path passes as `harbor-files-ae1d2539bc`, waits for Monaco and escaped hostile text, and supplies the final desktop/mobile visual evidence separately. Both screenshots were visually inspected: the editor and controls are visible, hostile HTML remains text, and the mobile layout has no horizontal overflow.

## Remaining gates and integration ownership

Owned Linux cleanup completed; `docker ps` shows no running containers after the final lanes. The separate SFTP resources and other feature evidence were preserved.

Independent implementation review completed in two rounds, with the final correction evidence below. This is not Verified status or downstream integration evidence. Live Codex/model credentials remain an inherited separate gate; P004 does not change the adapter or claim model execution evidence.

P009 integration must explicitly register `file_operations`, `file_inspections`, `file_operation_audits`, `file_events`, `file-receipts`, `file-slots.json` and `file-slots.lock`; honor typed file reservations and queued/dispatching/inspection drain; and preserve completed/uncertain evidence while revoking source-host dispatch and receipt replay on restore. The candidate exports this inventory in `packages/files/src/module.ts`; it does not claim unmerged P009 backup/restore behavior. P006 integration must preserve the independent reservation epoch/native generation distinction and the shared authenticated-document style nonce without weakening CSP. These downstream integrations require their own combined checks.

## Independent review round 1 correction batch

The separate reviewer found one High stale-completion draft-loss race and three Medium BOM, mode and receipt-parent durability defects. The batch at source `324022da4ddc36128348a01427cba6028a2bd44bb176848f9b47a34d56d37fa2` (882 files) checks the live draft revision before replacing an open file, reads Monaco models with BOM preservation, applies the inspected safe rwx mode with `fchmod` before publication, and syncs the receipt authority parent before any effect.

A targeted Linux test injects rejection into the actual parent-directory `FileHandle.sync` and asserts no receipt or changed file, then verifies a successful save retains mode 0666. The real browser regression holds file B's response, edits file A, releases B and asserts A's draft survives; a separate edit/save checks exact BOM/CRLF bytes and mode 0775. The complete managed-XFS application passes as `harbor-files-a475c452df`; the critical Node 24 regression passes as `harbor-e2e-76c87b1fa9`. Both record exact start/end `324022da…d37fa2` / 882 files. Node 24 check/build pass. The independent round-2 recheck closed all four findings with no additional actionable finding in that bounded scope. The parent-fsync regression injects rejection into the real FileHandle method; it is not a power-loss test. The earlier frozen-candidate results above remain historical evidence rather than being silently attributed to this newer batch.

## Main-branch integration checkpoint — 8 September 2026

The reviewed feature was committed as `9a82f072c3b77740cbbbfb923f400b868eed88ab` and integrated onto root `fe05647`, which includes the reviewed P009 baseline. Combined source is `4494463f4b3da0ad0a281768ab368b2dc6e86782fbae549dab3c7eee037c6f9b`, 921 files. Three overlapping locations preserve both API imports, both storage service imports, and the restored-installation activation guard before file dispatch. The feature author independently inspected those merge resolutions and found no actionable conflict-resolution defect.

Node 24.11.1 `pnpm check` passed on this combined source, including 71 documentation files. The P004 native helper and Linux evidence remain their exact historical checkpoints above. Combined terminal/application and deployment module/drain/restored-authority work is still pending, so this addendum makes no new cumulative E2E, Linux or restore claim. The overall proposal remains In progress while those required behaviors are missing.

## Navigation and ownership addendum — 20 September 2026

P017 reconciled legacy planning records into the [proposal archive](../../design/proposals/archive/). [Current subsystem designs](../../design/systems/) now own requirements, and unfinished acceptance belongs to the [issue inbox](../../issues/) until a new proposal is selected. Historical statements here about active proposal states, queues, permissions and recovery locations describe the recorded date. This addendum changes navigation/ownership only: original tested sources, commands, results, review rounds and limitations above remain historical evidence. No application gate was rerun for the documentation migration. Historical raw artifacts or worktrees not present in this checkout were not newly verified.
