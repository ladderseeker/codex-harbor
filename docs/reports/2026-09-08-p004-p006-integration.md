# File, terminal and deployment integration

Recorded 8 September 2026. This is an integration checkpoint, not completed P004/P006/P009 acceptance. The feature reports preserve [file evidence](2026-09-08-p004-files.md), [terminal evidence](2026-09-08-p006-development.md), and [deployment evidence](2026-09-08-p009-development.md) on their respective tested sources.

## Combined source and merge review

The baseline combines P004 author `9a82f072c3b77740cbbbfb923f400b868eed88ab`, P006 `f033824b495c1b42fe2c6fd9e9d94bac8db8db3f` and P009 follow-up `2dc2ecff33e0b99311c022e9f0c8c9082a213397`. The final corrected merge fingerprint is `f0a4cdf2aea0738bc790f9bff6eb539ffc0b4ccbdfd7dd83410464d9c8cced99`, 951 files.

One authenticated document supplies the style nonce to both self-hosted Monaco and xterm assets. Both renderer plugins and UI entry points remain present. The combined token scope vocabulary and schema limits include both features. Generic writer epochs stay independent from native runtime generations. Terminal runners retain no egress and their narrow PTY seccomp derivative; conversation launch retains deployment's immutable-image selection and explicit missing-image rejection.

A separate agent reviewed the merge and found one High interaction: terminal admission did not join the project-wide P004 Git effect fence. The correction checks dispatching and unacknowledged-uncertain stage/unstage/commit work under the existing project lock before claiming a terminal in another derived worktree. File admission already checks all project writer owners for the reverse ordering. The reviewer inspected the correction and matching evidence and closed the bounded merge review without another actionable finding. Each feature's two earlier implementation rounds remain separate historical reviews.

## Verification at this checkpoint

Node 24.11.1 frozen dependency installation, `pnpm check` and `pnpm build` passed. Focused `pnpm test:e2e --terminals --terminal-git-fence` passed as `harbor-e2e-5e4cbbc6b2`, with identical start/end fingerprint above. It creates real derived Git checkouts with the same common-directory identity and exercises Harbor UI/API/PostgreSQL/supervisor plus a real fixture PTY. Explicit synthetic file lifecycle checkpoints cover all three Git kinds in dispatching and unacknowledged uncertainty; they are not described as physical helper execution. The opposite admission order is covered too.

The first focused test attempt used an incorrect HTTP 202 assertion for the existing HTTP 200 workspace-creation contract. That test assertion was corrected and check passed before the final execution. No production behavior changed to satisfy the mistaken expectation.

The earlier full critical and terminal/workspace results belong to the source identities in their feature reports. No new Linux claim is inferred from a successful host test or larger source digest.

### Cumulative verification, 8 September 2026

On committed revision `6f21e45`, explicit Node 24.11.1 ran `pnpm test` (11/11), `pnpm test:contract` (18/18), `pnpm test:e2e`, and `pnpm test:e2e --terminals`; all passed. The real pinned Codex 0.153.4 contract lane includes account-free PTY readiness, resize, delayed exit and exact terminal seccomp derivation. It makes no model request and does not replace the dedicated live-account gate.

The critical run `harbor-e2e-9a6fb5047a` covers the combined P001/P002/P003/P005/P007 regression suite. The terminal run `harbor-e2e-01d513e7fd` covers P006's complete application acceptance. Both record identical start/end source fingerprint `f0a4cdf2aea0738bc790f9bff6eb539ffc0b4ccbdfd7dd83410464d9c8cced99`, 951 files, with fresh real Harbor browser/API/PostgreSQL/supervisor components and fixtures only at the external OIDC/Codex boundary. Their result files and sanitized supporting artifacts remain under `.test-runs/<instance>/`. These runs establish the cumulative host application result; actual combined Linux/module acceptance was pending at this checkpoint; subsequent evidence is linked at the end.

## Integration obligations at this checkpoint

P009 must register file/terminal tables and durable authority paths, package the file helper and terminal profile, configure installed services, freeze new admission and account for accepted work during draining, retire exact runners/helpers, extend administrator recovery, and revoke restored dispatch/controller/input authority while preserving historical outcomes and uncertainty. Restore must not replay a helper, PTY or pending input or contact a source-host runtime. At this historical checkpoint these were missing required behaviors, owned by the active [P004 issue](../../issues/2026-09-08-012652-p004-implementation-review.md) and [P006 issue](../../issues/2026-09-08-005028-p006-implementation-review.md).

A later installed browser attempt exposed a separate [web-asset packaging omission](../../issues/archive/2026-09-08-031032-p009-installed-web-assets.md): artifact `75ea68ec…9ea29f` lacks the served web entry despite passing backend readiness. The failed artifact/source identity, reproduction and required corrected-package evidence remain in that issue, now resolved with separately recorded installed evidence. This does not change the earlier host application passes or turn backend readiness into an installed UI result.

After the web entry was included, installed project creation reached the UI but file inspection exposed a [broken workspace quota import](../../issues/archive/2026-09-08-031857-workspace-quota-import.md). The resolved record distinguishes the actual installed failure from main's separate compile-only reproduction and owns the required Linux correction evidence.

Actual protected transfer/restore remains subject to its [separate blocker](../../issues/2026-09-07-231526-p009-backup-transfer-approval.md). Local metadata/rebinding tests cannot close that gate. The [historical P007 cleanup observation](../../issues/2026-09-08-020429-p007-regression-evidence.md) stays Open despite a later passing instrumented regression; its old cause is not established.

## Subsequent installed delivery

Root `04ab86b` includes the independently reviewed installed module implementation and final evidence. Ordinary and deliberate-response-loss browser lanes passed on immutable `8c11ca94…84f22`; the [final module report](2026-09-08-installed-module-integration.md#final-installed-outcome-and-review-closure) records exact production/test-driver identities, Linux/host checks, local metadata rebind and prior failed-attempt qualifications. P004/P006 are now Implemented. Their active review issues remain Blocked specifically for required protected fresh-host filesystem restore/authority evidence; earlier source implementation gaps described here are corrected.
