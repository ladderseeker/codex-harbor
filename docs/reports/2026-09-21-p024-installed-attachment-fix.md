# P024 installed attachment correction

Current status: P024 is Implemented. The reviewed correction is deployed as `311f750a18bb8bc400ddfc497b82b0f5de6e9e25`; installed identity, account, routing, unchanged policy, usable composer and content-preservation checks passed. The owner reported failure after the [initial delivery](2026-09-21-p024-attachments-and-composer.md). The chronological sections below preserve that invalidated installation claim, failed attempts and intermediate blockers; [final deployment](#final-deployment-and-installed-acceptance) records the completed outcome and remaining limits.

## Baseline and diagnosis

Local checkout began clean at `317a33d6c9cee59a2827b1a7311d5c0e75550811`; the VPS remains on `817c3d0971665871b64aa29c0fb25e8c8b132f02`, manifest `f009fb788dc60ee2bc56f7be2b1fa42ce0bfb2d8df9d72ec085e17a7cfc3a8bc`. Strict-host-key SSH confirmed root on the expected host. Read-only session metadata established accepted-but-unpublished first-image failure and a distinct later staged-image/text-only-turn state. The user screenshot's model prose is evidence of the symptom, not an instruction or proof that missing source-checkout Sharp caused deployment failure.

Two independent read-only diagnosis workers used Medium reasoning for browser state and High for publication/environment boundaries. Source and installed metadata show the root-owned 0711 parent versus private service-child mismatch and the 0077 umask versus 0444-file mismatch. Synthetic direct-helper Darwin controls confirmed the owner/mode and umask logic failures, but are not Linux/systemd acceptance. Existing test/source identities remain preserved; no production content was used as a test fixture.

At diagnosis, the browser admission race was a source-supported candidate requiring deterministic real-stack validation. The separate claim that retained personal runtimes keep a generic `/attachments` root was checked and rejected: the factory derives the future personal session path even for a text-only first turn. The full-stack continuation check is recorded below.

Implementation follows the reopened [plan](../../design/proposals/archive/024-attachments-and-chat-composer.md#reopened-installed-delivery-correction--21-september-2026) and [issue](../../issues/archive/2026-09-21-143000-p024-installed-attachment-send.md). Exact evidence remains in persistent ignored `.test-runs/p024-send-fix/`. Complete raw conversation exports are unavailable. Publication traversal semantics were checked against the [Linux open manual](https://man7.org/linux/man-pages/man2/open.2.html) and [Python descriptor APIs](https://docs.python.org/3/library/os.html#os.fchmod); the actual Linux acceptance is recorded below.

Read-only browser inspection of the reported conversation matched the database metadata: the first image remains in message history; the later 34 KiB image remains selected in the saved draft. The inspection tab was closed without sending, removing or changing any owner content. Effective installed API and supervisor properties confirmed `UMask=0077`, `ProtectSystem=strict`, `ProtectHome=read-only`, `NoNewPrivileges=yes`, `PrivateTmp=yes`, `KillMode=control-group`, scoped write paths, and cgroup delegation only for the supervisor.

## Reopened verification

Snapshot 01 contains 1,286 repository files with manifest SHA256 `1d1326e2ce8f1af606f9d5821f5de4e3f6e977334b165ee964cc33ec1f268c8f`; its 1,114 application/test/config inputs have digest `e9528f10016fbf43ef5a8896afc3d3c29e93a18179f2ca57163b8595933c8c36`. It is retained locally at `.test-runs/p024-send-fix/snapshot-01` and on the VPS at `/var/lib/harbor-dev-20260914/p024-sendfix-snapshot01`. Every fixed gate verifies the complete captured input inventory before and after execution.

On 21 September, the isolated Linux candidate passed frozen-lockfile installation, `pnpm build`, `pnpm check`, all 51 integration tests and the root-orchestrated/nonroot publication boundary test. That test proves the old helper fails at the installed root-owned 0711 container and separately leaves 0400 files under umask 0077. The corrected helper preserves 0700 private children and 0444 published files, and rejects forged identities, content tampering, malformed modes, extra hard links, missing bound files and replaced/symlink directories. Receipts and logs are retained in `.test-runs/p024-send-fix/linux-01/`.

The hardened personal attachment fixture passed with actual nonroot API/supervisor units and effective property/process/mode receipts. All 28 pinned runtime contracts then passed. Their first attempt failed because a parallel root fixture created the candidate evidence parent before the nonroot contract runner; correcting only that run-owned parent ownership resolved four output-directory EACCES failures. Candidate application and production permissions were not changed by that correction.

The full-stack live lane passed four synthetic turns at Low effort on `gpt-6-astra`: image-only, text plus image and opaque file, a text-only opening turn, then an image continuation. The model identified the red circle and blue rectangle and returned the exact opaque-file marker. Real browser/API/PostgreSQL/supervisor and pinned native Codex 0.153.4 were used; only OIDC was a fixture. Published bytes/inodes/modes, accepted composer clearing, draft reload and history references were asserted. `.test-runs/p024-send-fix/live-01/` contains the result, effective installed constraints, synthetic screenshot and cleanup receipts. The dedicated credential-only copy was removed, original installed credentials remained unchanged, and test services, cgroups, database, user and state directory were retired. This verifies candidate behavior under installed-equivalent constraints; it is not yet a post-promotion observation.

The first design and old-UI-control attempts failed because the new test's barrier cleanup raced its route handler, masking the intended assertion; the browser's ordinary autosave timer could also win before the intended Send call. Snapshot 02 changes only `tests/e2e/p024.ts` to freeze that timer and await held-route completion. Its input digest is `ac95a47bda496f33b94cd61088f51f0d7c833ed480fbff17a322f4c42a07cebd`, manifest `ba760a029664d1e6586015c351a787f99970bc01e20f60b3488f59182f3957fc`. Old-UI control 02 uses the same tests with only App/Attachments restored from 317a33d; digest `f51c49b32803479f6ffe2c8d38bdb8131b4ba980f465ed8d4f89de1ce0c45f93`, manifest `604f7de46d8318884289eb5c031b004c53c8f3101d71ed48f7e93f6bc42f58e9`. Failed run resources were cleaned by exact instance labels and PID start identities, preserving other tests and production.

Two critical-lane attempts independently observed HTTP 502 rather than the required 413 for a 10 MiB plus one-byte upload. This was retained as a blocker for direct/proxied transport diagnosis and a bounded correction; it cannot be waived as a passing image test. The selected correction's existing API/attachment/test fence covers this attachment acceptance blocker. No size or authorization limit may be weakened.

A separate read-only deployment preparation audit confirmed exact cgroup/durable-member drain checks and same-schema migration equality. Main added previous-running-executable checks, refusal of internal schedule admissions, and explicit clean-HEAD/commit linkage for final packaging. This preparatory audit does not replace the required fresh paired completion reviews.

The corrected design lane passed on snapshot 02. Old-UI control 02 failed specifically because Choose attachment remained enabled while Send awaited its draft-save response, confirming the missing submission reservation independently of source inference. Its expected-failure receipt is retained separately; it is not counted as a green application gate. This controlled path does not prove the precise sequence of the owner's later staged image. The fixed lane also verifies same-task file enqueue/Send, pending-upload Enter, duplicate submit, acceptance followed by failed draft GET and newer other-tab attachment draft preservation.

A direct/proxied probe of the immutable full-stack fixture reproduced the oversized-body issue: three proxy responses were 413, 502 and 413, while three direct API responses were 413. The pinned parser closes the connection before the proxy necessarily finishes writing the rejected body. The settled correction is limited to its body-too-large error: retain the parsing cap, send 413, discard framed unread input without buffering for at most five seconds and terminate the exact unfinished socket at that absolute deadline. Malformed/aborted framing and authority remain unchanged. The backend implementer supplied the narrow correction and transport regressions. Snapshot 03 captures them; its completion gate is in progress.

Snapshot 03 has manifest SHA256 `a375df5c9216b00cef2dc9987a53f529399e056998ad155fe6d08378c096f54b` and 1,114-input digest `cc5087f6f8ad3ec9d9587bc061c684fe6f24387f432b61e96d1a2ff6f9e1e3ad`. Full inventory comparison with snapshot 02 finds only three changed code/test paths: `apps/api/src/server.ts`, `tests/e2e/p005.ts` and `tests/e2e/run.ts`, plus four current Markdown records. Publisher, adapter/native boundary, UI implementation, design guide/prototype and UI race test inputs are byte-identical to their identified passing snapshots. The unchanged publication/native and design evidence retains those original identities; the changed API is undergoing fresh critical and full-stack model checks. Snapshot 03 has passed locked installation, build, check and all 51 integration tests on the VPS.

Snapshot 03 also passed all twelve grouped personal concurrency checks under the hardened service fixture. Its refreshed actual-model lane passed the same four bounded turns, with unchanged installed credentials and complete owned-resource cleanup. The copied receipts and synthetic screenshot are in `.test-runs/p024-send-fix/linux-03/` and `live-03/`; main inspected the screenshot showing the image in history, correct color/shape response and empty composer. The critical lane passed its new transport checks and P005/P002 portions, then failed at the later flood-session poll (`tests/e2e/run.ts:1507`) because an unchecked snapshot response had no `session`. That separate failure is retained as a blocker for diagnosis; no final green gate or deployment is claimed.

The failed critical run's browser metadata contains 429 and 502 entries, but it cannot attribute either to the failed API-request snapshot: P002 intentionally tests rate limits and other phases intentionally restart the API. Snapshot 04 adds only fail-fast sanitized snapshot status/error code/timing and owned-process metadata, retaining exact 200/session-ID assertions without retries or pacing changes. Its manifest is `e7f8dc0c4b0db1a8ea2aee73f5996d1e34776b44550376e53cbefd8c5ebd1b8d`, input digest `5633533ff302eed9bd6dfeba84889cb6a2dffea6fb54d6cadcfdb30a93c106c1`; comparison against snapshot 03 changes only `tests/e2e/run.ts` and this report. Main is rerunning the critical lane for observed evidence.

Snapshot 04 passed the previous flood poll without producing the diagnostic failure artifact, so the original response's cause remains unresolved. It later failed the existing P007 empty-credential-removal quota check with observed `409 CREDENTIAL_IN_USE`; `.test-runs/p024-send-fix/linux-04/critical-01.log` and the run-owned `p007-removal-failure.json` retain the result. Source inspection confirms discovery may overlap the repeated requests. Even with no encrypted database row, D004 requires native credential cleanup, and the existing supervisor denies that cleanup while discovery or active work is in progress. The bounded correction is test synchronization only: keep one idempotency key per logical empty-removal request, retry only this explicit pre-effect busy denial within a deadline, require eventual 200 and unchanged intent/audit counts, and fail immediately on every other error. Product cleanup, authority and quota rules stay unchanged.

Snapshot 05 captures that test-only synchronization: manifest `2b0947af3572f1fa1de4e50fabbcf87fd0106a39f16d0faa9a91ca8ac3893a8a`, 1,114-input digest `c9fbf433a197ed550cd43580f9d50a6fbe8a72fe8d52f1156b23ee5327da96f5`. Only `tests/e2e/p007.ts` and this report differ from snapshot 04. Each of three logical empty-removal calls uses its own unchanged key, permits only bounded explicit busy responses, checks no intent/audit growth on each busy result and after completion, and records all attempts. Snapshot 04's cleanup metadata showed no active operations, but did not directly capture the supervisor's discovery flag; attributing that busy response specifically to discovery remains a source-supported inference. No application, native, publication or UI source changed after snapshot 03.

Snapshot 05 passed installation/build/check and the attachment portion again, then timed out in an earlier P002 approval setup. The retained run `harbor-e2e-543fdbbc14` reports the target operation queued with `workspace_busy`, while the managed workspace's writer session and generation match the target session. No SQL lock blockers were present. This run did not reach the revised P007 check. The unchanged supervisor/workspace admission path needs bounded diagnosis; no full critical pass, review completion or deployment is claimed. The actual personal VPS model/concurrency evidence remains valid at its stated source and cannot substitute for this incomplete critical gate.

A fresh High-reasoning read-only workspace audit verified that queued turns do not prevent release of the exact managed writer and that the transaction helper does not retry transactions. The observed persistent reservation therefore cannot be fixed responsibly with a blind delay. The next diagnostic isolates the unchanged P002 suite and captures bounded operation/event/approval/writer metadata before teardown. The audit changed no files or infrastructure, and does not establish a root cause or replace the required completion reviews.

Snapshot 06 adds only the isolated runner and bounded diagnostic capture in `tests/e2e/run.ts`, plus current plan/report text. Its manifest is `1b971c7a0b02a82e491bd3e4986a162b848f06a0ce25b71698af6a7041123fb4`; input digest is `f9951cee2e0f24910b25c877778227c0442558e46ebb83a4bcb48969a30d7c00`. `--p002-only` reuses real authentication/API/PostgreSQL/supervisor services and invokes the unchanged P002 assertions in a new synthetic project. Metadata captures are bounded samples, not a transactionally frozen state; unsupported queue schema is explicitly unavailable. Full critical assertions remain intact.

Snapshot 06 passed installation, build, check and the isolated P002 suite. The focused log SHA256 is `3a2d39d75ed27bed52e3a941e0515cfb299c5e9e9dba0cc86206d87442a07526`. This is nonreproduction, not proof of the earlier stall's cause or a full critical pass. Main started the complete critical lane on the same immutable snapshot, with the additional diagnostic capture retained.

The focused P002 metadata (`linux-06/p002-focused-state-passed.json`, SHA256 `0b0ff8b6dc3e9bd600b453bae4c5c459b10c2fdc8293ad98df8cd5b0c12d5327`) contains 21 terminal operations, zero unsettled operations and zero retained writers. It does not establish that the earlier timing window was exercised. The full snapshot 06 critical attempt instead failed after 20 seconds in an early cancellation setup: the unchecked admission body lacked `operation`, so its exact HTTP denial is not yet attributed. Its raw log SHA256 is `527f93f38d576db9aee403d74817078a87b1a77739138fe1b471f45bb3980515`. Main retained the failure and requested safe admission-response diagnostics; no product admission change or passing full critical claim is made.

Snapshot 07 adds bounded command-response status/error-code/timing diagnostics and an explicit accepted-operation assertion in `tests/e2e/run.ts`; no application, retry or pacing change is included. The diagnostic records omit request bodies, headers and keys, and retain only allowlisted process error categories. Handled API exceptions may not appear in process logs, so their absence proves nothing. Its manifest is `b4d203872a496f313f0446c1e7c18931c019bdffb62036e87bb749df538c017c`, input digest `cfc71c11efcae1f4989a040ca54abee016d01dd97d70651eaf96a523ac0ecd7a`. The full critical gate is being rerun.

Snapshot 07 installation, build and check passed. A complete manifest comparison (`snapshot-07/reused-boundaries.json`) confirms all application/package/infrastructure files remain byte-identical to snapshot 03, and the UI implementation, guide, prototype and race-test files remain byte-identical to snapshot 02. Their identified model, concurrency, integration, native/publication and design evidence is reused only for these unchanged boundaries.

Snapshot 07 passed the P005/P002 portions, then failed an input-flood session snapshot with observed HTTP 500 `INTERNAL_ERROR` after 1,031 ms while API and supervisor processes remained running. The critical log SHA256 is `77767f2f8384f2fa895231c80c339acefb757beb355460f942b90582cb0cf2e7`; fixture `harbor-e2e-0106661346`. This attributes the latest failure but does not identify the underlying exception or retrospectively prove snapshot 03's cause. Central command records contain no 5xx responses, and allowlisted process diagnostics contain no error matches. Because the API handles unknown errors without logging them, PostgreSQL evidence before fixture teardown and a focused lock-order audit are required; no retry, altered admission or full critical pass is claimed.

Snapshot 08 adds only bounded PostgreSQL diagnostic capture before fixture teardown, plus current proposal/report/issue text. It retains fixed error categories and numeric transaction-lock wait edges, never raw PostgreSQL logs or SQL values. Manifest SHA256 is `5a80bb397fb362d39870c359ccd723a542ba5ca49ba571e04ec576ba4de67df1`; input digest is `0d849907d82c052b86160ec8704a1d06f4f10bbec3368a2cc7b8f06a3d0ab4d2`. Product code is unchanged. A separate bounded synthetic PostgreSQL probe is being prepared to test the source-supported lock hypothesis, not assumed to prove it.

The first synthetic replay probe stopped during provisioning before recording the server version or executing any iteration. Its exact temporary container was removed and snapshot 07 input verification passed before/after. The receipt cannot identify the connection error because its initial error allowlist omitted Node network codes. Main approved a harness-only TCP-readiness check and bounded network-code/phase capture for a second attempt. This failed setup is not evidence of application replay behavior or a proven setup cause.

Snapshot 08 passed build/check but failed P002 at the pending-approval state assertion. Its bounded PostgreSQL capture proves four deadlocks, without yet identifying the participating statements. Critical log SHA256: `856edc6e9646c1759040c7da2775d5d11dc3436eb5cf548bfc821895288a2cc2`. Main retained the [current blocker](../../issues/archive/2026-09-21-163057-conversation-parent-lock-deadlocks.md). The corrected synthetic probe 02 completed 100 rounds/300 transactions without error, with 44 lock-contention samples, exact retention bounds and confirmed owned cleanup (receipt SHA256 `1e01060030926fd7af6167cc406887041244e393da5bc5b4a002bafb95d80947`). It omitted parent-lock contention and therefore cannot disprove that separate source-backed cycle. A deterministic parent-holder probe and full-stack static-query attribution are pending.

The controlled probe 03 reproduced the parent/session foreign-key inversion in one iteration using two real transactions and unchanged snapshot 07 modules. It observed the parent holder waiting for the child-held session before releasing the child into `event()`, then recorded mutual blocking and PostgreSQL `40P01`. The exact run-owned container was removed and complete source verification passed before/after. Its receipt SHA256 is `5d17ef0b43c5e0ecb68814c871a1db4adec33bce2b685ee2f8c4ed4b0e05c93a`; the diagnostic exit 1 records the expected old-source failure, not a passing application gate.

Snapshot 09 adds only static-source query attribution to the bounded diagnostics and current records. Its manifest is `055479dd334f638dc2c82e69f40683b2042b4d600a9f3a1b4976602797d3c7d5`, input digest `c45d8b63eb0a0585605ae6f51bf64fc7f86b25049a332f7e9473737d46ee624a`. Build/check passed; full critical failed at P002's pending-approval count. Its four deadlocks pair the exact sequence-update SQL at frozen `packages/storage/src/index.ts:84` with the exact session-generation lock SQL shared by `apps/supervisor/src/main.ts:649,1425,1622`. Static SQL attribution identifies that statement family, not which of the three identical callsites ran. These observed full-stack pairs match the controlled mechanism. Raw critical log SHA256 is `035395e8461d293615de3a5594a26de234cb7737e540ca2b3508bee3a4b4778b`, fixture `harbor-e2e-5163711787`; sanitized metadata is retained in `linux-09/`.

Main updated the canonical persistence contract and Accepted P024-R4 fence for the demonstrated current-gate blocker. The original backend implementer is applying the explicit parent-before-child resource fence across affected transaction entries, standalone replay and ordered startup reconciliation, with a real PostgreSQL barrier regression. There is no schema, lock-mode, native-runtime, authority or generic transaction-retry change. The changed supervisor will receive fresh integration/critical, personal concurrency, pinned-contract and bounded actual-model checks; prior source 03 model evidence cannot certify the new persistence implementation.

The writer audit also found a separate managed administrator interruption order problem, recorded in [its own issue](../../issues/2026-09-21-164130-managed-interruption-lock-order.md). That existing managed-only path is unused by the selected personal VPS promotion and is not changed or certified by P024. Main added the existing native acknowledgement contention test to the correction fence solely to observe the new parent wait while preserving its ACK/request/event assertions.

Snapshot 10 freezes the twelve-file persistence correction and barrier tests. Its complete 1,290-file manifest is `45c9018065053902f62fd07efea49d8835588416a00bfe6061be68c68b2088b1`; the 1,116-input digest is `6bd9d8864d0e243fab5a101e2aa100255d0c20c8a4f96f8cfea4344cb1ee1f94`. Local TypeScript, formatting and whitespace checks passed. Fresh Linux gates are pending. The real-service barrier observes API replay pruning and supervisor output/approval settlement waiting at the project while a separate transaction can acquire the session with NOWAIT; fixed test-only PostgreSQL application names distinguish the two service processes. Full critical finalization rejects any unexpected database/log deadlock. No production switch has occurred.

Snapshot 10 passed build/check and all 51 integration tests. Its full critical attempt stopped at the new supervisor-waiter observation (expected at least two, observed zero); the API snapshot barrier had passed with a free session, and both PostgreSQL counters/logs reported zero deadlocks at teardown. This incomplete run is not a full critical pass. The observer only matched immediate blockers, which can miss PostgreSQL tuple-lock queues; the failed graph was not retained, so this exact explanation remains unproven. Main authorized a test-only bounded transitive blocker graph and safe success/failure receipts, preserving waiter counts, session NOWAIT and actual settlement assertions.

The first snapshot 10 contract attempt passed 27/28 but the existing 150 ms fixture timeout test failed before its expected rejected turn, during parallel build/test load. The same unchanged 28-test contract gate then passed when run alone (log SHA256 `4c254060fcf76aa9439493df86667663b0a768e9930f08613f42926a2203f88e`). The initial failed receipt remains retained; load is a plausible timing explanation, not instrumented causal proof. No adapter source, timeout or assertion changed.

Snapshot 11 changes only the two contention test observers and this report relative to snapshot 10. Manifest SHA256 is `453e6c7beacc5210ed20a2b970051fb929bc6572899f01a973f0ed9d0ce8df47`; input digest is `d87bb6cde7c89e4a5a418f0bdfc55310f1f72c5303448f564fab334faa2a93aa`. Its bounded graph follows up to 128 nodes and depth 32 to the exact test gate, with the original database, service and query identity requirements. It records graphs on success or failure, without retaining raw SQL or parameter values. The source change does not establish the cause of snapshot 10's unrecorded graph. Full path/hash comparison in `snapshot-11/reused-boundaries.json` proves all 936 application/package/infrastructure files identical to snapshot 10, and unchanged UI/publication paths identical to their original passing evidence. Snapshot 10's twelve grouped personal concurrency checks passed (log SHA256 `4c146068435fbb3f7c886bc2792f706d0377fa3154c395bf3eaf51d179cda3e1`); its fresh actual-model check is running.

The refreshed snapshot 10 actual-model lane passed all four turns from 09:00:31 to 09:02:05 UTC (log SHA256 `fb4c8b7f0a61d268b2a71ef668c581c457bebc0c2075a9b6913a6494be9fd5e1`). Its receipt confirms the installed credential was unchanged and the isolated credential copy removed. The source remains byte-identical to snapshot 11's application/package/infrastructure boundary. Effective service constraints, model results, synthetic screenshot and owned-resource cleanup are retained in `live-10/`. Full critical acceptance and independent completion reviews remain pending.

Snapshot 11's real API and supervisor lock barriers passed. The captured supervisor chains were PID98→API142→gate178 and PID99→API142→gate178; the independent session NOWAIT probe succeeded and the operation/approval settled successfully. No database or logged deadlock was observed. The full critical run later failed browser bootstrap at P005 upload recovery with failed capabilities/draft/snapshot requests, while API/supervisor remained running. Chromium failure codes were not retained, so the cause is unresolved. This run overlapped another fixture's Docker network startup (critical09:02:32–09:03:43; design09:03:30–09:05:57 UTC). The design lane passed. Main is rerunning the same immutable critical gate alone, without source, pacing or assertion changes; environment interference remains an inference. Critical01 log SHA256 is `60c5412031b0f2a9fd2664fb161909210cfe0c6257881af4d86957968dbd4460`, with exact sanitized graph/network metadata retained in `linux-11/`.

The isolated snapshot 11 critical02 attempt passed the prior P005 browser load and all attachment recovery/association checks, without proving the earlier transport cause. It then failed the existing P002 cancellation-expiry observer because that test still matched the old literal session-lock SQL. The corrected shared helper uses the ordered session-array query, so the old observer cannot recognize the actual wait. Main added only `tests/e2e/p002.ts` to the necessary test fence: retain the held session and authority-expiry/native-effect assertions, match the new supervisor query with exact database/gate attribution, and audit other literal observers for the same changed-callsite mismatch. No product, timeout, pacing or permission change is selected. The failed log SHA256 is `30393ba55654f106ba0d3654a0155c5c38fdc53f66d26f28a4a7870eee6b3d7e`.

Snapshot 12 changes the P002 observer and current Markdown only. Manifest SHA256 is `09ab6ef096c5272bef5f889f4babb511284e0a8049bea62d19e62fbcfad485d1`; input digest is `ba45c0b5808329930439fec1c1a4b95d6b087ffe64de402b13648d738fa2f7f9`. The observer follows a bounded deduplicated blocker graph to the held session connection, retaining all original expiry, denial, native-effect and timing assertions. The implementer audited all 25 pg_stat_activity observers; the other literal statements still match their unchanged implementation. Local formatting/TypeScript/whitespace passed. Application bytes and the already-passing model, native, integration, concurrency and design boundaries are unchanged; the complete critical lane is being rerun alone.

## Complete correction gate

Snapshot 12 passed the full isolated Linux critical lane, including P005 attachment recovery/association, P002 authority expiry, native acknowledgement contention, the parent-lock regressions and P007 history/credential checks. Build/check also passed. Critical log SHA256 is `0fd20dda4afcae527af71791c8e2eeb2dad129a6ccc5a689f8889d063ec81840`; the gate's complete source inventory was verified before and after execution. Earlier failures and uncertain attribution remain preserved above. This complete pass does not retrospectively prove the cause of each failed attempt.

The release evidence combines this critical/build/check gate with the byte-identical application boundary's 51 integration tests, 28 pinned runtime contracts, twelve grouped personal concurrency checks, four actual-model turns under installed-equivalent constraints, and snapshot 11's fresh desktop/mobile design and attachment race checks. The unchanged publisher's Linux failure controls and corrected adversarial cases retain their original snapshot 01 identity. These mappings are mechanically recorded in the frozen manifests and unchanged-boundary receipts. Fresh independent design and provenance review round 1 begins after this green gate; promotion remains pending.

## Gate evidence index

The following successful gates apply to the final correction through snapshot 12. Earlier entries above preserve the state at each attempt; failures are retained rather than overwritten.

| Gate | Source | Result | Log SHA256 |
| --- | --- | --- | --- |
| Build | 12 | Passed | `ca0cabdaa373d7698a7b7994e7de7c0d72bf4a4b78893220c65be96d9c808d0a` |
| Static and documentation checks | 12 | Passed | `b024ed5c8cfbddab730a774003b8ca3452d06a7f3c112f7f60432792fedf2ff7` |
| Complete critical E2E and zero deadlocks | 12 | Passed | `0fd20dda4afcae527af71791c8e2eeb2dad129a6ccc5a689f8889d063ec81840` |
| Desktop/mobile design and attachment races | 11 | Passed | `d4d8c6a0dc9ecbe0fef809b4fd9866280b9f9d4fa3e8a1acfd11b22e868a307b` |
| 51 integration tests | 10 | Passed | `2371b890bdf98eedd1c50aa5726c341ca251299b50a15ba328c34571ef5ba7d0` |
| 28 pinned runtime contracts | 10 | Passed | `4c254060fcf76aa9439493df86667663b0a768e9930f08613f42926a2203f88e` |
| 12 grouped personal concurrency checks | 10 | Passed | `4c146068435fbb3f7c886bc2792f706d0377fa3154c395bf3eaf51d179cda3e1` |
| Four actual-model turns under installed service constraints | 10 | Passed | `fb4c8b7f0a61d268b2a71ef668c581c457bebc0c2075a9b6913a6494be9fd5e1` |

Raw receipts/logs and sanitized fixture artifacts are in the corresponding persistent ignored `linux-10/`, `linux-11/`, `linux-12/` and `live-10/` directories. The frozen manifests plus `snapshot-11/reused-boundaries.json` and `snapshot-12/unchanged-product.json` establish unchanged-boundary reuse. Snapshot 12 critical fixture `harbor-e2e-3b0446f7f8` reports zero database deadlocks and zero logged error-category matches; its `result.json` matches the same input digest at start/end. The log retains the deliberate supervisor-kill recovery observation. Full raw session export remains unavailable.

## Independent reviews

The reopened correction completed one fresh paired review round after the green gate. The High-reasoning design reviewer and Medium-reasoning provenance reviewer independently inspected all 39 changed/untracked paths against `317a33d6c9cee59a2827b1a7311d5c0e75550811` and returned no actionable blocker or advisory. Main accepted both findings. They verified source/fence identity, meaningful regressions, preserved authority/publication/draft behavior, accessible receipt/log hashes, unchanged-boundary reuse and exact owned cleanup. Read-only documentation and whitespace checks passed.

The provenance review raised a count clarification: the complete capture has 1,118 non-Markdown files, while the selected source digest has 1,116 inputs. Both are correct; the two canonical design HTML files are captured but outside `scripts/source-digest.ts` roots. Main verified this distinction and found no source or evidence mismatch. Reviewer messages are retained task evidence, not a complete raw session export.

R1–R4 are satisfied for the selected personal VPS correction. R5 remains pending until the exact committed artifact is packaged, backed up, promoted and verified. Native macOS lifecycle, managed promotion/restore and the separately tracked managed interruption issue are not certified by these reviews.

## Committed candidate and interrupted promotion

Main committed and pushed the reviewed correction as `c7f2d26a552b1db47125f5aa18417a1b42a2038a`. The clean committed capture has manifest `74fa939a1cb3d98b833cf047880fad61b1f3c7376b008c37d9d1bf22115e8ae5` and the same 1,116-input digest as snapshot 12. Its four subsequent Markdown differences do not change the tested application. Frozen-lock installation and the committed VPS build passed; the build log SHA256 is `f9bbdbb22f46c3e339942d3bbc79924a675f0533fb85107c28acd240a20db94a`.

The immutable candidate is `/opt/harbor-personal/releases/p024-sendfix-c7f2d26a552b`, artifact manifest `0eaa243f6fe8e812a3b19a799d0b102aa526c6246b91979183114011d664c0a0`. Its complete file/mode/symlink inventory SHA256 is `f8e58dd6366d4bad22b50cdb0b77619d6adff9e4e75be8653378370a8269b263`; archive SHA256 is `145b6ac41ed75c6874bc2fe432d24cc954b30c2df5605a8a3df9491da90fa21d`. Main reviewed every artifact delta: 18 implementation/generated-output files, 78 command shims differing only by candidate path, and two pnpm metadata files differing only by path/timestamp. No dependency code, native executable or migration changed. All 18 migration identities match the installed release. Exact package verification, a packaged Sharp PNG normalization check and root-owned staging passed. Private evidence is in `.test-runs/p024-send-fix/release-c7f2d26/`.

The first promotion attempt at 09:37 UTC failed a pre-activation assertion and resumed the previous services. The new release was not activated, and the proposed backup directory was not created. The helper recorded the exception class but not the failed assertion; post-resumption metadata alone cannot establish its exact failure location. It showed zero active/uncertain operations, workspace writers and releases, but two retained conversation runtimes and two stop-requested session flags. At 09:45 UTC, bounded read-only inspection verified both recorded identities: valid instance-owned cgroup paths, matching host, current boot and session/member generation, with both cgroup paths absent. No owner input was replayed and no draft or ownership row was manually cleared.

The installed [D014 restart contract](../../design/decisions/014-personal-conversation-concurrency.md#idle-classification-and-retirement) intentionally treats missing same-boot cgroups as unknown. Its existing recovery accepts a verified changed host boot; an ordinary service restart supplies no additional absence proof. Main requested separate authorization for one VPS reboot because it interrupts all host services, beyond a Harbor-only release switch. A matched pre-reboot checkpoint and the unchanged zero-ownership promotion gate remain required. The [retained-runtime issue](../../issues/2026-09-21-174500-personal-stop-retained-runtimes.md) owns the unresolved normal-stop lifecycle diagnosis. The immutable candidate remains ready, while R5 stays incomplete pending supported recovery and installed verification.

A proposed export of raw supervisor logs was rejected by automatic approval review for potential private content. It was not performed or retried indirectly. Diagnosis relies on bounded metadata and inspected source; the precise shutdown failure remains unproven.

The fixed-instance reboot-checkpoint procedure passed read-only preflight: the two unknown members are the only retained ownership; all checked operation, workspace, schedule, approval, managed terminal/preview, file and recovery counters are zero. A fresh 09:54 UTC filesystem check confirms that the first promotion's proposed backup directory is absent. The independent design reviewer found no actionable issue in the procedure, SHA256 `208c0a85ed09bf7259f6dd17610be34be84755e7aa887df56ae8e0a518adfdf2`. Preparation would preserve the unknown records in a private checkpoint and stop only the old application units; it contains no reboot, activation or database mutation. This is a reviewed plan and passing read-only preflight, not an executed checkpoint, reboot or delivery. Owner authorization for the whole-host interruption is still pending.

The independent provenance follow-up found no actionable finding in the committed/package/staging evidence, downloaded build/install logs, failed-promotion receipt and bounded recovery observations. It verified the 1,290 captured file hashes and source linkage. The later preflight and backup-absence checks corroborate current state, not the exact earlier assertion or normal-stop cause. This follow-up does not certify pending R5 execution. Documentation checks passed for 173 files and whitespace checks passed.

## Authorized reboot and installed continuation blocker

The owner subsequently explicitly authorized the VPS reboot and completion of deployment. The reviewed checkpoint completed at `/var/lib/harbor-personal-backups/pre-reboot-817-20260921T110353Z-75e217f5ca594db7911400b0655a3a45`, database SHA256 `eda5d55136be9ed1e39aa9c5fcb21d737e8c509f6a29fb288d9cd62655b530bb`. After `systemctl reboot`, a read-only verifier established the changed boot on the same host, intact checkpoint, automatic release of both exact runtime members/background flags, zero other ownership counters and healthy authenticated routing. The source-reviewed verifier has SHA256 `bdab2615d38d2c75c67f0798412c1c1dd55816afcb68d97dc7e782961dd2d969`; its receipt SHA256 is `331080c6f373f90019e0408f564ffdb4c39302fe55273a92dabb8be0c3cfa831`. Intermittent pre-authentication SSH/SFTP closures during reconnection were handled through the same pinned SSH identity, without weakening host verification.

The unchanged reviewed promotion procedure then successfully installed `c7f2d26` in manifest `0eaa243f6fe8e812a3b19a799d0b102aa526c6246b91979183114011d664c0a0`, after an independent all-zero drain and fresh matched checkpoint `/var/lib/harbor-personal-backups/p024-sendfix-c7f2d26a552b-20260921T111040Z`. Its database SHA256 is `30a95388a50bb7e2918414ec3e387f1ab198823d8acec480709550453a4d7ca2`. Installed API/supervisor executables and assets matched the candidate, no old-release processes remained, account discovery authenticated, and all 18 migrations, site configuration and restrictions were preserved. Public health returned 200; protected assets/API/root returned 401; anonymous preview returned 403. Exact receipts and hashes are in `release-c7f2d26/`. These are same-host checkpoints, not project backups or tested fresh-host restoration.

The final browser check required reconnecting the expired owner login through its existing sign-in link; no credential or access-policy change was made. It loaded the installed `/assets/index-BU36WBXv.js` bundle and the requested conversation, but exposed an additional R5 blocker: the stale uncertain session projection hides the composer. Runtime ownership and stop flags were gone, and no operation was uncertain. The 11:03:37 and 11:12:45 UTC fingerprints prove the saved draft, two attachments and sixteen messages unchanged; no owner input was replayed or removed. C7 is installed, but usable continuation and R5 completion are not claimed.

Source inspection confirms startup confirmed-absence and live-retirement branches omit session derivation, while the disconnected tick also omits background clearing. The existing operations-derived helper is safe only after ownership and unsettled-effect guards. Main settled the narrow personal projection correction in the current conversation design and Accepted P024 plan before implementation. It must also handle sessions already released by the prior version. Fresh affected gates and the next paired review are required; the normal-stop cause remains separately unresolved.

## Guarded continuation correction verification

The three-file projection implementation was frozen in snapshot 13 against `522b431a7eb7217e81e429925aea84591c4ef339`: 1,291 captured files, manifest SHA256 `65463d62f4dddb79325ea5c0172024df0ec90e1a2ad9c7f22dd426f45c01b681`, 1,116-input source digest `5ea4a0a0edc92f824548aecc3a9b17497a57593aaf5f44ad24f668df6d2e0618`. The only non-Markdown changes from the previously reviewed final snapshot 12 are supervisor orchestration, guarded storage derivation and the personal concurrency regression. It changes no migration, native protocol, dependency, launch restriction or UI byte. The implementer ran TypeScript, formatting, whitespace and fifteen focused retirement/cgroup/runtime checks locally; main owns the Linux acceptance below.

The isolated Linux candidate `/var/lib/harbor-dev-20260914/p024-sendfix-projection-13/source` passed locked installation, `pnpm build`, `pnpm check` and `pnpm test` (51 tests). Their raw-log SHA256 values are, respectively, `38e42b75f69382f065e76f7f0dd25ccb2a29bfb41a142de882780b816b67aacd`, `1916f91d22158fffcb66214d84f276a4c05cf269efb8133e4cf1637bb823dc20`, `41a55fdab5d6b88d0824256c7a040919862b79dce6bd6a7c7b47e44b643af015` and `a93455b73374f3987229a81c6b5015640d1211308e8ed18a38df5536a6a97603`. Full critical E2E passed 11:34:18–11:39:26 UTC on the real browser/proxy/API/PostgreSQL/supervisor with deterministic external boundaries, fixture `harbor-e2e-1b69dfbc06`; raw-log SHA256 `235f4ec44dcb243fe246bfca92f316fd7f545f4b44fb85687f9839bd6fda06fe`. Both before/after source inventories matched the frozen snapshot. Actual database deadlock count and every captured allowlisted PostgreSQL error category were zero. The deliberate supervisor SIGKILL diagnostic in this successful run is part of the interruption assertion, not an omitted failure. Receipts, raw logs, complete result and sanitized diagnostics are retained in `.test-runs/p024-send-fix/linux-13/`.

`snapshot-13/reused-boundaries.json` compares complete selected path/hash/mode inventories: three unchanged publication paths against snapshot 01, 33 unchanged UI/control paths against snapshot 02, 739 unchanged adapter/contracts/runtime/dependency paths against snapshot 10, and 34 unchanged desktop/mobile design paths against snapshot 11. These retain the earlier adversarial publication, old-UI negative controls, 28 pinned Linux/native contracts and desktop/mobile design results with their original identities and limitations. They do not substitute for refreshed tests of the changed supervisor/storage orchestration. Personal continuation, actual-model delivery, paired review and final installed R5 verification were still pending when this paragraph was recorded.

The fresh personal concurrency lane passed 14 grouped checks at 11:39:58–11:41:54 UTC, raw-log SHA256 `7e72f9ccf0840e9e18ce6c264ef85b80b7a87cc9484686bc94fe3d136b2c7176`. `linux-13/concurrency-result.json` (SHA256 `704a02072c891e1d9fc7dff60a046ac959d72183c926de98758df3dac8e00db8`) records real PostgreSQL guard preservation, startup/live-retirement/disconnected-tick derivation, intact draft/history/native thread, no automatic admission and exactly one successful explicit browser continuation. All existing P018 parallelism, ordering, capacity, protected/unknown, generation, queue and desktop/mobile assertions remain. Previous-boot identities in this lane are explicitly synthetic. Its owned cleanup confirms stopped services and removal of only the fixture database, user and run directory.

The first refreshed live run failed the opaque-marker assertion after successfully submitting/clearing the composer, identifying image colors/shapes and validating every published file's mode/inode/device/bytes. The model reported a file-tool “Operation not permitted”; that text is not a captured syscall failure. Raw-log SHA256 `87bdaacfde1596a0e8b0183174f019ff7dfe00062175f78b2680d6254300b5eb`, receipt and run-owned artifacts are retained in `live-13-failed/`. The credential-only copy was removed and installed credential bytes were unchanged. No product assertion was weakened.

A read-only audit at 11:45:37 UTC established that the operator runner still selected old817's executable pathname, while the installed AppArmor user-namespace allowance had moved to c7's exact pathname during promotion. The kernel user-namespace restriction was enabled; both executable files had the same pinned SHA256 `56ef98ab4032d317ab26e9b5e5a175650717351edb16ed9cde0cb6d1734d62da`. This proves a test-environment mismatch, not the precise failed native syscall; raw kernel/runtime logs were not exported. Main corrected only the ignored fixed runner to use the currently installed profiled path and verify it against installed configuration, retaining source, credentials isolation, all four turns and every assertion. Helper SHA256 `cac715770b83c0850c85ed0afefcca38db92946c16c386e2218a011fd7bcfb03`; the original failure and original runner remain available. The repeat is a new numbered attempt, not a replacement of the failed record.

The corrected operator entry completed all four actual-model turns at 11:46:30–11:47:31 UTC, exit 0, raw-log SHA256 `3bddff872e386e3723e32f2fee6378ec085f4b629e4e4f6ec88bba8bd8ede60d`. It used the exact currently profiled c7 native path, Codex 0.153.4 and GPT-6-Astra/Low, fresh private project/database/native home, actual hardened nonroot API/supervisor and browser; only OIDC was deterministic. Image-only send, exact opaque marker plus image response, text-first image continuation, accepted composer clearing, reload/history, four accepted operations and published file identities passed. Installed credential bytes were unchanged and the copied credential was removed. The successful receipt, raw log, service-boundary artifact, synthetic screenshot and owned cleanup are retained in `live-13/`; the earlier failure remains in `live-13-failed/`. No source changed between those attempts. This pass supports the corrected environment; it does not retroactively supply the unavailable failed syscall trace.

### Projection review round 2

Independent High design review found one P2 in the seven-file delta: the pre-existing startup update relabels non-turn uncertain operations as failed before the new guard runs, so a released session with an unacknowledged uncertain cancellation could incorrectly derive to succeeded. The direct rollback-transaction guard fixture did not exercise startup ordering. Main accepted the finding and sent the same implementer a personal-only preservation correction and actual restart regression, keeping managed behavior unchanged. This is a contract violation for the supported durable-state fixture, not a claim that current cancellation producers routinely create it. No other actionable design findings were reported.

The independent Medium provenance reviewer returned clear, verifying all frozen files/modes/digest, raw gate hashes, unchanged-boundary inventories, zero deadlocks, live failure/correction limits and cleanup. This clear evidence review does not waive the design finding. `.test-runs/p024-send-fix/reviews/round-2-disposition.json` retains Main's disposition; complete raw review-session exports remain unavailable. The third and final paired round follows the scoped fix and refreshed applicable checks. No follow-up code commit, package or promotion has occurred yet.

### Final gate catalog-fixture correction

Snapshot 14 froze the accepted startup fix and restart regression: manifest SHA256 `eaf9fe5e2d258f6bcd91b1644338dc11b265b97181d882696c953ee5dae89b4c`, source digest `50dcd6f8eba1ad558b5867b23aba8349c003e5f674396ce79513aea0d7b2aaa8`. Only supervisor startup and personal concurrency code changed from snapshot 13. Build/check passed, but critical run 01 failed at P005's image-model denial: expected 403, observed 202. Raw-log SHA256 `6663aab82aeabe45eb55e199f52394ed76315ba466ba09ca28153b2f3281afbd`; receipts and diagnostics are retained in `linux-14/`.

The test committed a text-only catalog, verified the browser warning, then sent its crafted request without fencing subsequent catalog writes. Source inspection shows both periodic discovery and runtime initialization replace that catalog; API validation reads its current contents. The browser result therefore does not establish the catalog at API admission. The exact intervening write is unavailable, so this remains a source-supported race candidate, not a proven authorization defect or retroactive causal attribution. Main added only `tests/e2e/p005.ts` to the bounded test fence: publish then row-share-lock and verify the committed text-only catalog, keep ordinary API reads available, hold through unchanged 403/no-operation assertions, and release before restoration. A setup race must fail explicitly; no mutation retry, increased limits or API permission change is authorized. The failed gate remains visible; a fresh complete critical pass is required.

The final product source (snapshot 14) independently passed `pnpm test` again (51 tests; log SHA256 `431642e20029d95d2d8e9ba29bc69edf04214847ff6a423b0bf694b73abda06a`), 15 personal concurrency groups at 12:01:40–12:03:37 UTC (log `b4c2c9c58cb3f6dd60dae38d902591c8ce6c4b8a7fed9333b2da953dda3c2e1c`) and four actual-model turns at 12:03:38–12:04:34 UTC (log `672f02eb7e6eddee064d4a7c5b31f79cf04bf864f6a29b5cbceca08e19d615d2`). The new actual-restart check preserves every field of the uncertain nonturn operation and keeps its session blocked while an unrelated eligible session repairs and continues. Concurrency result SHA256 `f3a0bf60f54883209bbe9c97b1de3779151ea117b601a7ef4b2a9819c3cfb785`; owned services/database/user/native home were removed, and live credential copy removal plus installed-credential preservation passed. Receipts, raw logs and artifacts are in `linux-14/` and `live-14/`. These independent passes do not turn the failed critical run into a pass.

Snapshot 15 freezes only the test catalog synchronization and Main documentation over that product: 1,291 files, manifest SHA256 `dcbef621fcecba6b4dd03f9d2021311820f6e4a84848f3455b7a6d7d8039093a`, 1,116-input digest `e1768decdc6145091d18484689416209d605702d0134a872045dbccc373b08fe`. `snapshot-15/reused-product-14.json` verifies all 1,117 other non-Markdown files and modes unchanged; `tests/e2e/p005.ts` is the only non-Markdown difference. The actual-model, integration and personal-concurrency runners do not import that test. Earlier publication/native/design/control inventories were also rechecked against snapshot 15. Fresh build/check/full critical and the third paired review remain mandatory for this final test snapshot.

Snapshot 15's locked install, build, check and complete critical run passed. Build raw-log SHA256 `e723908a5f7d894b98ee7ed02fcf3269a9e004330827134894d33fbec4a85a47`; check `7e27337f68e47d9eed37a4289be67357f08d57a48c73ea3f52355fbfdc2a0723`; full critical `f4ed30a5f30e1fad5b01af707f1050afda90346985ca62cc31df25f82124fa22`. The actual API denial, no-operation assertion and unchanged P001/P002/P003/P005/P007 suite all passed with the model catalog fixture fenced. Exact before/after source receipts, raw logs and sanitized PostgreSQL/catalog evidence are retained in `linux-15/`. This is a new successful run; the unfenced failure remains recorded above.

The final critical run completed at 12:06:29–12:11:34 UTC, fixture `harbor-e2e-6487f9de13`; both full source inventories matched. Actual database deadlocks and every captured allowlisted PostgreSQL error category were zero. `p005-modality-fence.json`, SHA256 `6f8dcb8a5a5364825757de698bfbc9244609e492539eddfb005f4b20d864264f`, confirms the selected catalog stayed text-only, the fixture precondition and API/UI assertions passed, and the bounded fence was not lost. This metadata proves the successful controlled run, not the unavailable intervening write in the earlier failure.

### Final paired review disposition

The third and final paired round returned no actionable findings from either the independent High design reviewer or Medium provenance reviewer. Main accepted both. They verified the prior P2 resolution through actual restart, the authorized P005 test fence, final eight-file source scope, complete frozen inventories, green raw gate identities, controlled API denial, retained failed attempts, live cleanup and unchanged-boundary linkage. Documentation links/whitespace passed for 173 files and `git diff --check` passed. Main's disposition is retained in `reviews/round-3-disposition.json`; complete raw review-session exports remain unavailable. The final product is reviewed for commit and immutable packaging within the owner's existing authorization. R5 remains pending the follow-up promotion and usable installed conversation check; normal-stop retention and managed interruption remain open.

## Final deployment and installed acceptance

Main committed and pushed `311f750a18bb8bc400ddfc497b82b0f5de6e9e25` after the final paired review. The clean committed snapshot has manifest SHA256 `22708a86f08caeb4383f5184ab0b2bc1fc0827067ff2250e262019f6f3290ab4`; its source digest is the same `e1768decdc6145091d18484689416209d605702d0134a872045dbccc373b08fe` as tested snapshot 15. `committed-311f750/tested-source-linkage.json` compares every non-Markdown file and mode; only proposal/report prose changed after the test snapshot. Locked installation and build of the committed Linux candidate both exited zero, with raw-log SHA256 `55a0fcd7c0f04f43880e40fc6f850d7c69ee1ce87871d19032016b1ec8b71e19` and `b61586e1cf0184fc654a1b6112f1f9652cd9e662c1a2db08d881e784db556b82`.

The resulting release is `/opt/harbor-personal/releases/p024-sendfix-311f750a18bb`, manifest SHA256 `0ad775d55a52d75cb7e95fd0bb475d3202d86f90df8a70ac9c3d61e259382795`. Its complete 17,953-entry package inventory has SHA256 `1828ef6e669a1f914fd4527acb440aabaed15d078e06a24ac6a0767678c09d8b`; the 253,214,770-byte archive has SHA256 `2eb556b988af041f6a23e8081276cf477abcf7a3934decc107009f05f7c6d11e`. Exact source/native/copy identity and packaged Sharp 0.35.4/vips 8.18.6 normalization passed. Main inspected all 82 file differences from installed `c7f2d26`: two reviewed product source files, 78 package-manager shims differing only in the exact candidate path, and two parsed metadata files differing only in that path and their recorded pruning/validation timestamps. Native executable, dependency pins, UI assets and all 18 migration digests are unchanged. The complete package receipt has SHA256 `37444b358ea64a1e5bcf68fb9da63327d6c9e86055a99e3efc171070f955f41c`; artifact delta review has SHA256 `2716ddadb52a601965c85762f39ce565ee21c0eefe68755563195287f6fdd122`.

The original packaging SSH caller exited 255 after its owned transport master was closed to recover a stalled channel. The remote packager nevertheless completed its durable receipt successfully at 12:23:12 UTC. A proposed resume failed its first guard before any rename, write or process launch because that receipt already existed. No second packager ran and no partial package was substituted. The original successful package receipt, caller failure and reconciliation are retained. This was a transport failure, not a successful SSH exit or an unrecorded rebuild.

Main approved policy SHA256 `280eb73fe94939b3fc56cb848ecd751e194b0e1dabb33fd26d0cfa8c9ad50f3c` and staged the verified immutable inventory. At 12:28:41 UTC, the fixed promotion helper was launched once as an owned detached process with durable stdout/stderr and a launch receipt. It stopped admission, drained operations, confirmed retirement of the exact old systemd control group, verified zero operation/writer/release/background/runtime ownership, and created `/var/lib/harbor-personal-backups/p024-sendfix-311f750a18bb-20260921T122841Z`. The private matched database backup has SHA256 `77ace1abde5ccc90994a18dfcbaa173b7f50f71ea589e40838540818528c92ea`, verified against its receipt. Promotion succeeded, retained the existing preview configuration and changed only the release in site configuration. No second VPS reboot, manual membership/state repair or owner-input replay occurred. Promotion receipt SHA256: `36d2c476e5c5d73809bd1f1e54aaab285abf2dbcaf80e7cc14b802c6ee178161`.

Read-only installed checks at 12:37 UTC verified both active service executables from the new release, no old-release processes, health 200, and anonymous 401 for the root, API and exact JS/CSS assets. The supervisor was authenticated with all five configured models and had zero active or uncertain operations. Both existing writable-path drop-ins retained SHA256 `f4cc28a6d4a95d94026bd3e35ae7325bb1856384127de9488339abb2a205871d`. API remains nondelegated, supervisor delegated, and both retain `ProtectHome=read-only`; the complete migration map equals the same 18 packaged digests. Identity/routing receipt SHA256 is `6b0753765ae26bf4c995f7e6bb955c1f9a2cb0a63e272cde4aee7ef4201fbe46`, account/runtime receipt `30b0946832f0c61bed66c7e99b29c490f0ff720a2b7d86fb0d5111451566179c`, and final backup/policy/drain evidence `32dba5a76d9e77e1e51fca3d2f4360d191fe838afc74d66538cec8df51c49a9b`.

At 12:41 UTC, both affected sessions had their original generations, derived `succeeded` state and cleared background ownership, consistent with their settled durable turns. At 12:43 UTC, Main explicitly reloaded the owner's existing requested-conversation tab and observed its visible enabled message box and Attach file control, with no stale uncertainty barrier. Its saved draft is empty, so Send is correctly disabled; no input, attachment selection, removal or send was performed. The unchanged JS/CSS assets match the installed artifact. Content-free browser receipt SHA256: `8b9d800a7d392e32cc2c3cf4895a72accd6279e821ef893ba527e902c10f1bec`.

The early diagnosis observed a selected 34 KiB image, but that is not its final state. Final read-only metadata shows that image already marked deleted and draft revision 15 empty, last updated at 08:50:23 UTC. Server-side fingerprints of the draft, 16 messages and two attachment records are identical at the first pre-reboot observation (09:49:10), before authorized recovery, after `c7f2d26`, immediately before this promotion and after it. Thus this deployment preserved the established content; the fingerprints do not identify who made the earlier deletion, and this report does not attribute it. The older 225 KiB submitted image remains in history with its original failed attempt. No owner image was resent or restored. A new explicit attachment selection is required to send the removed image again. Metadata receipt SHA256: `870dc2c636c12baae3e3ea1b62c765bf205d0a32e861b81042403a0ec240a875`.

All final receipts, downloaded with SHA256 verification, are retained in `.test-runs/p024-send-fix/release-311f750/`; `preservation-comparison.json` records the exact content comparisons. Actual image-only, mixed image/file and continuation sending is established by the isolated hardened four-turn actual-model lane above, not by submitting owner content in the installed conversation. P024-R1–R5 are complete, both review roles are clear after three rounds, and the installed attachment and parent-lock issues are resolved. P018 concurrency and sidebar ordering regressions remain green. The [normal-stop retention cause](../../issues/2026-09-21-174500-personal-stop-retained-runtimes.md) and [managed interruption ordering](../../issues/2026-09-21-164130-managed-interruption-lock-order.md) remain open. Same-host checkpoints do not prove off-host/fresh-host restore; project files and special process state are excluded, and any rollback must assess post-backup work. Raw complete review-session exports remain unavailable.
