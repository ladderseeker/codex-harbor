# P008 scheduling development checkpoint — 8 September 2026

[P008](../../design/proposals/008-scheduled-tasks.md) is **Accepted / Implemented**, not Verified. Two feature review rounds and bounded module integration review have closed at integration commit `9f3b414`. The dated checkpoints below retain their original scope. The targeted DST follow-up below has passed; dedicated live-account and protected full restore/promotion evidence remain pending. The acceptance mapping preserves the historical evidence boundary.

## Implemented checkpoint

The initial unreviewed implementation commit was `8c5ab9e`; the worktree then integrated the reviewed file/terminal baseline `cfc49d2` and continued scheduling acceptance. The following checkpoint retains that historical scope.

The responsive schedule editor, preview, project-filtered lists, occurrence history and attention links use versioned authenticated APIs. Temporal0.5.1 provides bounded numeric cron/one-time evaluation with explicit gap/fold policy. PostgreSQL and pg-boss commit stable occurrence/workspace/session/turn identities and opaque wake jobs atomically. Immutable source-bound grants are rechecked through preparation and final native send; retained browser grants survive logout, while revoked/expired source PATs cannot authorize new dispatch. Existing manual/terminal ownership blocks scheduled work. Standalone schedules use committed Git content or an explicitly authorized non-Git snapshot through the shared workspace receipt contract.

The current real-stack schedule scenarios cover browser-offline existing/standalone execution, committed revision versus current committed HEAD, exclusion of dirty Local content, source-policy change denial, scoped cookie-free PATs, expiry behind an actual session lock, revoked queued grants, native uncertainty without replay, explicit P007 continuation preserving the original uncertain occurrence, offline approval expiry, cancellation reconciliation, fixed three-item catch-up, default missed-range skipping and backward-clock attention. Metadata quota fixtures exercise reserved controls after128 ordinary command results,256-record history/pagination/retention,16 enabled schedules and16 retained workspace admission. Those metadata rows are not described as physically allocated or executing work.

## Evidence retained

- Calendar and isolated real PostgreSQL/pg-boss admission: seven tests passed, including COMMIT rollback, concurrent identity deduplication, post-lock grant expiry and a blocked stale planner whose saved batch changed. `pnpm test:schedules` is the maintained entry point.
- Host real UI/API/PostgreSQL/Caddy/supervisor schedule run `e5d41bf13e` passed on Node24.11.1 at exact `a925af7afa6a401332d8914ffd36f51b823bf5f745435f70302750c7ddf926a1` /984 files. Expanded manual-overlap, missed-range and workspace-capacity run `931eef8390` also passed. External Codex and OIDC fixtures replace only those external boundaries.
- Actual managed-XFS copy scheduling passed on Linux Node24.11.1 at exact `9c7b04f779183de6226b651c88f21deeda36fd87abb3b9b3c3db027c7bd22c0b` /984 files, run `xfs-c3c245e7-c3d4-45ee-80fe-389212c45f79`. The real API/supervisor created a UID10001 workspace with exact source bytes and completed one scheduled turn with no viewer open. Both external boundaries were fixtures; model requests were zero. Exact run directories, quota limits, containers and volumes were cleaned. The result is `.test-runs/p008-managed-linux-result.json`. A subsequent source hash comparison found all887 application/package/infra files byte-identical to the host review candidate; later host changes at that comparison were tests only.
- The first managed attempt exposed the separately reviewed quota module import defect. After that exact correction, the next run showed completed storage but unavailable derived workspace and zero turns. The worker had omitted workspace ID/type/common identity from post-create validation. Passing the captured identity fields preserved strict inode checks and produced the managed pass above. The installed-module owner tracks the shared correction separately; this checkpoint does not waive its Git-worktree or installed acceptance.
- Corrected Node24 PATH contract run passed18/18, including actual pinned Codex0.153.4 no-account initialization, native history and PTY contracts. The first invocation omitted the existing Codex executable directory from PATH and had four ENOENT failures before native launch; those logs remain retained.
- Canonical Node24 `pnpm check`, `pnpm build` and `pnpm test` pass at candidate `521ea276490e80e59961aa40d41df9a9d39ac1f8dfd4f5ea9a1c5a03b99a21ae` /984 files; the integration suite has11 passing tests. Same-candidate schedule run `3654c1f260` and cumulative P001/P002/P003/P005/P007 run `6a37f4cb4e` both exited0 with identical start/end source. The latter retains its deliberate supervisor fault-test output; the final result records success.

## Regression observations and remaining integration

Cumulative run `de494a3ad5` failed the existing P005 text-selection visibility assertion immediately after navigation. Source inspection found that hidden-input `setInputFiles` can bypass the disabled visible attachment button while its draft is still loading. The isolated harness now records the visible/hidden readiness booleans and awaits the real visible control before selecting. No user mutation is retried, production behavior is unchanged, and the original failure is not retroactively assigned a cause solely from a subsequent pass. The passing `6a37f4cb4e` diagnostic recorded both controls enabled at its sample. It therefore does not establish the earlier failure's cause; that observation remains unestablished.

The installed module registry, maintenance/drain inventory, restored paused/revoked grants and nondispatchable old wake jobs remain a required integration delta on the module owner's final baseline. Migration013 must retain its identity and be tested both on a fresh database and as a lower-ID addition after016 using the exact supported-predecessor contract. The module owner is also correcting independent common-Git inode validation; its final reviewed correction is not yet imported here. No protected backup, full restore, promotion or source-host contact is claimed.

`pnpm test:live --schedules` exits2 before resource creation or model requests when its dedicated key is absent. The actual real-account branch and P008-07 remain unverified under the [live credential blocker](../../issues/2026-09-07-171225-live-runtime-credentials.md). Independent P008 feature review and its consolidated corrections are still required. Earlier module source review is not a substitute for that feature review.

## Review correction checkpoint — 8 September 2026

Independent round 1 identified admission-time Git HEAD capture, material-edit execution authority, idle control-receipt retention, one-shot pause frontier, and durable emergency schedule pause/revocation. The correction batch captures the immutable OID under source/project locks, checks edit execution/profile authority before revision changes, prunes expired receipts during periodic maintenance, keys pause against the latest grant epoch, and pauses/revokes atomically at emergency stop. A settlement-only supervisor fence retains lease/identity/deployment checks while permitting denied occurrence cleanup during emergency; unresolved queued/dispatching storage receipts remain owned until their ordinary reconciler settles. Round 2 source review found no remaining actionable finding in that scope.

Node 24.11.1 check/build, 11 integration tests and 18 pinned-runtime contracts passed at `1f91692c5091df9e9ce401203acc9ca8b5fa58ed2ef9b2581c695f612e62a909` (985 files). Real-stack schedule acceptance `4b2ae9e5f7` passed with identical start/end source, including all five corrections and emergency accepted-occurrence cleanup. Earlier `fb393ef35c` passed the five corrections before the final settlement-only refinement. The first corrective attempt `3f78aa709a` timed out on API admission while the test had SIGSTOP-held the supervisor; no database lock diagnostic established the cause. The new Git test instead confirms exact owned process exit before API admission, moves HEAD after the accepted occurrence, and restarts the supervisor before checking the resulting checkout.

Cumulative run `9a406b0873` failed P005's text-selection visibility assertion after navigation and an enabled Attach button, before reload. It repeats the earlier unestablished selection symptom; the admission changes are not established as its cause. A subsequent test-only bounded diagnostic records request method/path/status/failure and attachment/draft metadata on failure, omitting headers, bodies and content. Its result and the unresolved regression remain separate from the passed P008 feature scope. No production attachment change, retry, timeout increase or relaxed assertion was made.

The one diagnostic cumulative rerun `6a3bf52ef0` passed on Node 24.11.1, pinned runtime 0.153.4 and Chromium 1194, with start/end `c3abd75c6c73174c9650e56d2168703a66227b15a5a770ae2cbeda0e2dfbbec8` (985 files). The sole source delta from feature checkpoint `1f91692…a909` is the P005 status-only diagnostic catch. Its readiness observation was visible Attach disabled/hidden input enabled before the existing awaited ready condition; the selection then passed. This does not establish the earlier failure's cause. After that run, the diagnostic catch alone was wrapped so a diagnostic I/O failure cannot replace the original assertion; no production behavior, assertion, timeout or user mutation changed. The final documentation/type/format checks cover that small harness correction without relabeling the earlier exact digests.

## Installed-module integration checkpoint — 8 September 2026

The reviewed correction `cc38b94` is merged with module checkpoint `77b2ad50` in the feature worktree. The only new production integration behavior is fixed P008 table/status inventory, active-effect drain accounting, administrator grant revocation/pause and fresh-instance historical schedule rebind. The shared workspace verifier retains the reviewed caller/common-identity correction. Existing migration bytes and native launcher boundaries remain unchanged.

Node 24.11.1 full feature run `9cefdbb8f5` passed with exact start/end source `f0db8bcc0cd515d42b8519df6403379d7698ad8672a1659fa9ceea64259b6b47` (998 files). Alongside the prior full schedule fault/authority/quota/UI assertions, it holds an accepted occurrence during real API/supervisor maintenance, rejects new API admission, then confirms exactly one ordinary operation after maintenance release. The raw result retains the older intermediate scope label; this paragraph describes the actual current assertions without rewriting that artifact.

Focused real PostgreSQL tests apply the exact `77b2ad50` migration history, including 016, then apply unchanged 013 and compare the resulting map to a fresh full schema. Original project data and every old migration digest remain intact. A real pg-boss metadata-rebind test retains original occurrence history, pauses every schedule, revokes every grant, removes the private fixture clock and denies the old internal actor even though its original queue notification remains. This is local metadata recovery, not a protected backup or full destination deployment test.

The public predecessor manifest for artifact `8c11ca945b654d27fe1f787db0ef5b56ce3d269a88fe3883167d3820c8848f22` has a verified self-hash and exactly matches the 77b migration bytes. The production compatibility function rejects a lower-ID addition without that exact predecessor artifact/map, accepts the explicit matching record, and rejects altered identity/map records. Candidate packaging/promotion and protected off-host restore are not exercised by this component check.

Initial focused test startup observed unpublished ephemeral Docker ports immediately after `run`; the harness now waits at most five seconds for its own loopback mapping. The next migration test found a test-only non-UUID root ID and was corrected to a fresh UUID. These failures preceded the affected assertion outcomes; production policy was unchanged. The subsequent focused checks passed. Broader final checks, critical regression and independent integration review are pending at this checkpoint.

Final integration checks and evidence: Node24 check/build passed, 11 integration tests and 18 pinned0.153.4 contracts passed. Cumulative critical run `177ce1a3c3` passed with exact start/end source `ad4517f57745bb18accd7c870767acbc22335794d02fb49a1f346e3609df7c09` /998. Production bytes match feature `f0db8bcc…`; the difference is the P005 diagnostic catch scope. Earlier integrated attempts `8405f19db0` (Attach remained disabled) and `250f0fb20a` (selected text did not reach the saved-draft status within five seconds) remain failed observations with unestablished causes. The final diagnostic covers load, readiness, selection, draft save and reload, records the first failing phase plus bounded status-only evidence, and preserves every original assertion and mutation order. Its final phase-label change was inspected separately; no passing rerun is attributed to a production correction.

Actual Linux/root source administrator bridge acceptance passed on the final test source `ce8ad2847e116ac6f6f059198bd9bbaf1564fc74edfc09bdf78113fe72858a9e` /998, exactly matching host and guest. Relative to the critical checkpoint, only the final P005 diagnostic labels and schedule test's explicit Linux bridge branch/tmpfs data option changed. The real PostgreSQL/pg-boss test invokes `infra/deploy/database.ts` for fixed registry and fresh-instance rebind, retries the exact restore identity, then checks historical prompt/turn identity, paused schedules, revoked grants/browser authority, preserved queue notification and denied stale actor. Admission stays disabled pending destination activation. No source runner is contacted and no native process, installed artifact, filesystem backup or transfer is exercised by this lane. Evidence: `.test-runs/p008-linux-admin-bridge.json` and matching `p008-linux-admin-source.json`.

The dedicated B namespace used a preallocated 2 GiB XFS image (allocated bytes2,147,487,744 versus size2,147,483,648); PostgreSQL data was limited to a256MiB tmpfs. Existing installations, native state and SFTP resources were preserved. A first startup attempt correctly failed because copied esbuild was the macOS binary; the actual run used the already-pinned public Linux esbuild0.25.12 cache without changing it. The owned database container was removed; Docker showed no running containers afterward. The secret-free result, successful log and matched source identity were copied locally; the exact disposable source/dependency directory was then removed. B was returned idle with previous resources untouched.

The independent reviewer closed the bounded module merge source, feature and critical scope. The reviewer inspected the exact Linux bridge artifact and closed the final bounded integration evidence scope with no actionable finding. Full dedicated live-account and protected off-host restore/promotion gates remain unverified; this integration does not waive them.

## Acceptance evidence audit — 8 September 2026

Run `9cefdbb8f5` executed the expanded real-stack suite, despite its stale intermediate/smoke result label. Its historical JSON is unchanged. The table records that run's scope and the gaps found at the audit; the targeted follow-up below subsequently closes its DST gap. The emitter names the executed lifecycle, fault, authority, quota and maintenance scope without claiming every acceptance gate closed.

| Acceptance | Executed evidence and limit |
| --- | --- |
| P008-01 | Real UI one-time standalone creation and offline recurring existing-conversation execution, persisted unique occurrence and reopened result. |
| P008-02 | Production calendar tests cover New York gap/fold, Lord Howe non-hour gap and Apia skipped day. Real-stack tests cover UTC preview and forward/backward clock changes. Non-UTC/DST API/UI preview plus persisted occurrence assertions remain a targeted end-to-end gap. |
| P008-03 | Real supervisor restarts, skip and fixed three-item catch-up, overlap and retained cursor assertions. |
| P008-04 | Real PostgreSQL deferred COMMIT faults at occurrence/job and storage-result settlement, supervisor restart and native-fixture crash with original uncertainty/no replay. |
| P008-05 | Real pause/activation/run-now exact-key replay, active cancellation, grant-frontier and emergency future/current outcome assertions. |
| P008-06 | Offline approval expiry, scoped profile/edit and source-policy denial, expired/revoked PAT with zero dispatch, and persisted capacity/control-reserve assertions. Metadata quota prefill is not physical resource exhaustion. |
| P008-07 | Missing dedicated credentials: remains unverified; no fixture or metadata bridge result substitutes for it. |

The separate actual Linux metadata bridge checks fresh-instance paused/revoked authority and stale-job denial, with the exact predecessor/migration checks described above. They do not establish protected full filesystem restoration or promotion. No broad suite was rerun solely for this label and documentation correction.


## Targeted P008-02 DST follow-up — 8 September 2026

`pnpm test:e2e --schedules-dst` passed on Node24.11.1 in run `caa98775a7`, exact start/end source `18682c08061e1617f5a2d6cc9d2e54b359e29fd7473a959c446c99c11fcef0a3` (999 files). The real authenticated browser/API/PostgreSQL/supervisor lane rejects nonexistent one-time minutes through the UI, shows the exact timezone/local/offset/UTC cron preview, admits the schedule through the API and checks its single successful persisted occurrence and ordinary turn identity. Only external OIDC/Codex boundaries and the guarded scheduler clock are fixtures; authentication, grants and lease clocks remain real.

| Case | Expected first persisted local minute | UTC instant |
| --- | --- | --- |
| New York spring gap | 2026-03-09 02:30 | 2026-03-09 06:30Z |
| New York autumn fold | 2026-11-01 01:30 | 2026-11-01 05:30Z |
| Lord Howe half-hour gap | 2026-10-05 02:15 | 2026-10-04 15:15Z |
| Apia skipped day | 2011-12-31 12:00 | 2011-12-30 22:00Z |

During the second New York 01:30 instant, the test restarts the supervisor, waits for its actual planner pass, checks the persisted next due time is the following day and requires unchanged original occurrence/turn IDs. This closes the missing real-stack DST scope in the mapping above; earlier UTC clock-jump coverage remains separate. Evidence is `.test-runs/harbor-e2e-caa98775a7/{result.json,dst-cases.json,dst-preview.png}`. `pnpm check` passed at the same code; the owned fresh stack was cleaned by the normal harness.

Three preliminary runs failed test assumptions: project/session creation returns200, the cron label includes help text, and an idle planner updates `last_planned_at` rather than its due-work `last_observed_at`. Their logs remain retained. Corrections changed only test contract/locator/observation logic. No production code, runtime adapter or isolation boundary changed; no broad regression rerun is claimed for this test-only addition. Independent bounded source and matching-artifact review closed with no actionable finding. Live-account and protected full restore/promotion gates remain unverified.
